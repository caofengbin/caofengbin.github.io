<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>Caofengbin</title>
  
  <link href="/atom.xml" rel="self"/>
  
  <link href="http://haojen.github.io/"/>
  <updated>2017-01-21T09:57:28.324Z</updated>
  <id>http://haojen.github.io/</id>
  
  <author>
    <name>Caofengbin</name>
    <email>1050430934@qq.com</email>
  </author>
  
  <generator uri="http://hexo.io/">Hexo</generator>
  
  <entry>
    <title>逍遥游</title>
    <link href="http://haojen.github.io/2099/12/31/aaa-top-file/"/>
    <id>http://haojen.github.io/2099/12/31/aaa-top-file/</id>
    <published>2099-12-31T09:56:16.000Z</published>
    <updated>2017-01-21T09:57:28.324Z</updated>
    
    <content type="html"><![CDATA[<p>北冥有鱼，其名为鲲。鲲之大，不知其几千里也；化而为鸟，其名为鹏。鹏之大，不知几千里也；怒而飞，其翼若垂天之云。是鸟也，海运则将徙于南冥。南冥者，天池也。齐谐者，志怪者也。谐之言曰：「鹏之徙于南冥也，水击三千里，抟扶摇而上者九万里，去以六月息者也。」野马也，尘埃也，生物之以息相吹也。天之苍苍，其正色邪？其远而无所至极邪？其视下也，亦若是则已矣。</p>
<p>且夫水之积也不厚，则其大舟也无力。覆杯水于坳堂之上，则芥为之舟；置杯焉则胶，水浅而舟大也。风之积也不厚，则其负大翼也无力。故九万里而风斯在下矣，而后乃之培风，背负青天而莫之夭阏者，而后乃今将图南。蜩与学鸠笑之曰：「我决起而飞，抢榆枋（而止），时则不至而控于地而已矣，奚以之九万里而南为？」适莽苍者，三餐而反，腹犹果然；适百里者，宿舂粮；适千里者，三月聚粮。之二虫，又何知！小知不及大知，小年不及大年。奚以知其然也？朝菌不知晦朔，蟪蛄不知春秋：此小年也。楚之南有冥灵者，以五百岁为春，五百岁为秋；上古有大椿者，以八千岁为春，以八千岁为秋：此大年也。而彭祖乃今以久特闻，众人匹之，不亦悲乎！</p>
<p>汤之问棘也是已：「穷发之北，有冥海者，天池也。有鱼焉，其广数千里，未有知其修者，其名为鲲。有鸟焉，其名为鹏，背若泰山，翼若垂天之云，抟扶摇羊角而上者九万里，绝云气，负青天，然后图南，且适南冥也。斥鴳笑之曰『彼且奚适也？我腾跃而上，不过数仞而下，翱翔蓬蒿之间，此亦飞之至也。而彼且奚适也？』」此小大之辩也。</p>
<p>故夫知效一官，行比一乡，德合一君，而征一国者，其自视也亦若此矣。而宋荣子犹然笑之。且举世而誉之而不加劝，举世而非之而不加沮，定乎内外之分，辩乎荣辱之竟，斯已矣；彼其于世，未数数然也。虽然，犹有未树也。夫列子御风而行，冷然善也，旬有五日而后反；彼于致福者，未数数然也。此虽免乎行，犹有所待者也。若夫乘天地之正，而御六气之辩，以游无穷者，彼且恶乎待哉！故曰：至人无己，神人无功，圣人无名。</p>
<p>尧让天下于许由，曰：「日月出矣，而爝火不息；其于光也；不亦难乎！时雨降矣，而犹浸灌；其于泽也，不亦劳乎！夫子立而天下治，而我犹尸之，吾自视缺然，请致天下。」许由曰：「子治天下，天下既已治也，而我犹代子，吾将为名乎？名者，实之宾也，吾将为宾乎？鹪鹩巢于深林，不过一枝；偃鼠饮河，不过满腹。归休乎！君。予无所用天下为。庖人虽不治庖，尸祝不越樽俎而代之矣。」</p>
<p>肩吾问于连叔曰：「吾闻言于接舆，大而无当，往而不返。吾惊怖其言，犹河汉而无极也；大有径庭，不近人情焉。」连叔曰： 「其言谓何哉？」曰：「藐姑射之山，有神人居焉。肌肤若冰雪，淖约若处子；不食五谷，吸风饮露；乘云气，御飞龙，而游乎四海之外。其神凝，使物不疪疠而年谷熟。吾以是狂而不信也。」连叔曰：「然，瞽者无以与乎文章之观，聋者无以与乎钟鼓之声。岂唯形骸有聋盲哉！夫知亦有之。是其言也，犹时汝也。之人也，之德也，将旁礡万物以为一，世蕲乎乱，孰弊弊焉以天下为事！之人也，物莫之伤，大浸稽天而不溺，大旱金石流而土焦而不热。是其尘垢秕糠，将犹陶铸尧、舜者也，孰肯以物为事！」</p>
<p>宋人资章甫而适诸越，越人断发文身，无所用之。尧治天下之民，平海内之政，往见四子藐姑射之山，汾水之阳，窅然丧其天下焉。</p>
<p>惠子谓庄子曰：「魏王贻我以大瓠之种，我树之成，而实五石。以盛水浆，其坚不能自举也。剖之以为瓢，则瓠落无所容。非不呺然大也，吾为其无用而掊之。」庄子曰：「夫子固拙于用大矣，宋人有善为不龟手之药者，世世以洴澼洸为事。客闻之，请买其方百金。聚族而谋曰：『我世世为洴澼洸，不过数金；今一朝而鬻技百金，请与之。』客得之，以说吴王。越有难，吴王使之将，冬与越人水战，大败越人，裂地而封之。能不龟手一也，或以封，或不免于洴澼洸，则所用之异也。今子有五石之瓠，何不虑以为大樽而浮乎江湖？而忧其瓠落无所容，则夫子犹有蓬之心也夫！」</p>
<p>惠子谓庄子曰：「吾有大树，人谓之樗，其大本拥肿而不中绳墨，其小枝卷曲而不中规矩。立之涂，匠者不顾。今子之言，大而不用，众所同去也。」庄子曰：「子独不见狸狌乎？卑身而伏，以候敖者，东西跳梁，不辟高下，中于机辟，死于冈罟。今夫牦牛，其大若垂天之云，此能为大矣，而不能执鼠。今子有大树，患其无用，何不树之于无何有之乡、广莫之野？彷徨乎无为其侧，逍遥乎寝卧其下；不夭斤斧，物无害者。无所可用，安所困苦哉？」</p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;北冥有鱼，其名为鲲。鲲之大，不知其几千里也；化而为鸟，其名为鹏。鹏之大，不知几千里也；怒而飞，其翼若垂天之云。是鸟也，海运则将徙于南冥。南冥者，天池也。齐谐者，志怪者也。谐之言曰：「鹏之徙于南冥也，水击三千里，抟扶摇而上者九万里，去以六月息者也。」野马也，尘埃也，生物之以息
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title>OkHttp源码解析4：BridgeInterceptor流程</title>
    <link href="http://haojen.github.io/2018/03/29/okhttp-four/"/>
    <id>http://haojen.github.io/2018/03/29/okhttp-four/</id>
    <published>2018-03-29T01:23:30.000Z</published>
    <updated>2018-03-29T01:24:31.081Z</updated>
    
    <content type="html"><![CDATA[<h2 id="1-BridgeInterceptor概述"><a href="#1-BridgeInterceptor概述" class="headerlink" title="1.BridgeInterceptor概述"></a>1.BridgeInterceptor概述</h2><p>&emsp;&emsp;BridgeInterceptor是OkHttp的core拦截器链中的第二个拦截器，该拦截器的主要功能是：</p>
<blockquote>
<ul>
<li>在请求前对Header进行处理，如果用户有设置就直接使用，如果没有设置就使用默认的；</li>
<li>通过Chain调用下一个拦截器进行网络请求；</li>
<li>对与返回的结果，进行Gzip解压缩， Header以及cookie的处理；</li>
</ul>
</blockquote>
<p>&emsp;&emsp;相应的源码注释：</p>
<blockquote>
<ul>
<li>First it builds a network request from a user request</li>
<li>Then it proceeds to call the network</li>
<li>Finally it builds a user response from the network response</li>
</ul>
</blockquote>
<hr>
<h2 id="2-用户Request到网络Request的转换"><a href="#2-用户Request到网络Request的转换" class="headerlink" title="2.用户Request到网络Request的转换"></a>2.用户Request到网络Request的转换</h2><p>&emsp;&emsp;用户在构建一个Request对象的时侯往往只会简单设置url，RequestBody等几个属性，其实这样就已经可以发送一个网络请求了，<strong>但是真正要发送一个请求实际上没这么简单</strong>。OkHttp内部做了非常多的封装，使调用者使用这个框架去发送一个网络请求非常容易，很多细节已经在框架内做了良好的封装。<br>&emsp;&emsp;BridgeInterceptor在原来request的基础上添加了很多请求头，列举如下：</p>
<h3 id="1-Content-Type："><a href="#1-Content-Type：" class="headerlink" title="(1) Content-Type："></a>(1) Content-Type：</h3><figure class="highlight java"><table><tr><td class="code"><pre><div class="line">Content-Type: text/html; charset=UTF-<span class="number">8</span></div></pre></td></tr></table></figure>
<p>&emsp;&emsp;首部字段Content-Type说明了实体主体内对象的媒体类型。和首部字段Accept一样，字段值用type/subtype形式赋值。参数charset使用iso-8859-1或euc-jp等字符集进行赋值。</p>
<h3 id="2-Content-Length："><a href="#2-Content-Length：" class="headerlink" title="(2) Content-Length："></a>(2) Content-Length：</h3><p>&emsp;&emsp;首部字段Content-Length表明了实体主体部分的大小（单位是字节）。对实体主体进行内容编码传输时，不能再使用Content-Length首部字段。</p>
<h3 id="3-Transfer-Encoding："><a href="#3-Transfer-Encoding：" class="headerlink" title="(3) Transfer-Encoding："></a>(3) Transfer-Encoding：</h3><p>&emsp;&emsp;首部字段Transfer-Encoding规定了<strong>传输报文主体时采用的编码方式。HTTP/1.1的传输编码方式仅对分块传输编码有效。</strong>值为 chunked 表示请求体的内容大小是未知的。<strong>因此Transfer-Encoding与Content-Length两个首部不能共存。</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="keyword">long</span> contentLength = body.contentLength();</div><div class="line"><span class="keyword">if</span> (contentLength != -<span class="number">1</span>) &#123;</div><div class="line">    requestBuilder.header(<span class="string">"Content-Length"</span>, Long.toString(contentLength));</div><div class="line">    requestBuilder.removeHeader(<span class="string">"Transfer-Encoding"</span>);</div><div class="line">&#125; <span class="keyword">else</span> &#123;</div><div class="line">    requestBuilder.header(<span class="string">"Transfer-Encoding"</span>, <span class="string">"chunked"</span>);</div><div class="line">    requestBuilder.removeHeader(<span class="string">"Content-Length"</span>);</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<h3 id="4-Host："><a href="#4-Host：" class="headerlink" title="(4) Host："></a>(4) Host：</h3><p>&emsp;&emsp;首部字段Host会告知服务器，请求的资源所处的互联网主机名和端口号。<strong>Host首部字段在HTTP/1.1规范内是唯一一个必须被包含在请求内的首部字段。</strong><br>&emsp;&emsp;首部字段Host和以单台服务器分配多个域名的虚拟主机的工作机制有很密切的关联，这是首部字段Host必须存在的意义。<br>&emsp;&emsp;请求被发送至服务器时，请求中的主机名会用IP地址直接替换解决。但如果这时，<strong>*相同的IP地址下部署运行着多个域名，那么服务器就会无法理解究竟是哪个域名对应的请求。因此，就需要使用首部字段Host来明确指出请求的主机名</strong>。若服务器未设定主机名，那直接发送一个空值即可。</p>
<p><img src="http://static.zybuluo.com/caofengbin/wfnti7tmj52cj86abx0brkq9/image_1c9l3euoinaf1p4llgu1ehk163m9.png" alt="image_1c9l3euoinaf1p4llgu1ehk163m9.png-213kB"></p>
<h3 id="5-Connection："><a href="#5-Connection：" class="headerlink" title="(5) Connection："></a>(5) Connection：</h3><p>&emsp;&emsp;默认就是 “Keep-Alive”，就是一个 TCP 连接之后不会关闭，保持连接状态。</p>
<h3 id="6-Accept-Encoding："><a href="#6-Accept-Encoding：" class="headerlink" title="(6) Accept-Encoding："></a>(6) Accept-Encoding：</h3><p>&emsp;&emsp;Accept-Encoding首部字段用来告知服务器用户代理<strong>支持的内容编码及内容编码的优先级顺序</strong>。可一次性指定多种内容编码。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="keyword">boolean</span> transparentGzip = <span class="keyword">false</span>;</div><div class="line"><span class="keyword">if</span> (userRequest.header(<span class="string">"Accept-Encoding"</span>) == <span class="keyword">null</span> &amp;&amp; userRequest.header(<span class="string">"Range"</span>) == <span class="keyword">null</span>) &#123;</div><div class="line">    transparentGzip = <span class="keyword">true</span>;</div><div class="line">    requestBuilder.header(<span class="string">"Accept-Encoding"</span>, <span class="string">"gzip"</span>);</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<h3 id="7-Cookie"><a href="#7-Cookie" class="headerlink" title="(7) Cookie"></a>(7) Cookie</h3><p>&emsp;&emsp;首部字段Cookie会告知服务器，当客户端想获得HTTP状态管理支持时，就会在请求中包含从服务器接收到的Cookie。接收到多个Cookie时，同样可以以多个Cookie形式发送。</p>
<h3 id="8-User-Agent"><a href="#8-User-Agent" class="headerlink" title="(8) User-Agent"></a>(8) User-Agent</h3><p>&emsp;&emsp;这个值根据 OKHTTP 的版本不一样而不一样，<strong>表示客户端的信息。</strong></p>
<p>&emsp;&emsp;通过上面这些操作，就对一个普通的Request添加了很多头信息，让其成为可以发送网络请求的Request 。</p>
<hr>
<h2 id="3-将这个Request进行网络请求得到响应Response"><a href="#3-将这个Request进行网络请求得到响应Response" class="headerlink" title="3.将这个Request进行网络请求得到响应Response"></a>3.将这个Request进行网络请求得到响应Response</h2><p>&emsp;&emsp;这一步操作是一个向服务发送请求的操作，并且这个请求之后服务端会给客户端返回一个 Response 响应。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line">Response networkResponse = chain.proceed(requestBuilder.build());</div></pre></td></tr></table></figure>
<hr>
<h2 id="4-将网络请求回来的响应-Response-转化为用户可用的-Response"><a href="#4-将网络请求回来的响应-Response-转化为用户可用的-Response" class="headerlink" title="4.将网络请求回来的响应 Response 转化为用户可用的 Response"></a>4.将网络请求回来的响应 Response 转化为用户可用的 Response</h2><p>&emsp;&emsp;对服务器返回的响应体进行进一步的加工处理，得到最终的Response即可。</p>
<h3 id="1-获取服务器返回的-Cookie"><a href="#1-获取服务器返回的-Cookie" class="headerlink" title="(1) 获取服务器返回的 Cookie"></a>(1) 获取服务器返回的 Cookie</h3><figure class="highlight java"><table><tr><td class="code"><pre><div class="line">HttpHeaders.receiveHeaders(cookieJar, userRequest.url(), networkResponse.headers());</div></pre></td></tr></table></figure>
<p>&emsp;&emsp;下一步主要就是为了保存相应的cookie。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">receiveHeaders</span><span class="params">(CookieJar cookieJar, HttpUrl url, Headers headers)</span> </span>&#123;</div><div class="line">        <span class="keyword">if</span> (cookieJar == CookieJar.NO_COOKIES) &#123;</div><div class="line">            <span class="keyword">return</span>;</div><div class="line">        &#125;</div><div class="line"></div><div class="line">        List&lt;Cookie&gt; cookies = Cookie.parseAll(url, headers);</div><div class="line">        <span class="keyword">if</span> (cookies.isEmpty()) &#123;</div><div class="line">            <span class="keyword">return</span>;</div><div class="line">        &#125;</div><div class="line"></div><div class="line">        cookieJar.saveFromResponse(url, cookies);</div><div class="line">    &#125;</div></pre></td></tr></table></figure>
<h3 id="2-将网络请求返回的响应进行转化"><a href="#2-将网络请求返回的响应进行转化" class="headerlink" title="(2) 将网络请求返回的响应进行转化"></a>(2) 将网络请求返回的响应进行转化</h3><p>&emsp;&emsp;当 transparentGzip 为 true ，表示请求设置的 Accept-Encoding 是支持gzip 压缩的，意思就是告知服务器客户端是支持gzip 压缩的，然后再判断服务器的响应头Content-Encoding是否也是GZIP压缩的，意思就是响应体内容是否是经过GZIP压缩的，如果都成立的条件下，那么它<strong>会将 Resposonse.body().source() 的输入流 BufferedSource 转化为 GzipSource 类型</strong>，这样的目的就是让调用者在使用 Response.body().string() 获取响应内容时就是以解压的方式进行读取流数据。<br>&emsp;&emsp;相应的源码如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="keyword">if</span> (transparentGzip</div><div class="line">          &amp;&amp; <span class="string">"gzip"</span>.equalsIgnoreCase(networkResponse.header(<span class="string">"Content-Encoding"</span>))</div><div class="line">          &amp;&amp; HttpHeaders.hasBody(networkResponse)) &#123;</div><div class="line">      <span class="comment">// 通过GzipSource，转化为解压的数据流</span></div><div class="line">      GzipSource responseBody = <span class="keyword">new</span> GzipSource(networkResponse.body().source());</div><div class="line">      Headers strippedHeaders = networkResponse.headers().newBuilder()</div><div class="line">              .removeAll(<span class="string">"Content-Encoding"</span>)</div><div class="line">              .removeAll(<span class="string">"Content-Length"</span>)</div><div class="line">              .build();</div><div class="line">      responseBuilder.headers(strippedHeaders);</div><div class="line">      String contentType = networkResponse.header(<span class="string">"Content-Type"</span>);</div><div class="line">      responseBuilder.body(<span class="keyword">new</span> RealResponseBody(contentType, -<span class="number">1L</span>, Okio.buffer(responseBody)));</div><div class="line">  &#125;</div></pre></td></tr></table></figure>
<hr>
<h2 id="5-BridgeInterceptor中关于Cookie的处理"><a href="#5-BridgeInterceptor中关于Cookie的处理" class="headerlink" title="5.BridgeInterceptor中关于Cookie的处理"></a>5.BridgeInterceptor中关于Cookie的处理</h2><p>&emsp;&emsp;主要的处理逻辑由<strong>CookieJar</strong>来控制。相关的两个方法为：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="function"><span class="keyword">void</span> <span class="title">saveFromResponse</span><span class="params">(HttpUrl url, List&lt;Cookie&gt; cookies)</span></span>;</div><div class="line"><span class="function">List&lt;Cookie&gt; <span class="title">loadForRequest</span><span class="params">(HttpUrl url)</span></span>;</div></pre></td></tr></table></figure>
<hr>
<h2 id="参考链接"><a href="#参考链接" class="headerlink" title="参考链接"></a>参考链接</h2><p><a href="https://www.jianshu.com/p/ede34ab5b776" target="_blank" rel="external">1.OkHttp拦截器BridgeInterceptor的简单分析</a><br><a href="https://www.jianshu.com/p/fa0dcbfe05cd?utm_campaign=hugo&amp;utm_medium=reader_share&amp;utm_content=note&amp;utm_source=qq" target="_blank" rel="external">2.深入理解OkHttp源码及设计思想</a></p>
]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;1-BridgeInterceptor概述&quot;&gt;&lt;a href=&quot;#1-BridgeInterceptor概述&quot; class=&quot;headerlink&quot; title=&quot;1.BridgeInterceptor概述&quot;&gt;&lt;/a&gt;1.BridgeInterceptor概述&lt;/
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title>OkHttp源码解析3：RetryAndFollowUpInterceptor流程</title>
    <link href="http://haojen.github.io/2018/03/25/okhttp-three/"/>
    <id>http://haojen.github.io/2018/03/25/okhttp-three/</id>
    <published>2018-03-25T10:41:09.000Z</published>
    <updated>2018-03-25T10:41:58.391Z</updated>
    
    <content type="html"><![CDATA[<h2 id="1-RetryAndFollowUpInterceptor的作用"><a href="#1-RetryAndFollowUpInterceptor的作用" class="headerlink" title="1.RetryAndFollowUpInterceptor的作用"></a>1.RetryAndFollowUpInterceptor的作用</h2><p>&emsp;&emsp;它是一个负责失败重试和重定向的拦截器，如果我们想要实现失败重连，那么就要在OkHttpClient中进行配置，相应的配置方式如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line">OkHttpClient okHttpClient = <span class="keyword">new</span> OkHttpClient();</div><div class="line">okHttpClient.retryOnConnectionFailure();</div></pre></td></tr></table></figure>
<p>&emsp;&emsp;但是并不是所有的网络请求失败了都可以进行重连的，因此RetryAndFollowUpInterceptor内部会进行检测网络请求异常和响应码的情况，根据这些情况判断是否需要重新进行网络请求。</p>
<p>&emsp;&emsp;RetryAndFollowUpInterceptor的功能主要包括如下几点：</p>
<blockquote>
<ul>
<li>创建StreamAllocation对象；</li>
<li>调用realChain.proceed(request, streamAllocation, null, null)进行网络请求；</li>
<li>根据异常结果或者响应结果判断是否进行重新请求；</li>
<li>调用下一个拦截器，对responese进行处理，返回给上一个拦截器；</li>
</ul>
</blockquote>
<p>&emsp;&emsp;其中第二和第三点是在while(true)内部执行的，<strong>也就是系统通过死循环来实现重连机制。</strong></p>
<hr>
<h2 id="2-创建StreamAllocation对象"><a href="#2-创建StreamAllocation对象" class="headerlink" title="2.创建StreamAllocation对象"></a>2.创建StreamAllocation对象</h2><p>&emsp;&emsp;StreamAllocation在 RetryAndFollowUpInterceptor 创建，它会在 ConnectInterceptor中真正被使用到，主要就是用于获取连接服务端的 Connection和用于进行跟服务端进行数据传输的输入输出流 HttpStream。通过拦截器链进行一层层的传递。</p>
<hr>
<h2 id="3-网络请求"><a href="#3-网络请求" class="headerlink" title="3.网络请求"></a>3.网络请求</h2><p>&emsp;&emsp;<strong>因为在OkHttp中的拦截器的执行过程是一个递归的过程，也就是它内部会通过 RealInterceptorChain 这个类去负责将所有的拦截器进行串起来。只有所有的拦截器执行完毕之后，一个网络请求的响应 Response 才会被返回。</strong></p>
<p><img src="http://static.zybuluo.com/caofengbin/an108zqbygs55kmlrvqwb3b6/OkHttp%E6%8B%A6%E6%88%AA%E5%99%A8%E9%93%BE.png" alt="OkHttp拦截器链.png-53.8kB"></p>
<p>&emsp;&emsp;在上图中，从上往下就是网络请求的执行过程，从下往上就是Response相应回传的过程。</p>
<p>&emsp;&emsp;但是在执行这个过程中，难免会出现一些问题，例如连接中断，握手失败或者服务器检测到未认证等，那么这个resposne的返回码就不是正常的200了，因此说这个response并不一定是可用的，或者说在请求过程就已经抛出异常了，例如超时异常等，那么 RetryAndFollowUpInterceptor 需要依据这些问题进行判断是否可以进行重新连接。也就是第一节中分析提及到的，在一个while循环中进行检测，核心代码如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="keyword">while</span>(<span class="keyword">true</span>)&#123;</div><div class="line">    <span class="keyword">try</span>&#123;</div><div class="line">        ...</div><div class="line">        response = ((RealInterceptorChain) chain).proceed(request, </div><div class="line">        streamAllocation, <span class="keyword">null</span>, <span class="keyword">null</span>);</div><div class="line">        ...</div><div class="line">    &#125; <span class="keyword">catch</span>(RouteException e)&#123;</div><div class="line">        <span class="comment">//判断 RouteException  否可以重连</span></div><div class="line">    &#125; <span class="keyword">catch</span>(IOException e)&#123;</div><div class="line">        <span class="comment">//判断 IOException 否可以重连</span></div><div class="line">    &#125; <span class="keyword">finally</span>&#123;</div><div class="line">        <span class="comment">//释放流</span></div><div class="line">    &#125;</div><div class="line">    ...</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<hr>
<h2 id="4-失败重连机制"><a href="#4-失败重连机制" class="headerlink" title="4.失败重连机制"></a>4.失败重连机制</h2><p>&emsp;&emsp;实现失败重连机制主要基于while循环，核心代码如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="keyword">try</span> &#123;</div><div class="line">  <span class="comment">//网络请求</span></div><div class="line">  response = ((RealInterceptorChain) chain).proceed(request, streamAllocation, <span class="keyword">null</span>, <span class="keyword">null</span>);</div><div class="line">  <span class="comment">//表示是否要释放连接，在 finally 中会使用到。</span></div><div class="line">  releaseConnection = <span class="keyword">false</span>;</div><div class="line">&#125; <span class="keyword">catch</span> (RouteException e) &#123;</div><div class="line">  <span class="comment">//路由异常RouteException </span></div><div class="line">  <span class="comment">// The attempt to connect via a route failed. The request will not have been sent.</span></div><div class="line">  <span class="comment">//检测路由异常是否能重新连接</span></div><div class="line">  <span class="keyword">if</span> (!recover(e.getLastConnectException(), <span class="keyword">true</span>, request)) <span class="keyword">throw</span> e.getLastConnectException();</div><div class="line">  <span class="comment">//可以重新连接，那么就不要释放连接</span></div><div class="line">  releaseConnection = <span class="keyword">false</span>;</div><div class="line">  <span class="comment">//重新进行while循环，进行网络请求</span></div><div class="line">  <span class="keyword">continue</span>;</div><div class="line">&#125; <span class="keyword">catch</span> (IOException e) &#123;</div><div class="line">   <span class="comment">//检测该IO异常是否能重新连接</span></div><div class="line">  <span class="comment">// An attempt to communicate with a server failed. The request may have been sent.</span></div><div class="line">  <span class="keyword">if</span> (!recover(e, <span class="keyword">false</span>, request)) <span class="keyword">throw</span> e;</div><div class="line">  <span class="comment">//可以重新连接，那么就不要释放连接</span></div><div class="line">  releaseConnection = <span class="keyword">false</span>;</div><div class="line"> <span class="comment">//重新进行while循环，进行网络请求</span></div><div class="line">  <span class="keyword">continue</span>;</div><div class="line">&#125; <span class="keyword">finally</span> &#123;</div><div class="line">  <span class="comment">//当 releaseConnection 为true时表示需要释放连接了。</span></div><div class="line">  <span class="comment">// We're throwing an unchecked exception. Release any resources.</span></div><div class="line">  <span class="keyword">if</span> (releaseConnection) &#123;</div><div class="line">    streamAllocation.streamFailed(<span class="keyword">null</span>);</div><div class="line">    streamAllocation.release();</div><div class="line">  &#125;</div></pre></td></tr></table></figure>
<h3 id="1-RouteException-异常的重连机制"><a href="#1-RouteException-异常的重连机制" class="headerlink" title="(1) RouteException 异常的重连机制"></a>(1) RouteException 异常的重连机制</h3><p>&emsp;&emsp;RouteException重连机制核心流程：</p>
<blockquote>
<ul>
<li>通过 recover 方法检测该 RouteException 是否能重新连接；</li>
<li>可以重新连接，那么就不要释放连接 releaseConnection = false；</li>
<li>continue进入下一次循环，进行网络请求；</li>
<li>不可以重新连接就直接走 finally 代码块释放连接；</li>
</ul>
</blockquote>
<p>&emsp;&emsp;从下图可以看出RouteException是在<strong>获取一个HttpStream流和与Socket建立连接时出现异常才被抛出的</strong>，在抛异常的方法内部并没有显示地去捕获，因此异常会被RetryAndFollowUpInterceptor#intercept 中的 catch 捕获。</p>
<p><img src="http://static.zybuluo.com/caofengbin/u97nopeestgselxrkohtwrmy/image_1c9e0fed0fhm12d1105p2qr1pk312.png" alt="image_1c9e0fed0fhm12d1105p2qr1pk312.png-31.2kB"></p>
<h3 id="2-recover方法判断是否可以进行重连"><a href="#2-recover方法判断是否可以进行重连" class="headerlink" title="(2) recover方法判断是否可以进行重连"></a>(2) recover方法判断是否可以进行重连</h3><p>&emsp;&emsp;recover方法核心如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">boolean</span> <span class="title">recover</span><span class="params">(IOException e, StreamAllocation streamAllocation,</span></span></div><div class="line">                            <span class="keyword">boolean</span> requestSendStarted, Request userRequest) &#123;</div><div class="line">        streamAllocation.streamFailed(e);</div><div class="line"></div><div class="line">        <span class="comment">// The application layer has forbidden retries.</span></div><div class="line">        <span class="comment">// 1.判断 OkHttpClient 是否支持失败重连的机制</span></div><div class="line">        <span class="keyword">if</span> (!client.retryOnConnectionFailure()) &#123;</div><div class="line">            <span class="keyword">return</span> <span class="keyword">false</span>;</div><div class="line">        &#125;</div><div class="line"></div><div class="line">        <span class="comment">// We can't send the request body again.</span></div><div class="line">        <span class="keyword">if</span> (requestSendStarted &amp;&amp; userRequest.body() <span class="keyword">instanceof</span> UnrepeatableRequestBody)</div><div class="line">            <span class="keyword">return</span> <span class="keyword">false</span>;</div><div class="line"></div><div class="line">        <span class="comment">// This exception is fatal.</span></div><div class="line">        <span class="comment">// 2.检测该异常是否是致命的。</span></div><div class="line">        <span class="keyword">if</span> (!isRecoverable(e, requestSendStarted)) &#123;</div><div class="line">            <span class="keyword">return</span> <span class="keyword">false</span>;</div><div class="line">        &#125;</div><div class="line"></div><div class="line">        <span class="comment">// No more routes to attempt.</span></div><div class="line">        <span class="comment">// 3.是否有更多的路线</span></div><div class="line">        <span class="keyword">if</span> (!streamAllocation.hasMoreRoutes()) &#123;</div><div class="line">            <span class="keyword">return</span> <span class="keyword">false</span>;</div><div class="line">        &#125;</div><div class="line"></div><div class="line">        <span class="comment">// For failure recovery, use the same route selector with a new connection.</span></div><div class="line">        <span class="keyword">return</span> <span class="keyword">true</span>;</div><div class="line">    &#125;</div></pre></td></tr></table></figure>
<p>&emsp;&emsp;从上面源码可以看出 recover 方法主要做了以下几件事：</p>
<blockquote>
<ul>
<li>1.判断 OkHttpClient 是否支持失败重连的机制；</li>
<li>2.通过 isRecoverable 方法检测该异常是否是致命的，如果不是致命的异常，就可以进行重试；</li>
<li>3.是否有更多的路由路线，可以重试；</li>
</ul>
</blockquote>
<h3 id="3-isRecoverable方法检测异常"><a href="#3-isRecoverable方法检测异常" class="headerlink" title="(3) isRecoverable方法检测异常"></a>(3) isRecoverable方法检测异常</h3><p>&emsp;&emsp;不可恢复的几类异常：</p>
<blockquote>
<ul>
<li>ProtocolException：协议异常</li>
<li>SSLHandshakeException：ssl握手异常</li>
<li>SSLPeerUnverifiedException：ssl证书校验异常</li>
</ul>
</blockquote>
<p>&emsp;&emsp;关于连接超时的异常处理：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="keyword">if</span> (e <span class="keyword">instanceof</span> InterruptedIOException) &#123;</div><div class="line">    <span class="keyword">return</span> e <span class="keyword">instanceof</span> SocketTimeoutException &amp;&amp; !requestSendStarted;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>&emsp;&emsp;对于SocketTimeoutException的异常，表示连接超时异常，这个异常是可以进行重连的，即OkHttp内部对连接超时异常会进行自动重试处理。</p>
<h3 id="4-followUpRequest-响应码检测"><a href="#4-followUpRequest-响应码检测" class="headerlink" title="(4) followUpRequest 响应码检测"></a>(4) followUpRequest 响应码检测</h3><p>&emsp;&emsp;当代码可以执行到followUpRequest方法就表示这个请求是成功的，但是服务器返回的状态码可能不是 200 ok的情况，这时还需要对该请求进行检测，其主要就是通过返回码进行判断的。这里我们主要关注重定向相关的逻辑：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line"> <span class="keyword">case</span> HTTP_MULT_CHOICE:</div><div class="line"><span class="keyword">case</span> HTTP_MOVED_PERM:</div><div class="line"><span class="keyword">case</span> HTTP_MOVED_TEMP:</div><div class="line"><span class="keyword">case</span> HTTP_SEE_OTHER:</div><div class="line">    <span class="comment">// Does the client allow redirects?</span></div><div class="line">    <span class="comment">// 300,301,302,303几个重定向相关的状态码</span></div><div class="line">    <span class="keyword">if</span> (!client.followRedirects()) &#123;</div><div class="line">        <span class="keyword">return</span> <span class="keyword">null</span>;</div><div class="line">    &#125;</div><div class="line"></div><div class="line">    <span class="comment">// 获取响应头 Location 值，这就是要重定向的地址；</span></div><div class="line">    String location = userResponse.header(<span class="string">"Location"</span>);</div><div class="line">        <span class="keyword">if</span> (location == <span class="keyword">null</span>) &#123;</div><div class="line">             <span class="keyword">return</span> <span class="keyword">null</span>;</div><div class="line">    &#125;</div><div class="line"></div><div class="line">    HttpUrl url = userResponse.request().url().resolve(location);</div><div class="line"></div><div class="line">    <span class="comment">// Don't follow redirects to unsupported protocols.</span></div><div class="line">    <span class="keyword">if</span> (url == <span class="keyword">null</span>) <span class="keyword">return</span> <span class="keyword">null</span>;</div><div class="line"></div><div class="line">    <span class="comment">// 进行重定向的相关操作</span></div></pre></td></tr></table></figure>
<p>&emsp;&emsp;通过client.followRedirects()判断是否运行重定向，<strong>之后获取响应头 Location 值，这就是要重定向的地址</strong>，之后进行重定向相关的操作即可；</p>
<h3 id="5-重试次数判断"><a href="#5-重试次数判断" class="headerlink" title="(5) 重试次数判断"></a>(5) 重试次数判断</h3><p>&emsp;&emsp;在 RetryAndFollowUpInterceptor 内部有一个 MAX_FOLLOW_UPS常量，它表示该请求可以重试多少次，在 OKHTTP 内部中是不能超过 20 次，<strong>如果超过 20 次，那么就不会再请求了。</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> MAX_FOLLOW_UPS = <span class="number">20</span>;</div><div class="line"></div><div class="line"><span class="keyword">if</span> (++followUpCount &gt; MAX_FOLLOW_UPS) &#123;</div><div class="line">  streamAllocation.release();</div><div class="line">  <span class="keyword">throw</span> <span class="keyword">new</span> ProtocolException(<span class="string">"Too many follow-up requests: "</span> + followUpCount);</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<hr>
<h2 id="5-完整的流程图"><a href="#5-完整的流程图" class="headerlink" title="5.完整的流程图"></a>5.完整的流程图</h2><p><img src="http://static.zybuluo.com/caofengbin/quz7t2nimwvfnarv4ng35m9o/RetryAndFollowUpInterceptor%E6%B5%81%E7%A8%8B.png" alt="RetryAndFollowUpInterceptor流程.png-97.3kB"></p>
<hr>
<h2 id="参考链接"><a href="#参考链接" class="headerlink" title="参考链接"></a>参考链接</h2><p><a href="https://www.jianshu.com/p/64111e0db93c" target="_blank" rel="external">1.OKHTTP拦截器RetryAndFollowUpInterceptor的简单分析</a><br><a href="https://www.jianshu.com/p/fa0dcbfe05cd?utm_campaign=hugo&amp;utm_medium=reader_share&amp;utm_content=note&amp;utm_source=qq" target="_blank" rel="external">2.深入理解OkHttp源码及设计思想</a></p>
]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;1-RetryAndFollowUpInterceptor的作用&quot;&gt;&lt;a href=&quot;#1-RetryAndFollowUpInterceptor的作用&quot; class=&quot;headerlink&quot; title=&quot;1.RetryAndFollowUpIntercepto
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title>长连接及心跳保活原理简介</title>
    <link href="http://haojen.github.io/2018/03/16/dhcp-and-nat/"/>
    <id>http://haojen.github.io/2018/03/16/dhcp-and-nat/</id>
    <published>2018-03-16T07:10:39.000Z</published>
    <updated>2018-03-17T07:10:39.063Z</updated>
    
    <content type="html"><![CDATA[<h1 id="长连接及心跳保活原理简介"><a href="#长连接及心跳保活原理简介" class="headerlink" title="长连接及心跳保活原理简介"></a>长连接及心跳保活原理简介</h1><p>&emsp;&emsp;本文简要的分析了长连接产生的背景以及所解决的问题，并对比了keep-alive与心跳机制对长连接保活的影响，最后详细的介绍了心跳保活的两个关键因素–DHCP协议与NAT原理。如有不当之处，欢迎批评和指正。</p>
<hr>
<h2 id="1-短连接，并行连接，持久连接与长连接"><a href="#1-短连接，并行连接，持久连接与长连接" class="headerlink" title="1.短连接，并行连接，持久连接与长连接"></a>1.短连接，并行连接，持久连接与长连接</h2><h3 id="1-短连接简介"><a href="#1-短连接简介" class="headerlink" title="(1) 短连接简介"></a>(1) 短连接简介</h3><p>&emsp;&emsp;在互联网发展过程中，最为普及的应用就是HTTP超文本传输协议，而在早期–HTTP1.0的协议都是建立在TCP协议基础上，其特点就是<strong>传输完数据后，立马就释放掉该TCP链接</strong>，所以就有了形象的短连接这个称号。下图形象的展示出了在一个事务的处理过程中，各个阶段的处理时长：</p>
<p><img src="http://static.zybuluo.com/caofengbin/j2zymzi097wv2hlmolw843u8/image_1c8acepra1ko1m287rb1mi3qes9.png" alt="image_1c8acepra1ko1m287rb1mi3qes9.png-38.9kB"></p>
<p>&emsp;&emsp;可以看到，<strong>与建立TCP连接，以及传输请求和响应报文的时间相比，事务处理时间可能是很短的。</strong>短连接的性能瓶颈主要集中在如下几个方面：</p>
<h4 id="a-TCP连接的握手时延"><a href="#a-TCP连接的握手时延" class="headerlink" title="a.TCP连接的握手时延"></a>a.TCP连接的握手时延</h4><p><img src="http://static.zybuluo.com/caofengbin/5lm3ttacwktdwcmpr6gqzplv/image_1c8acqp088hdctrpe113981ucrm.png" alt="image_1c8acqp088hdctrpe113981ucrm.png-50.1kB"></p>
<p>&emsp;&emsp;在发送数据之前，<strong>TCP要传送两个分组来建立连接(现代的TCP栈都允许客户端在确认分组中发送数据)</strong>，此时，SYN/SYN+ACK握手会产生一个可测量的时延。</p>
<h4 id="b-延迟确认"><a href="#b-延迟确认" class="headerlink" title="b.延迟确认"></a>b.延迟确认</h4><p>&emsp;&emsp;每个TCP段都有一个序列号和数据完整性校验和。每个段的接收者收到完好的段时，都会向发送者回送小的<strong>确认分组</strong>。如果发送者没有在指定的窗口时间内收到确认信息，发送者就认为分组已被破坏或损毁，并重发数据。<br>&emsp;&emsp;由于确认报文很小，所以TCP允许在发往相同方向的输出数据分组中对其进行“捎带”。TCP将返回的确认信息与输出的数据分组结合在一起，可以更有效地利用网络。为了增加确认报文找到同向传输数据分组的可能性，很多TCP栈都实现了一种“延迟确认”算法。<strong>延迟确认算法会在一个特定的窗口时间（通常是100～200毫秒）内将输出确认存放在缓冲区中，以寻找能够捎带它的输出数据分组。如果在那个时间段内没有输出数据分组，就将确认信息放在单独的分组中传送。</strong></p>
<h4 id="c-TCP慢启动"><a href="#c-TCP慢启动" class="headerlink" title="c.TCP慢启动"></a>c.TCP慢启动</h4><p>&emsp;&emsp;TCP连接会随着时间进行自我“调谐”，起初会限制连接的最大速度，如果数据成功传输，<strong>会随着时间的推移提高传输的速度，这种调谐被称为TCP慢启动</strong>，用于防止因特网的突然过载和拥塞。<br>&emsp;&emsp;由于存在这种拥塞控制特性，所以<strong>新连接的传输速度会比已经交换过一定量数据的、“已调谐”连接慢一些。</strong></p>
<h3 id="2-短连接的适用场景与优缺点"><a href="#2-短连接的适用场景与优缺点" class="headerlink" title="(2) 短连接的适用场景与优缺点"></a>(2) 短连接的适用场景与优缺点</h3><p>&emsp;&emsp;<strong>短连接多用于操作频繁，点对点的通讯，而且连接数不能太多的情况</strong>。每个TCP连接的建立都需要三次握手，每个TCP连接的断开要四次挥手。适用于并发量大，但是每个用户又不需频繁操作的情况。<br>&emsp;&emsp;但是在用户需要频繁操作的业务场景下(如新用户注册，网购提交订单等)，频繁的使用短连接则会使性能时延产生叠加，如下如：</p>
<p><img src="http://static.zybuluo.com/caofengbin/ozehftn6u1mu53rc75n0jsv5/image_1c8adodlr1ad4cr91uu9unf1e3413.png" alt="image_1c8adodlr1ad4cr91uu9unf1e3413.png-61.4kB"></p>
<p>&emsp;&emsp;因此就产生了一些列关于连接性能的改进方案。</p>
<h3 id="3-并行连接"><a href="#3-并行连接" class="headerlink" title="(3) 并行连接"></a>(3) 并行连接</h3><p>&emsp;&emsp;并行连接允许客户端打开多条连接，并行地执行多个事务，每个事务都有自己的TCP连接。这样<strong>可以克服单条连接的空载时间和带宽限制，时延可以重叠起来</strong>，而且如果单条连接没有充分利用客户端的网络带宽，可以将未用带宽分配来装载其他对象。<br>&emsp;&emsp;在PC时代，利用并行连接来充分利用现代浏览器的多线程并发下载能力的场景非常广泛。<br>&emsp;&emsp;但是并行连接也会产生一定的问题，首先并行连接不一定更快，因为带宽资源有限，<strong>每个连接都会去竞争这有限的带宽，这样带来的性能提升就很小，甚至没什么提升</strong>。另外打开大量连接会消耗很多内存资源，从而引发自身的性能问题，因此每个浏览器，允许对每个域名的连接数一般是有上限的，如下图所示：</p>
<p><img src="http://static.zybuluo.com/caofengbin/6v7qnkjts4biamlmklern4ly/image_1c8aeqcro1gpnohp1j8ng18pd9.png" alt="image_1c8aeqcro1gpnohp1j8ng18pd9.png-89kB"></p>
<h3 id="4-持久连接"><a href="#4-持久连接" class="headerlink" title="(4) 持久连接"></a>(4) 持久连接</h3><p>&emsp;&emsp;HTTP1.0版本以后，<strong>允许HTTP设备在事务处理结束之后将TCP连接保持在打开状态，以便为未来的HTTP请求重用现存的连接。在事务处理结束之后仍然保持在打开状态的TCP连接被称为持久连接</strong>。非持久连接会在每个事务结束之后关闭。持久连接会在不同事务之间保持打开状态，直到客户端或服务器决定将其关闭为止。<br>&emsp;&emsp;<strong>现在很多方案都会采用持久连接+新连接结合的方式，这种方式尽可能的减少了新建连接的浪费，同时当现有连接没有办法满足需求的时候，可以建立新连接满足需求，比较灵活。</strong><br>&emsp;&emsp;持久连接的时间参数，通常由服务器设定，比如nginx的keepalivetimeout，keepalive timout时间值意味着：<strong>一个http产生的tcp连接在传送完最后一个响应后，还需要hold住keepalive_timeout秒后，才开始关闭这个连接；</strong></p>
<p>&emsp;&emsp;持久连接与并行连接相比，带来的优势如下：</p>
<blockquote>
<ul>
<li>避免了每个事务都会打开/关闭一条新的连接，造成时间和带宽的耗费；</li>
<li>避免了TCP慢启动特性的存在导致的每条新连接的性能降低；</li>
<li>可打开的并行连接数量实际上是有限的，持久连接则可以减少建立的连接的数量；</li>
</ul>
</blockquote>
<h3 id="5-长连接"><a href="#5-长连接" class="headerlink" title="(5) 长连接"></a>(5) 长连接</h3><p>&emsp;&emsp;长连接与持久连接本质上非常的相似，持久连接侧重于HTTP应用层，特指一次请求结束之后，服务器会在自己设置的keepalivetimeout时间到期后才关闭已经建立的连接。<strong>长连接则是client方与server方先建立连接，连接建立后不断开，然后再进行报文发送和接收，直到有一方主动关闭连接为止。</strong></p>
<p>&emsp;&emsp;长连接的适用场景也非常的广泛：</p>
<blockquote>
<ul>
<li>监控系统：后台硬件热插拔、LED、温度、电压发生变化等；</li>
<li>IM应用：收发消息的操作；</li>
<li>即时报价系统：例如股市行情push等；</li>
<li>推送服务：各种App内置的push提醒服务；</li>
</ul>
</blockquote>
<p>&emsp;&emsp;像以上这些连接，如果每次操作都要建立连接然后再操作的话处理速度会降低，并且时效性也不高。通过长连接，第一次连接上以后每次直接发送数据就可以了，不用再建立TCP连接。</p>
<hr>
<h2 id="2-长连接保活，Keep-Alive与心跳保活技术"><a href="#2-长连接保活，Keep-Alive与心跳保活技术" class="headerlink" title="2.长连接保活，Keep-Alive与心跳保活技术"></a>2.长连接保活，Keep-Alive与心跳保活技术</h2><h3 id="1-为何需要长连接保活"><a href="#1-为何需要长连接保活" class="headerlink" title="(1) 为何需要长连接保活"></a>(1) 为何需要长连接保活</h3><p>&emsp;&emsp;上一节的分析可以看到，对于客户端而言，使用TCP长连接来实现业务的好处在于：<strong>在当前连接可用的情况下，每一次请求都只是简单的数据发送和接受，免去了DNS解析，连接建立，TCP慢启动等时间，大大加快了请求的速度，同时也有利于接收服务器的实时消息。</strong><br>&emsp;&emsp;在使用TCP长连接的业务场景下，<strong>保持长连接的可用性非常重要</strong>。如果长连接无法很好地保持，<strong>在连接已经失效的情况下继续发送请求会导致迟迟收不到响应直到超时，又需要一次连接建立的过程，其效率甚至还不如直接使用短连接</strong>。而连接保持的前提必然是检测连接的可用性，并在连接不可用时主动放弃当前连接并建立新的连接。</p>
<h3 id="2-心跳保活"><a href="#2-心跳保活" class="headerlink" title="(2) 心跳保活"></a>(2) 心跳保活</h3><p>&emsp;&emsp;App实现长连接保活的方式通常是采用<strong>应用层心跳</strong>，通过心跳包的超时和其他条件(网络切换)来执行重连操作。心跳一般是指某端(绝大多数情况下是客户端)每隔一定时间向对端发送自定义指令，以判断双方是否存活，因其按照一定间隔发送，类似于心跳，故被称为心跳指令。</p>
<h3 id="3-Keep-Alive可否实现保活？"><a href="#3-Keep-Alive可否实现保活？" class="headerlink" title="(3) Keep-Alive可否实现保活？"></a>(3) Keep-Alive可否实现保活？</h3><h4 id="a-HTTP中的Keep-Alive"><a href="#a-HTTP中的Keep-Alive" class="headerlink" title="a.HTTP中的Keep-Alive"></a>a.HTTP中的Keep-Alive</h4><p>&emsp;&emsp;实现HTTP/1.0 keep-alive连接的客户端可以通过包含Connection:Keep-Alive首部请求将一条连接保持在打开状态，如果服务器愿意为下一条请求将连接保持在打开状态，就在响应中包含相同的首部。如果响应中没有Connection: Keep-Alive首部，客户端就认为服务器不支持keep-alive，会在发回响应报文之后关闭连接。HTTP/1.1以后Keep-Alive是默认打开的。</p>
<h4 id="c-TCP中的Keep-Alive"><a href="#c-TCP中的Keep-Alive" class="headerlink" title="c.TCP中的Keep-Alive"></a>c.TCP中的Keep-Alive</h4><p>&emsp;&emsp;TCP协议的实现中，提供了KeepAlive报文，用来探测连接的对端是否存活。在应用交互的过程中，可能存在以下几种情况：</p>
<blockquote>
<ul>
<li>客户端或服务器意外断电，死机，崩溃，重启；</li>
<li>中间网络已经中断，而客户端与服务器并不知道；</li>
</ul>
</blockquote>
<p>&emsp;&emsp;利用保活探测功能，可以探知这种对端的意外情况，从而保证在意外发生时，可以释放半打开的TCP连接。TCP保活报文交互过程如下：</p>
<p><img src="http://static.zybuluo.com/caofengbin/zejn7gybbbsgciwfjdo13jls/image_1c8ai46ncp561qieot2n0h15p5m.png" alt="image_1c8ai46ncp561qieot2n0h15p5m.png-174.7kB"></p>
<p>&emsp;&emsp;虽然TCP提供了KeepAlive机制，但是并不能替代应用层心跳保活。原因主要如下：</p>
<blockquote>
<ul>
<li>(1) Keep Alive机制开启后，TCP层将在定时时间到后发送相应的KeepAlive探针以确定连接可用性。默认时间为7200s(两小时)，失败后重试10次，每次超时时间75s。显然默认值无法满足移动网络下的需求；</li>
<li>(2) 即便修改了(1)中的默认值，也不能很好的满足业务需求。<strong>TCP的KeepAlive用于检测连接的死活而不能检测通讯双方的存活状态</strong>。比如某台服务器因为某些原因导致负载超高，无法响应任何业务请求，但是使用TCP探针则仍旧能够确定连接状态，这就是<strong>典型的连接活着但业务提供方已死的状态</strong>，对客户端而言，这时的最好选择就是断线后重新连接其他服务器，而不是一直认为当前服务器是可用状态，一直向当前服务器发送些必然会失败的请求。</li>
<li>(3) socks代理会让Keep Alive失效。socks协议只管转发TCP层具体的数据包，而不会转发TCP协议内的实现细节的包。所以，一个应用如果使用了socks代理，那么TCP的KeepAlive机制就失效了。</li>
<li>(4) 部分复杂情况下Keep Alive会失效，如路由器挂掉，网线直接被拔除等；</li>
</ul>
</blockquote>
<p>&emsp;&emsp;因此，KeepAlive并不适用于检测双方存活的场景，这种场景还得依赖于应用层的心跳。<strong>应用层心跳也具备着更大的灵活性，可以控制检测时机，间隔和处理流程，甚至可以在心跳包上附带额外信息。</strong></p>
<h3 id="4-影响心跳频率的关键因素"><a href="#4-影响心跳频率的关键因素" class="headerlink" title="(4) 影响心跳频率的关键因素"></a>(4) 影响心跳频率的关键因素</h3><p>&emsp;&emsp;通过上一节的分析可以看到<strong>应用层心跳是检测连接有效性以及判断双方是否存活的有效方式</strong>。但是心跳过于频繁会带来耗电和耗流量的弊病，心跳频率过低则会影响连接检测的实时性。业内关于心跳时间的设置和优化，主要基于如下几个因素：</p>
<blockquote>
<ul>
<li>1.NAT超时–大部分移动无线网络运营商在链路一段时间没有数据通讯时，会淘汰 NAT表中的对应项，造成链路中断；</li>
<li>2.DHCP租期–DHCP租期到了需要主动续约，否则会继续使用过期IP导致长连接偶然的断连；</li>
<li>3.网络状态变化–手机网络和WIFI网络切换、网络断开和连上等情况有网络状态的变化，也会使长连接变为无效连接；</li>
</ul>
</blockquote>
<p>&emsp;&emsp;网络状态变化导致长连接变为无效连接的原因很容易理解。但是NAT超时和DHCP租期的问题对长连接保活存在的影响就涉及到网络协议底层的细节了。后续会对这两个原理进行相应的分析。</p>
<hr>
<h2 id="3-DHCP原理浅析及其对心跳保活的影响"><a href="#3-DHCP原理浅析及其对心跳保活的影响" class="headerlink" title="3.DHCP原理浅析及其对心跳保活的影响"></a>3.DHCP原理浅析及其对心跳保活的影响</h2><h3 id="1-DHCP协议简介"><a href="#1-DHCP协议简介" class="headerlink" title="(1) DHCP协议简介"></a>(1) DHCP协议简介</h3><p>&emsp;&emsp;DHCP协议全称为<strong>Dynamic Host Configuration Protocol– 动态主机配置协议，主要用于在一个局域网里为主机动态的分配IP地址</strong>。DHCP有三种分配IP地址方式：</p>
<blockquote>
<ul>
<li>自动分配：DHCP给客户端分配永久性的IP地址；</li>
<li>动态分配：DHCP给客户端分配的IP地址过一段时间后会过期，或者客户端可以主动释放该地址(最常用的方式)；</li>
<li>手动配置：由用户手动为客户端指定IP地址；</li>
</ul>
</blockquote>
<h3 id="2-DHCP工作流程详解"><a href="#2-DHCP工作流程详解" class="headerlink" title="(2) DHCP工作流程详解"></a>(2) DHCP工作流程详解</h3><p>&emsp;&emsp;DHCP协议为客户端分配IP的过程大致如下：</p>
<p><img src="http://static.zybuluo.com/caofengbin/tsvv4gdmmogggpjtnhpjdenm/Image.png" alt="DHCP分配IP的过程"></p>
<h4 id="1-DHCP-Discover"><a href="#1-DHCP-Discover" class="headerlink" title="1.DHCP Discover"></a>1.DHCP Discover</h4><p>&emsp;&emsp;DHCP客户端(需要上网的设备)<strong>以广播(因为客户端还不知道DHCP服务器的IP地址)的方式发送DHCP Discover包</strong>，来寻找DHCP服务器，即向地址255.255.255.255发送特定的广播信息。网络上每一台安装了TCP/IP协议的主机都会收到该广播消息，但只有DHCP服务器才会做出响应。</p>
<p><img src="http://static.zybuluo.com/caofengbin/m6qlec5ucjp021ir1qn4k1lp/DHCPDiscover1.png" alt="DHCPDiscover1.png-31.2kB"></p>
<p><img src="http://static.zybuluo.com/caofengbin/hcylmyvqzrb3sdexj124l0of/DHCPDiscover2.png" alt="DHCPDiscover2.png-70kB"></p>
<h4 id="2-DHCP-Offer"><a href="#2-DHCP-Offer" class="headerlink" title="2.DHCP Offer"></a>2.DHCP Offer</h4><p>&emsp;&emsp;在该阶段，DHCP服务器提供IP地址。在网络中接收到DHCP Discover包的DHCP服务器，都会做出响应。这些DHCP服务器从尚未出租的IP地址中挑选一个给客户端，向客户端发送一个包含IP地址和其他设置的DHCP Offer包。</p>
<p><img src="http://static.zybuluo.com/caofengbin/44dgy9u21i3mxyo39x8yxc5r/DHCPOffer1.png" alt="DHCPOffer1.png-36.6kB"></p>
<p><img src="http://static.zybuluo.com/caofengbin/wg39d9v69q6pinpr00eh08d9/DHCPOffer2.png" alt="DHCPOffer2.png-128.8kB"></p>
<h4 id="3-DHCP-Request"><a href="#3-DHCP-Request" class="headerlink" title="3.DHCP Request"></a>3.DHCP Request</h4><p><img src="http://static.zybuluo.com/caofengbin/c6h3se0c8fth33ch0vfwcnxa/DHCP%E7%A1%AE%E8%AE%A4.png" alt="DHCP确认.png-218.1kB"><br>&emsp;&emsp;该阶段需要DHCP客户端选择某台DHCP服务器提供的IP地址，如上图所示，可以看到3台DHCP服务器都向客户端发送了DHCP Offer，此时，<strong>DHCP客户端只能接受第一个收到的DHCP Offer包信息。然后，以广播的方式回答一个DHCP Request请求信息，该信息中包含它所选定的DHCP服务器请求IP地址的内容。</strong></p>
<p><img src="http://static.zybuluo.com/caofengbin/jps34xu9dmpydzvhljr1vv0m/DHCPRequest.png" alt="DHCPRequest.png-84.4kB"></p>
<h4 id="4-DHCP-ACK"><a href="#4-DHCP-ACK" class="headerlink" title="4.DHCP ACK"></a>4.DHCP ACK</h4><p>&emsp;&emsp;确认阶段，DHCP服务器确认所提供的的IP地址阶段，告诉DHCP客户端可以使用它所提供的IP地址。</p>
<p><img src="http://static.zybuluo.com/caofengbin/a69vdqcvzna1j02zpr1zhsf2/DHCP_ACK.png" alt="DHCP_ACK.png-88.7kB"></p>
<h3 id="3-DHCP的续租问题"><a href="#3-DHCP的续租问题" class="headerlink" title="(3) DHCP的续租问题"></a>(3) DHCP的续租问题</h3><p>&emsp;&emsp;在DHCP ACK报文中，有3个关于续租时间相关的字段：</p>
<blockquote>
<ul>
<li>Lease Time：<br>IP地址租约时间，超过了这个时间后，IP地址被DHCP服务器收回；</li>
<li>Renewal Time：<br>默认为Lease Time的1/2，表示客户端需要进行续约的时间。<strong>客户端发送一个DHCP REQUEST消息给原始的DHCP服务器</strong>，并等待回复。DHCP服务器返回DHCP ACK则表示同意续期，客户端更新自己的Renewal Time与Rebinding Time即可。</li>
<li>Rebinding Time：<br>默认为Lease Time的7/8，客户端在续期失败的情况下，Rebinding Time到期时，会向<strong>局域网内广播发送一条DHCP REQUEST消息</strong>，如果还没有DHCP服务器响应直至租约Lease Time到期，将恢复到初始状态。</li>
</ul>
</blockquote>
<p>&emsp;&emsp;DHCP完成的状态变迁流程如下：</p>
<p><img src="http://static.zybuluo.com/caofengbin/13nvwh580a50hcyj3hlkk9c6/DHCP%E7%8A%B6%E6%80%81%E5%9B%BE.PNG" alt="DHCP状态图.PNG-156.6kB"></p>
<h3 id="4-DHCP租期问题对心跳保活的影响"><a href="#4-DHCP租期问题对心跳保活的影响" class="headerlink" title="(4) DHCP租期问题对心跳保活的影响"></a>(4) DHCP租期问题对心跳保活的影响</h3><p>&emsp;&emsp;在设计心跳频率时，DHCP租期是一个不确定因素，但是原则是心跳的最大间隔应该低于DHCP的租期时间。<br>&emsp;&emsp;另外，在Android的一些版本上，<strong>存在DHCP租期到了不会主动续约并且会继续使用过期IP的bug</strong>。这个问题导致的问题表象是，在超过租期的某个时间点（没有规律）会导致IP过期，老的TCP连接不能正常收发数据。并且系统没有网络变化事件，只有等应用判断主动建立新的TCP连接才引起安卓设备重新向DHCP Server申请IP租用。详情可见–<a href="https://www.net.princeton.edu/android/android-stops-renewing-lease-keeps-using-IP-address-11236.html" target="_blank" rel="external">Android 2.1 - 4.1.1 Allows DHCP Lease to Expire, Keeps Using IP Address。</a></p>
<hr>
<h2 id="4-NAT原理浅析及其对心跳保活的影响"><a href="#4-NAT原理浅析及其对心跳保活的影响" class="headerlink" title="4.NAT原理浅析及其对心跳保活的影响"></a>4.NAT原理浅析及其对心跳保活的影响</h2><h3 id="1-NAT技术产生的背景"><a href="#1-NAT技术产生的背景" class="headerlink" title="(1) NAT技术产生的背景"></a>(1) NAT技术产生的背景</h3><p>&emsp;&emsp;在网络协议制定的初期设计网络地址的时候，<strong>32bits位长即2的32次幂台终端设备连入互联网已经是一个非常大的数量了，再加上增加ip的长度（即使是从4字节增到6字节）对当时设备的计算、存储、传输成本也是相当巨大的</strong>。因此IP地址设计为了32位，并且在早期所有需要上网的设备都有自己的IP地址，也就是说那个时候<strong>没有内网和外网的区别，所有客户端都是直接连接到互联网的</strong>。<br>&emsp;&emsp;进入20世纪90年代之后，互联网逐步向公众普及，接入互联网的设备数量也快速增长，<strong>如果还用原来的方法接入，过不了多久，可分配的地址就用光了</strong>。如果不能保证每台设备有唯一不重复的地址，就会从根本上影响网络包的传输，这是一个非常严重的问题。如果任由这样发展下去，不久的将来，一旦固定地址用光，新的设备就无法接入了。在这个背景下NAT技术诞生了(<strong>虽然ipv6也是解决办法，但始终普及不开来，而且未来到底ipv6够不够用仍是未知</strong>)。</p>
<h3 id="2-NAT技术的基本工作原理"><a href="#2-NAT技术的基本工作原理" class="headerlink" title="(2) NAT技术的基本工作原理"></a>(2) NAT技术的基本工作原理</h3><h4 id="a-NAT技术的本质"><a href="#a-NAT技术的本质" class="headerlink" title="a.NAT技术的本质"></a>a.NAT技术的本质</h4><p>&emsp;&emsp;NAT技术主要是为了解决公网IP地址不足的问题，所以才会采取这种地址转换的策略。本质上就是<strong>让一群机器公用同一个IP，这样就暂时解决了IP短缺的问题。</strong></p>
<h4 id="b-私有地址与公有地址"><a href="#b-私有地址与公有地址" class="headerlink" title="b.私有地址与公有地址"></a>b.私有地址与公有地址</h4><p>&emsp;&emsp;不同内网之间是完全独立的。内网之间不会有网络包流动，<strong>即使内网A的某台服务器和内网B的某台客户端具有相同的IP地址也没关系，因为它们之间不会进行通信</strong>。只要在每个内网自己的范围内，能够明确判断网络包的目的地就可以了，是否和其他局域网中的内网地址重复无关紧要，只要每个局域网自己的网络是相互独立的，<br>就不会出现问题。<br>&emsp;&emsp;解决地址不足的问题，利用的就是这样的原理，即局域网的内部设备的地址不一定要和其他局域网中的内部设备地址不重复。这样一来，局域网的内部设备就不需要分配固定地址了，从而大幅节省了IP地址。内部设备分配IP地址的方式，就是通过上一节的DHCP协议进行。<strong>内网地址的分配有相应的规则，规定某些地址是用于内网的，这些地址叫作私有地址，而原来的固定地址则叫作公有地址。</strong><br>&emsp;&emsp;在内网中可用作私有地址的范围仅限以下这些：</p>
<blockquote>
<ul>
<li>10.0.0.0～10.255.255.255</li>
<li>172.16.0.0～172.31.255.255</li>
<li>192.168.0.0～192.168.255.255</li>
</ul>
</blockquote>
<p>&emsp;&emsp;在制定私有地址规则时，这些地址属于公有地址中还没有分配的范围。换句话说，<strong>私有地址本身并没有什么特别的结构，只不过是将公有地址中没分配的一部分拿出来规定只能在内网使用它们而已。</strong></p>
<h4 id="c-地址转换-NAT-机制的加入"><a href="#c-地址转换-NAT-机制的加入" class="headerlink" title="c.地址转换(NAT)机制的加入"></a>c.地址转换(NAT)机制的加入</h4><p>&emsp;&emsp;<strong>当内网和互联网之间需要传输包的时候，问题就出现了，因为如果很多地方都出现相同的地址，包就无法正确传输了。</strong>因此当公司内网和互联网连接的时候，需要采用下图这样的结构，即将公司内网分成两个部分，一部分是对互联网开放的服务器，另一部分是公司内部设备。其中<strong>对互联网开放的部分分配公有地址，可以和互联网直接进行通信。相对地，内网部分则分配私有地址，内网中的设备不能和互联网直接收发网络包，而是通过一种特别的机制进行连接，这个机制就叫地址转换。</strong></p>
<p><img src="http://static.zybuluo.com/caofengbin/0ko51uw63ye4uqucbpyy83hs/%E5%85%AC%E6%9C%89%E5%9C%B0%E5%9D%80%E5%92%8C%E7%A7%81%E6%9C%89%E5%9C%B0%E5%9D%80.png" alt="公有地址和私有地址.png-70.6kB"></p>
<h4 id="d-地址转换-NAT-的基本原理"><a href="#d-地址转换-NAT-的基本原理" class="headerlink" title="d.地址转换(NAT)的基本原理"></a>d.地址转换(NAT)的基本原理</h4><p>&emsp;&emsp;地址转换的基本原理是<strong>在转发网络包时对IP头部中的IP地址和端口号进行改写</strong>，如下图所示：TCP连接操作的第一个包被转发到互联网时，会将发送方IP地址从私有地址改写成公有地址。这里使用的公有地址是<strong>地址转换设备的互联网接入端口的地址</strong>。与此同时，端口号也需要进行改写，地址转换设备会随机选择一个空闲的端口。然后，<strong>改写前的私有地址和端口号，以及改写后的公有地址和端口号，会作为一组相对应的记录保存在地址转换设备内部的一张表(NAT表)中</strong>。</p>
<p><img src="http://static.zybuluo.com/caofengbin/9sltv39ks85l184d2sxywnb7/NAT%E5%8E%9F%E7%90%86.png" alt="地址转换的基本原理.png-103kB"></p>
<p>&emsp;&emsp;改写发送方IP地址和端口号之后，包就被发往互联网，最终到达服务器，然后服务器会返回一个包。服务器返回的包的接收包是原始包的发送方，<strong>因此返回的包的接收方就是改写后的公有地址和端口号。这个公有地址其实是地址转换设备的地址</strong>，因此这个返回包就会到达地址转换设备。接下来，地址转换设备会<strong>从地址对应表中通过公有地址和端口号找到相对应的私有地址和端口号，并改写接收方信息</strong>，然后将包发给局域网的内部设备，这样包就能够到达原始的发送方了。</p>
<h4 id="e-为什么需要改写端口号？"><a href="#e-为什么需要改写端口号？" class="headerlink" title="e.为什么需要改写端口号？"></a>e.为什么需要改写端口号？</h4><p>&emsp;&emsp;<strong>早期的地址转换机制是只改写地址，不改写端口号的</strong>。使用这种方法的前提是私有地址和公有地址必须一一对应，也就是说，<strong>有多少台设备需要同时访问互联网，就需要多少个公有地址。</strong>访问动作结束后可以删除对应表中的记录，这时同一个公有地址可以分配给其他设备使用。<br>&emsp;&emsp;后续随着互联网的发展，同一个局域网里的设备也越来越多。改写端口号正是为了解决这个问题。客户端一方的端口号本来就是从空闲端口中随机选择的，因此改写了也不会有问题。端口号是一个16比特的数值，总共可以分配出几万个端口，因此如果用公有地址加上端口的组合对应一个私有地址，一个公有地址就可以对应几万个私有地址，这种方法<strong>提高了公有地址的利用率。</strong></p>
<h3 id="3-NAT技术带来的弊端"><a href="#3-NAT技术带来的弊端" class="headerlink" title="(3) NAT技术带来的弊端"></a>(3) NAT技术带来的弊端</h3><p>&emsp;&emsp;首先，NAT使IP会话的保持时效变短。因为NAT表中的每一条记录，在会话静默的这段时间，NAT网关会进行老化操作。这是任何一个NAT网关必须做的事情，因为IP和端口资源有限，通信的需求无限，所以必须在会话结束后回收资源。<strong>通常TCP会话通过协商的方式主动关闭连接，NAT网关可以跟踪这些报文，但总是存在例外的情况，要依赖自己的定时器(NAT超时机制)去回收资源。</strong>通过NAT超时机制回收会带来一个问题，如果应用需要维持连接的时间大于NAT网关的设置，通信就会意外中断。因为网关回收相关转换表资源以后，新的数据到达时就找不到相关的转换信息，必须建立新的连接。</p>
<p>&emsp;&emsp;其次，NAT在实现上将多个内部主机发出的连接复用到一个IP上，这就使依赖IP进行主机跟踪的机制都失效了。基于用户行为的日志分析也变得困难，因为一个IP被很多用户共享，如果存在恶意的用户行为，很难定位到发起连接的那个主机。NAT隐蔽了通信的一端，把简单的事情复杂化了。</p>
<p>&emsp;&emsp;NAT一下对IP端到端模型产生了破坏。NAT通过修改IP首部的信息变换通信的地址。但是在这个转换过程中只能基于一个会话单位。当一个应用需要保持多个双向连接时，麻烦就很大。NAT不能理解多个会话之间的关联性，无法保证转换符合应用需要的规则。当NAT网关拥有多个公有IP地址时，一组关联会话可能被分配到不同的公网地址，这通常是服务器端无法接受的。更为严重的是，当公网侧的主机要主动向私网侧发送数据时，NAT网关没有转换这个连接需要的关联表，这个数据包无法到达私网侧的主机。这些反方向发送数据的连接总有应用协议的约定或在初始建立的会话中进行过协商。</p>
<h3 id="4-NAT超时机制对心跳保活"><a href="#4-NAT超时机制对心跳保活" class="headerlink" title="(4) NAT超时机制对心跳保活"></a>(4) NAT超时机制对心跳保活</h3><p>&emsp;&emsp;上一节已经分析到，NAT超时机制会带来一个问题，如果应用需要维持连接的时间大于NAT网关的设置，通信就会意外中断。因为网关回收相关转换表资源以后，新的数据到达时就找不到相关的转换信息，必须建立新的连接。当这个新数据是由公网侧向私网侧发送时，就会发生无法触发新连接建立，也不能通知到私网侧的主机去重建连接的情况。这时候通信就会中断，不能自动恢复。即使新数据是从私网侧发向公网侧，因为重建的会话表往往使用不同于之前的公网IP和端口地址，公网侧主机也无法对应到之前的通信上，导致用户可感知的连接中断。<br>&emsp;&emsp;所以普遍的一个做法就是使用心跳保活，在一段时间没有数据需要发送时，主动发送一个NAT能感知到而又没有实际数据的保活消息–心跳，这么做的主要目的就是重置NAT的会话定时器。<strong>理想的情况下,客户端应当以略小于NAT超时时间的间隔来发送心跳包。</strong>根据微信团队测试的一些数据，一些常用网络的NAT超时时间如下表所示：</p>
<table>
<thead>
<tr>
<th>地区/网络</th>
<th>NAT超时时间</th>
</tr>
</thead>
<tbody>
<tr>
<td>中国移动3G和2G</td>
<td>5分钟</td>
</tr>
<tr>
<td>中国联通2G</td>
<td>5分钟</td>
</tr>
<tr>
<td>中国电信3G</td>
<td>大于28分钟</td>
</tr>
<tr>
<td>美国3G</td>
<td>大于28分钟</td>
</tr>
<tr>
<td>台湾3G</td>
<td>大于28分钟</td>
</tr>
</tbody>
</table>
<hr>
<h2 id="5-小结"><a href="#5-小结" class="headerlink" title="5.小结"></a>5.小结</h2><p>&emsp;&emsp;本文简单的总结了一些短连接的劣势，以及几种改进的连接方案，并引出长连接的概念和相关的使用场景，并详细对比了keep-alive和心跳机制的不同之处，强调心跳机制对长连接保活的重要意义。并对影响心跳时间的两个关键–DHCP与NAT进行了简单的介绍。现在动态心跳的方案也越来越普及，网上已经有不少文章做了相关的分享，本文参考文献部分也有相关的链接。如有不当之处，敬请批评指正。</p>
<hr>
<h2 id="参考文献"><a href="#参考文献" class="headerlink" title="参考文献"></a>参考文献</h2><p><a href="http://www.tldp.org/HOWTO/html_single/TCP-Keepalive-HOWTO/" target="_blank" rel="external">1. TCP Keepalive HOWTO</a><br><a href="http://www.52im.net/forum.php?mod=viewthread&amp;tid=133&amp;ctid=10" target="_blank" rel="external">2. 移动端IM开发需要面对的技术问题</a><br><a href="http://www.52im.net/thread-281-1-1.html" target="_blank" rel="external">3. 为什么说基于TCP的移动端IM仍然需要心跳保活</a><br><a href="http://www.tcpipguide.com/free/t_DHCPGeneralOperationandClientFiniteStateMachine.htm" target="_blank" rel="external">4. DHCP General Operation and Client Finite State Machine</a><br><a href="http://www.utdallas.edu/~ravip/cs6390/fall01/dhcp.figure.pdf" target="_blank" rel="external">5. dhcp.figure</a><br><a href="https://www.net.princeton.edu/android/android-stops-renewing-lease-keeps-using-IP-address-11236.html" target="_blank" rel="external">6. Android 2.1 - 4.1.1 Allows DHCP Lease to Expire, Keeps Using IP Address。</a><br><a href="https://book.douban.com/subject/26941639/" target="_blank" rel="external">7. 《网络是怎么连接的》–3.4.2节：地址转换的基本原理</a><br><a href="https://book.douban.com/subject/10746113/" target="_blank" rel="external">8. 《HTTP权威指南》–第4章：连接管理</a><br><a href="https://github.com/chokcoco/cnblogsArticle/issues/1" target="_blank" rel="external">9. 前端性能–浅谈域名发散与域名收敛</a><br><a href="https://www.cnblogs.com/hukey/p/5481173.html" target="_blank" rel="external">10. Tcp Keepalive 和 HTTP Keepalive详解</a><br><a href="http://www.52im.net/thread-341-1-1.html" target="_blank" rel="external">11.Android端消息推送总结：实现原理、心跳保活、遇到的问题等</a><br><a href="http://www.52im.net/forum.php?mod=viewthread&amp;tid=50&amp;highlight=NAT" target="_blank" rel="external">12.P2P技术详解(一)：NAT详解——详细原理、P2P简介</a><br><a href="https://www.zhihu.com/question/66893227" target="_blank" rel="external">13.知乎问答–NAT与DHCP的区别</a><br><a href="http://www.52im.net/thread-783-1-1.html" target="_blank" rel="external">14.一种Android端IM智能心跳算法的设计与实现探讨</a></p>
]]></content>
    
    <summary type="html">
    
      &lt;h1 id=&quot;长连接及心跳保活原理简介&quot;&gt;&lt;a href=&quot;#长连接及心跳保活原理简介&quot; class=&quot;headerlink&quot; title=&quot;长连接及心跳保活原理简介&quot;&gt;&lt;/a&gt;长连接及心跳保活原理简介&lt;/h1&gt;&lt;p&gt;&amp;emsp;&amp;emsp;本文简要的分析了长连接产生的背景以及
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title>HTTPS原理浅析及其在Android中的使用</title>
    <link href="http://haojen.github.io/2017/05/08/httpsAndroid/"/>
    <id>http://haojen.github.io/2017/05/08/httpsAndroid/</id>
    <published>2017-05-08T06:38:07.000Z</published>
    <updated>2017-05-08T06:43:51.419Z</updated>
    
    <content type="html"><![CDATA[<p>&emsp;&emsp;本文首先分析HTTP协议在安全性上的不足，进而阐述HTTPS实现安全通信的关键技术点和原理。然后通过抓包分析HTTPS协议的握手以及通信过程。最后总结一下自己在开发过程中遇到的HTTPS相关的问题，并给出当前项目中对HTTPS问题的系统解决方案，以供总结和分享。如有不当之处，欢迎批评和指正。</p>
<hr>
<h2 id="1-HTTP协议的不足"><a href="#1-HTTP协议的不足" class="headerlink" title="1.HTTP协议的不足"></a>1.HTTP协议的不足</h2><p>&emsp;&emsp;HTTP1.x在传输数据时，所有传输的内容都是明文，客户端和服务器端都无法验证对方的身份，存在的问题如下：</p>
<blockquote>
<ul>
<li>通信使用明文(不加密)，内容可能会被窃听；</li>
<li>不验证通信方的身份，有可能遭遇伪装；</li>
<li>无法证明报文的完整性，所以有可能已遭篡改；</li>
</ul>
</blockquote>
<p>&emsp;&emsp;其实这些问题不仅在HTTP上出现，<strong>其他未加密的协议中也会存在这类问题</strong>。</p>
<h3 id="1-通信使用明文可能会被窃听"><a href="#1-通信使用明文可能会被窃听" class="headerlink" title="(1) 通信使用明文可能会被窃听"></a>(1) 通信使用明文可能会被窃听</h3><p>&emsp;&emsp;按TCP/IP协议族的工作机制，<strong>互联网上的任何角落都存在通信内容被窃听的风险</strong>。而HTTP协议本身不具备加密的功能，所传输的都是明文。即使已经经过过加密处理的通信，也会被窥视到通信内容，这点和未加密的通信是相同的。只是说如果通信经过加密，就有可能让人无法破解报文信息的含义，<strong>但加密处理后的报文信息本身还是会被看到的</strong>。</p>
<h3 id="2-不验证通信方的身份可能遭遇伪装"><a href="#2-不验证通信方的身份可能遭遇伪装" class="headerlink" title="(2) 不验证通信方的身份可能遭遇伪装"></a>(2) 不验证通信方的身份可能遭遇伪装</h3><p>&emsp;&emsp;在HTTP协议通信时，由于<strong>不存在确认通信方的处理步骤，因此任何人都可以发起请求</strong>。另外，服务器只要接收到请求，不管对方是谁都会返回一个响应。因此不确认通信方，存在以下隐患：</p>
<blockquote>
<ul>
<li>无法确定请求发送至目标的Web服务器是否是按真实意图返回响应的那台服务器。<strong>有可能是已伪装的 Web 服务器</strong>；</li>
<li>无法确定响应返回到的客户端是否是按真实意图接收响应的那个客户端。有可能是已伪装的客户端；</li>
<li>无法确定正在通信的对方是否具备访问权限。因为某些Web服务器上保存着重要的信息，只想发给特定用户通信的权限；</li>
<li>无法判定请求是来自何方、出自谁手；</li>
<li>即使是无意义的请求也会照单全收，无法阻止海量请求下的DoS攻击；</li>
</ul>
</blockquote>
<h3 id="3-无法证明报文完整性，可能已遭篡改"><a href="#3-无法证明报文完整性，可能已遭篡改" class="headerlink" title="(3) 无法证明报文完整性，可能已遭篡改"></a>(3) 无法证明报文完整性，可能已遭篡改</h3><p>&emsp;&emsp;<strong>所谓完整性是指信息的准确度</strong>。若无法证明其完整性，通常也就意味着无法判断信息是否准确。HTTP协议无法证明通信的报文完整性，在请求或响应送出之后直到对方接收之前的这段时间内，<strong>即使请求或响应的内容遭到篡改，也没有办法获悉。</strong><br>&emsp;&emsp;比如，从某个Web网站下载内容，是无法确定客户端下载的文件和服务器上存放的文件是否前后一致的。文件内容在传输途中可能已经被篡改为其他的内容。即使内容真的已改变，作为接收方的客户端也是觉察不到的。像这样，<strong>请求或响应在传输途中，遭攻击者拦截并篡改内容的攻击称为中间人攻击(Man-in-the-Middle attack，MITM)。</strong></p>
<center><br><img src="http://occl9k36n.bkt.clouddn.com/2017_02_23_show_mitm.png" alt="中间人攻击示意图"><br></center>

<h3 id="4-安全的HTTP版本应该具备的几个特征"><a href="#4-安全的HTTP版本应该具备的几个特征" class="headerlink" title="(4) 安全的HTTP版本应该具备的几个特征"></a>(4) 安全的HTTP版本应该具备的几个特征</h3><p>&emsp;&emsp;由于上述的几个问题，需要一种能够提供如下功能的HTTP安全技术：</p>
<blockquote>
<ul>
<li>(1) 服务器认证(客户端知道它们是在与真正的而不是伪造的服务器通话)；</li>
<li>(2) 客户端认证(服务器知道它们是在与真正的而不是伪造的客户端通话)；</li>
<li>(3) 完整性(客户端和服务器的数据不会被修改)；</li>
<li>(4) 加密(客户端和服务器的对话是私密的，无需担心被窃听)；</li>
<li>(5) 效率(一个运行的足够快的算法，以便低端的客户端和服务器使用)；</li>
<li>(6) 普适性(基本上所有的客户端和服务器都支持这些协议)；</li>
</ul>
</blockquote>
<hr>
<h2 id="2-HTTPS的关键技术"><a href="#2-HTTPS的关键技术" class="headerlink" title="2.HTTPS的关键技术"></a>2.HTTPS的关键技术</h2><p>&emsp;&emsp;在这样的需求背景下，HTTPS技术诞生了。HTTPS协议的主要功能基本都依赖于TLS/SSL协议，<strong>提供了身份验证、信息加密和完整性校验的功能，可以解决HTTP存在的安全问题</strong>。本节就重点探讨一下HTTPS协议的几个关键技术点。</p>
<center><br><img src="http://occl9k36n.bkt.clouddn.com/2017_02_23_https_overview.png" alt="https概览"><br></center>

<h3 id="1-加密技术"><a href="#1-加密技术" class="headerlink" title="(1) 加密技术"></a>(1) 加密技术</h3><p>加密算法一般分为两种：</p>
<blockquote>
<ul>
<li><strong>对称加密：加密与解密的密钥相同。以DES算法为代表；</strong></li>
<li><strong>非对称加密：加密与解密的密钥不相同。以RSA算法为代表；</strong></li>
</ul>
</blockquote>
<p>&emsp;&emsp;对称加密强度非常高，一般破解不了，但存在一个很大的问题就是<strong>无法安全地生成和保管密钥</strong>，假如客户端和服务器之间每次会话都使用固定的、相同的密钥加密和解密，肯定存在很大的安全隐患。<br>&emsp;&emsp;<strong>在非对称密钥交换算法出现以前，对称加密一个很大的问题就是不知道如何安全生成和保管密钥。非对称密钥交换过程主要就是为了解决这个问题，使密钥的生成和使用更加安全。但同时也是HTTPS性能和速度严重降低的“罪魁祸首”。</strong><br>&emsp;&emsp;<strong>HTTPS采用对称加密和非对称加密两者并用的混合加密机制，在交换密钥环节使用非对称加密方式，之后的建立通信交换报文阶段则使用对称加密方式。</strong></p>
<h3 id="2-身份验证–证明公开密钥正确性的证书"><a href="#2-身份验证–证明公开密钥正确性的证书" class="headerlink" title="(2) 身份验证–证明公开密钥正确性的证书"></a>(2) 身份验证–证明公开密钥正确性的证书</h3><p>&emsp;&emsp;<strong>非对称加密最大的一个问题，就是无法证明公钥本身就是货真价实的公钥</strong>。比如，正准备和某台服务器建立公开密钥加密方式下的通信时，如何证明收到的公开密钥就是原本预想的那台服务器发行的公开密钥。或许在公开密钥传输途中，真正的公开密钥已经被攻击者替换掉了。<br>&emsp;&emsp;如果不验证公钥的可靠性，至少会存在如下的两个问题：中间人攻击和信息抵赖。</p>
<center><br><img src="http://occl9k36n.bkt.clouddn.com/2017_02_27_https_gonyue_yanzheng.png" alt="https概览"><br></center>

<p>&emsp;&emsp;为了解决上述问题，可以使用由<strong>数字证书认证机构(CA，Certificate Authority)和其相关机关颁发的公开密钥证书</strong>。</p>
<center><br><img src="http://occl9k36n.bkt.clouddn.com/2017_02_27_https_ca_use.png" alt="https概览"><br></center>

<p>CA使用具体的流程如下：</p>
<blockquote>
<ul>
<li>(1) 服务器的运营人员向数字证书认证机构(CA)提出公开密钥的申请;</li>
<li>(2) CA通过线上、线下等多种手段<strong>验证申请者提供信息的真实性</strong>，如组织是否存在、企业是否合法，是否拥有域名的所有权等；</li>
<li>(3) 如果信息审核通过，CA会<strong>对已申请的公开密钥做数字签名</strong>，然后分配这个已签名的公开密钥，并将该公开密钥放入公钥证书后绑定在一起。<br>证书包含以下信息：申请者公钥、申请者的组织信息和个人信息、签发机构CA的信息、有效时间、证书序列号等信息的明文，同时包含一个签名；<br>签名的产生算法：首先，使用散列函数计算公开的明文信息的信息摘要，然后，采用CA的私钥对信息摘要进行加密，密文即签名；</li>
<li>(4) 客户端在HTTPS握手阶段向服务器发出请求，<strong>要求服务器返回证书文件</strong>；</li>
<li>(5) <strong>客户端读取证书中的相关的明文信息，采用相同的散列函数计算得到信息摘要，然后，利用对应CA的公钥解密签名数据，对比证书的信息摘要，如果一致，则可以确认证书的合法性，即公钥合法</strong>；</li>
<li>(6) 客户端然后验证证书相关的域名信息、有效时间等信息；</li>
<li>(7) 客户端会内置信任CA的证书信息(包含公钥)，如果CA不被信任，则找不到对应CA的证书，证书也会被判定非法。</li>
</ul>
</blockquote>
<p>在这个过程注意几点：</p>
<blockquote>
<ul>
<li>(1) 申请证书不需要提供私钥，<strong>确保私钥永远只能被服务器掌握</strong>；</li>
<li>(2) 证书的合法性仍然依赖于非对称加密算法，证书主要是增加了服务器信息以及签名；</li>
<li>(3) 内置CA对应的证书称为<strong>根证书</strong>；颁发者和使用者相同，自己为自己签名，叫<strong>自签名证书</strong>；</li>
<li>(4) <strong>证书=公钥+申请者与颁发者信息+签名；</strong></li>
</ul>
</blockquote>
<hr>
<h2 id="3-HTTPS协议原理"><a href="#3-HTTPS协议原理" class="headerlink" title="3.HTTPS协议原理"></a>3.HTTPS协议原理</h2><h3 id="1-HTTPS的历史"><a href="#1-HTTPS的历史" class="headerlink" title="(1) HTTPS的历史"></a>(1) HTTPS的历史</h3><p>HTTPS协议历史简介：</p>
<blockquote>
<ul>
<li>(1) SSL协议的第一个版本由Netscape公司开发，但这个版本从未发布过；</li>
<li>(2) SSL协议第二版于1994年11月发布。第一次部署是在Netscape Navigator1.1浏览器上，发行于1995年3月；</li>
<li>(3) SSL 3于1995年年底发布，虽然名称与早先的协议版本相同，但<strong>SSL3是完全重新设计的协议，该设计一直沿用到今天</strong>。</li>
<li>(4) TLS 1.0于1999年1月问世，与SSL 3相比，版本修改并不大；</li>
<li>(5) 2006年4月，下一个版本TLS 1.1才问世，仅仅修复了一些关键的安全问题；</li>
<li>(6) 2008年8月，TLS1.2发布。该版本添加了对已验证加密的支持，并且基本上删除了协议说明中所有硬编码的安全基元，使协议完全弹性化；</li>
</ul>
</blockquote>
<h3 id="2-协议实现"><a href="#2-协议实现" class="headerlink" title="(2) 协议实现"></a>(2) 协议实现</h3><p>&emsp;&emsp;宏观上，TLS以记录协议(record protocol)实现。<strong>记录协议负责在传输连接上交换所有的底层消息，并可以配置加密</strong>。每一条TLS记录以一个短标头起始。标头包含记录内容的类型(或子协议)、协议版本和长度。消息数据紧跟在标头之后，如下图所示：</p>
<center><br><img src="http://occl9k36n.bkt.clouddn.com/2017_02_28_record_protocol.png" alt="记录协议"><br></center>

<p>&emsp;&emsp;TLS的主规格说明书定义了四个核心子协议：</p>
<blockquote>
<ul>
<li>握手协议(handshake protocol)；</li>
<li>密钥规格变更协议(change cipher spec protocol)；</li>
<li>应用数据协议(application data protocol)；</li>
<li>警报协议(alert protocol)；</li>
</ul>
</blockquote>
<h3 id="3-握手协议"><a href="#3-握手协议" class="headerlink" title="(3) 握手协议"></a>(3) 握手协议</h3><p>&emsp;&emsp;握手是TLS协议中最精密复杂的部分。在这个过程中，通信双方协商连接参数，并且完成身份验证。根据使用的功能的不同，整个过程通常需要交换6~10条消息。根据配置和支持的协议扩展的不同，交换过程可能有许多变种。在使用中经常可以观察到以下三种流程：</p>
<blockquote>
<ul>
<li>(1) 完整的握手，对服务器进行身份验证(单向验证，最常见)；</li>
<li>(2) 对客户端和服务器都进行身份验证的握手(双向验证)；</li>
<li>(3) 恢复之前的会话采用的简短握手；</li>
</ul>
</blockquote>
<h3 id="4-单向验证的握手流程"><a href="#4-单向验证的握手流程" class="headerlink" title="(4) 单向验证的握手流程"></a>(4) 单向验证的握手流程</h3><p>&emsp;&emsp;本节以QQ邮箱的登录过程为例，通过抓包来对单向验证的握手流程进行分析。单向验证的一次完整的握手流程如下所示：</p>
<center><br><img src="http://occl9k36n.bkt.clouddn.com/2017_02_28_handshake_protocol.png" alt="单向验证握手协议"><br></center>

<p>主要分为四个步骤：</p>
<blockquote>
<ul>
<li>(1) 交换各自支持的功能，对需要的连接参数达成一致;</li>
<li>(2) 验证出示的证书，或使用其他方式进行身份验证;</li>
<li>(3) 对将用于保护会话的共享主密钥达成一致;</li>
<li>(4) 验证握手消息是否被第三方团体修改;</li>
</ul>
</blockquote>
<p>下面对这一过程进行详细的分析。</p>
<h4 id="1-ClientHello"><a href="#1-ClientHello" class="headerlink" title="1.ClientHello"></a>1.ClientHello</h4><p>&emsp;&emsp;在握手流程中，ClientHello是第一条消息。这条消息将客户端的功能和首选项传送给服务器。包含客户端支持的SSL的指定版本、加密组件(Cipher Suite)列表(所使用的加密算法及密钥长度等)。</p>
<center><br><img src="http://occl9k36n.bkt.clouddn.com/2017_04_21_client_hello.png" alt="ClientHello报文"><br></center>

<h4 id="2-ServerHello"><a href="#2-ServerHello" class="headerlink" title="2.ServerHello"></a>2.ServerHello</h4><p>&emsp;&emsp;ServerHello消息将服务器选择的连接参数传送回客户端。这个消息的结构与ClientHello类似，只是每个字段只包含一个选项。<strong>服务器的加密组件内容以及压缩方法等都是从接收到的客户端加密组件内筛选出来的。</strong></p>
<center><br><img src="http://occl9k36n.bkt.clouddn.com/2017_04_21_server_hello.png" alt="ServerHello报文"><br></center>

<h4 id="3-Certificate"><a href="#3-Certificate" class="headerlink" title="3.Certificate"></a>3.Certificate</h4><p>&emsp;&emsp; 之后服务器发送Certificate报文，报文中包含公开密钥证书，服务器必须保证它发送的证书与选择的算法套件一致。不过Certificate消息是可选的，因为并非所有套件都使用身份验证，也并非所有身份验证方法都需要证书。</p>
<center><br><img src="http://occl9k36n.bkt.clouddn.com/2017_04_21_certificate.png" alt="Certificate报文"><br></center>

<h4 id="4-ServerKeyExchange"><a href="#4-ServerKeyExchange" class="headerlink" title="4.ServerKeyExchange"></a>4.ServerKeyExchange</h4><p>&emsp;&emsp;ServerKeyExchange消息的目的是携带密钥交换的额外数据。消息内容对于不同的协商算法套件都会存在差异。在某些场景中，服务器不需要发送任何内容，在这些场景中就不需要发送ServerKeyExchange消息。</p>
<center><br><img src="http://occl9k36n.bkt.clouddn.com/2017_04_21_server_key_exchange.png" alt="ServerKeyExchange报文"><br></center>

<h4 id="5-ServerHelloDone"><a href="#5-ServerHelloDone" class="headerlink" title="5.ServerHelloDone"></a>5.ServerHelloDone</h4><p>&emsp;&emsp;ServerHelloDone消息表明服务器已经将所有预计的握手消息发送完毕。在此之后，服务器会等待客户端发送消息。</p>
<center><br><img src="http://occl9k36n.bkt.clouddn.com/2017_04_21_server_hello_done.png" alt="ServerHelloDone报文"><br></center>

<h4 id="6-ClientKeyExchange"><a href="#6-ClientKeyExchange" class="headerlink" title="6.ClientKeyExchange"></a>6.ClientKeyExchange</h4><p>&emsp;&emsp;ClientKeyExchange消息携带客户端为密钥交换提供的所有信息。这个消息受协商的密码套件的影响，内容随着不同的协商密码套件而不同。</p>
<center><br><img src="http://occl9k36n.bkt.clouddn.com/2017_04_21_client_key_exchange.png" alt="ClientKeyExchange报文"><br></center>

<h4 id="7-ChangeCipherSpec"><a href="#7-ChangeCipherSpec" class="headerlink" title="7.ChangeCipherSpec"></a>7.ChangeCipherSpec</h4><p>&emsp;&emsp;<strong>ChangeCipherSpec消息表明发送端已取得用以生成连接参数的足够信息，已生成加密密钥，并且将切换到加密模式</strong>。客户端和服务器在条件成熟时都会发送这个消息。注意：ChangeCipherSpec不属于握手消息，它是另一种协议，只有一条消息，作为它的子协议进行实现。</p>
<center><br><img src="http://occl9k36n.bkt.clouddn.com/2017_04_21_change_cipher_spec.png" alt="ChangeCipherSpec报文"><br></center>

<h4 id="8-Finished"><a href="#8-Finished" class="headerlink" title="8.Finished"></a>8.Finished</h4><p>&emsp;&emsp;Finished消息意味着握手已经完成。消息内容将加密，以便双方可以安全地交换验证整个握手完整性所需的数据。客户端和服务器在条件成熟时都会发送这个消息。</p>
<h3 id="5-双向验证的握手流程"><a href="#5-双向验证的握手流程" class="headerlink" title="(5) 双向验证的握手流程"></a>(5) 双向验证的握手流程</h3><p>&emsp;&emsp;在一些对安全性要求更高的场景下，可能会出现双向验证的需求。完整的双向验证流程如下：</p>
<center><br><img src="http://occl9k36n.bkt.clouddn.com/2017_04_21_both_verify.png" alt="双向验证的握手流程"><br></center>

<p>&emsp;&emsp;可以看到，<strong>同单向验证流程相比，双向验证多了如下两条消息：CertificateRequest与CertificateVerify，其余流程大致相同。</strong></p>
<h4 id="1-Certificate-Request"><a href="#1-Certificate-Request" class="headerlink" title="1.Certificate Request"></a>1.Certificate Request</h4><p>&emsp;&emsp;Certificate Request是TLS规定的一个可选功能，<strong>用于服务器认证客户端的身份</strong>。通过服务器要求客户端发送一个证书实现，服务器应该在ServerKeyExchange之后立即发送CertificateRequest消息。</p>
<p>&emsp;&emsp;消息结构如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="keyword">enum</span> &#123;</div><div class="line">    rsa_sign(<span class="number">1</span>), dss_sign(<span class="number">2</span>), rsa_fixed_dh(<span class="number">3</span>),dss_fixed_dh(<span class="number">4</span>),</div><div class="line">    rsa_ephemeral_dh_RESERVED(<span class="number">5</span>),dss_ephemeral_dh_RESERVED(<span class="number">6</span>),</div><div class="line">    fortezza_dms_RESERVED(<span class="number">20</span>),</div><div class="line">    ecdsa_sign(<span class="number">64</span>), rsa_fixed_ecdh(<span class="number">65</span>),</div><div class="line">    ecdsa_fixed_ecdh(<span class="number">66</span>), </div><div class="line">    (<span class="number">255</span>)</div><div class="line">&#125; ClientCertificateType;</div><div class="line"></div><div class="line">opaque DistinguishedName&lt;<span class="number">1</span>..<span class="number">2</span>^<span class="number">16</span>-<span class="number">1</span>&gt;;struct &#123;</div><div class="line">    ClientCertificateType certificate_types&lt;<span class="number">1</span>..<span class="number">2</span>^<span class="number">8</span>-<span class="number">1</span>&gt;;</div><div class="line">    SignatureAndHashAlgorithm</div><div class="line">      supported_signature_algorithms&lt;<span class="number">2</span>^<span class="number">16</span>-<span class="number">1</span>&gt;;</div><div class="line">    DistinguishedName certificate_authorities&lt;<span class="number">0</span>..<span class="number">2</span>^<span class="number">16</span>-<span class="number">1</span>&gt;;</div><div class="line">&#125; CertificateRequest;</div></pre></td></tr></table></figure>
<p>&emsp;&emsp;可以选择发送一份自己接受的证书颁发机构列表，这些机构都用其可分辨名称来表示.</p>
<h4 id="2-CertificateVerify"><a href="#2-CertificateVerify" class="headerlink" title="2.CertificateVerify"></a>2.CertificateVerify</h4><p>&emsp;&emsp;当需要做客户端认证时，<strong>客户端发送CertificateVerify消息，来证明自己确实拥有客户端证书的私钥</strong>。这条消息仅仅在客户端证书有签名能力的情况下发送。CertificateVerify必须紧跟在ClientKeyExchange之后。消息结构如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line">struct &#123; </div><div class="line">Signature handshake_messages_signature; </div><div class="line">&#125; CertificateVerify;</div></pre></td></tr></table></figure>
<h3 id="6-应用数据协议-application-data-protocol"><a href="#6-应用数据协议-application-data-protocol" class="headerlink" title="(6) 应用数据协议(application data protocol)"></a>(6) 应用数据协议(application data protocol)</h3><p>&emsp;&emsp;应用数据协议携带着应用消息，只以TLS的角度考虑的话，这些就是数据缓冲区。记录层使用当前连接安全参数对这些消息进行打包、碎片整理和加密。如下图所示，可以看到传输的数据已经是经过加密之后的了。</p>
<center><br><img src="http://occl9k36n.bkt.clouddn.com/2017_05_05_application_data_protocol.png" alt="应用数据协议报文"><br></center>

<h3 id="7-警报协议-alert-protocol"><a href="#7-警报协议-alert-protocol" class="headerlink" title="(7) 警报协议(alert protocol)"></a>(7) 警报协议(alert protocol)</h3><p>&emsp;&emsp;警报的目的是以简单的通知机制告知对端通信出现异常状况。它通常会携带close_notify异常，在连接关闭时使用，报告错误。警报非常简单，只有两个字段：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line">struct &#123; </div><div class="line">    AlertLevel level; </div><div class="line">    AlertDescription description; </div><div class="line">&#125; Alert;</div></pre></td></tr></table></figure>
<blockquote>
<ul>
<li>AlertLevel字段：表示警报的严重程度；</li>
<li>AlertDescription：直接表示警报代码；</li>
</ul>
</blockquote>
<hr>
<h2 id="4-在Android中使用HTTPS的常见问题"><a href="#4-在Android中使用HTTPS的常见问题" class="headerlink" title="4.在Android中使用HTTPS的常见问题"></a>4.在Android中使用HTTPS的常见问题</h2><h3 id="1-服务器证书验证错误"><a href="#1-服务器证书验证错误" class="headerlink" title="(1) 服务器证书验证错误"></a>(1) 服务器证书验证错误</h3><p>&emsp;&emsp;这是最常见的一种问题，通常会抛出如下类型的异常：</p>
<center><br><img src="http://occl9k36n.bkt.clouddn.com/2017_05_05_certificate_wrong.png" alt="服务器证书验证错误"><br></center>

<p>出现此类错误通常可能由以下的三种原因导致：</p>
<blockquote>
<ul>
<li>(1) 颁发服务器证书的CA未知；</li>
<li>(2) 服务器证书不是由CA签署的，而是自签署(比较常见)；</li>
<li>(3) 服务器配置缺少中间 CA；</li>
</ul>
</blockquote>
<p>&emsp;&emsp;<strong>当服务器的CA不被系统信任时，就会发生 SSLHandshakeException。</strong>可能是购买的CA证书比较新，Android系统还未信任，也可能是<strong>服务器使用的是自签名证书(这个在测试阶段经常遇到)</strong>。<br>&emsp;&emsp;解决此类问题常见的做法是：指定HttpsURLConnection信任特定的CA集合。在本文的第5部分代码实现模块，会详细的讲解如何让Android应用信任自签名证书集合或者跳过证书校验的环节。 </p>
<h3 id="2-域名验证失败"><a href="#2-域名验证失败" class="headerlink" title="(2) 域名验证失败"></a>(2) 域名验证失败</h3><p>&emsp;&emsp;SSL连接有两个关键环节。首先是验证证书是否来自值得信任的来源，其次确保正在通信的服务器提供正确的证书。如果没有提供，通常会看到类似于下面的错误：</p>
<center><br><img src="http://occl9k36n.bkt.clouddn.com/2017_05_05_domain_wrong.png" alt="域名验证错误"><br></center>

<p>&emsp;&emsp;出现此类问题的原因通常是由于服务器证书中配置的域名和客户端请求的域名不一致所导致的。</p>
<p>有两种解决方案：</p>
<blockquote>
<ul>
<li>(1) 重新生成服务器的证书，用真实的域名信息;</li>
<li>(2) 自定义HostnameVerifier,在握手期间，如果URL的主机名和服务器的标识主机名不匹配，则验证机制可以回调此接口的实现程序来确定是否应该允许此连接。可以通过自定义HostnameVerifier实现一个白名单的功能。</li>
</ul>
</blockquote>
<p>代码如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line"></div><div class="line">HostnameVerifier DO_NOT_VERIFY = <span class="keyword">new</span> HostnameVerifier() &#123;</div><div class="line">  <span class="meta">@Override</span></div><div class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">verify</span><span class="params">(String hostname, SSLSession session)</span> </span>&#123;</div><div class="line">    <span class="comment">// 设置接受的域名集合</span></div><div class="line">    <span class="keyword">if</span> (hostname.equals(...))  &#123;</div><div class="line">         <span class="keyword">return</span> <span class="keyword">true</span>;</div><div class="line">    &#125;</div><div class="line">  &#125;</div><div class="line">&#125;;</div><div class="line"></div><div class="line">HttpsURLConnection.setDefaultHostnameVerifier(DO_NOT_VERIFY);</div></pre></td></tr></table></figure>
<h3 id="3-客户端证书验证"><a href="#3-客户端证书验证" class="headerlink" title="(3) 客户端证书验证"></a>(3) 客户端证书验证</h3><p>&emsp;&emsp;SSL支持服务端通过验证客户端的证书来确认客户端的身份。这种技术与TrustManager的特性相似。本文将在第5部分代码实现模块，讲解如何让Android应用支持客户端证书验证的方式。 </p>
<h3 id="4-Android上TLS版本兼容问题"><a href="#4-Android上TLS版本兼容问题" class="headerlink" title="(4) Android上TLS版本兼容问题"></a>(4) Android上TLS版本兼容问题</h3><p>&emsp;&emsp;之前在接口联调的过程中，测试那边反馈过一个问题是在Android 4.4以下的系统出现HTTPS请求不成功而在4.4以上的系统上却正常的问题。相应的错误如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="number">03</span>-<span class="number">09</span> <span class="number">09</span>:<span class="number">21</span>:<span class="number">38.427</span>: W/System.err(<span class="number">2496</span>): javax.net.ssl.SSLHandshakeException: javax.net.ssl.SSLProtocolException: SSL handshake aborted: ssl=<span class="number">0xb7fa0620</span>: Failure in SSL library, usually a protocol error</div><div class="line"></div><div class="line"><span class="number">03</span>-<span class="number">09</span> <span class="number">09</span>:<span class="number">21</span>:<span class="number">38.427</span>: W/System.err(<span class="number">2496</span>): error:<span class="number">14077410</span>:SSL routines:SSL23_GET_SERVER_HELLO:<span class="function">sslv3 alert handshake <span class="title">failure</span> <span class="params">(external/openssl/ssl/s23_clnt.c:<span class="number">741</span> <span class="number">0xa90e6990</span>:<span class="number">0x00000000</span>)</span></span></div></pre></td></tr></table></figure>
<p>&emsp;&emsp;按照官方文档的描述，Android系统对SSL协议的版本支持如下：</p>
<table>
<thead>
<tr>
<th>Name</th>
<th style="text-align:center">Supported (API Levels)</th>
</tr>
</thead>
<tbody>
<tr>
<td>Default</td>
<td style="text-align:center">10+</td>
</tr>
<tr>
<td>SSL</td>
<td style="text-align:center">10–TBD</td>
</tr>
<tr>
<td>SSLv3</td>
<td style="text-align:center">10–TBD</td>
</tr>
<tr>
<td>TLS</td>
<td style="text-align:center">1+</td>
</tr>
<tr>
<td>TLSv1</td>
<td style="text-align:center">10+</td>
</tr>
<tr>
<td>TLSv1.1</td>
<td style="text-align:center">16+</td>
</tr>
<tr>
<td>TLSv1.2</td>
<td style="text-align:center">16+</td>
</tr>
</tbody>
</table>
<p>&emsp;&emsp;也就是说，<strong>按官方的文档显示，在API 16+以上，TLS1.1和TLS1.2是默认开启的。但是实际上在API 20+以上才默认开启，4.4以下的版本是无法使用TLS1.1和TLS 1.2的，这也是Android系统的一个bug。</strong></p>
<p>&emsp;&emsp;参照stackoverflow上的一些方式，比较好的一种解决方案如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line">SSLSocketFactory noSSLv3Factory;</div><div class="line"><span class="keyword">if</span> (Build.VERSION.SDK_INT &lt;= Build.VERSION_CODES.KITKAT) &#123;</div><div class="line">    noSSLv3Factory = <span class="keyword">new</span> TLSSocketFactory(mSSLContext.getSSLSocket().getSocketFactory());</div><div class="line">&#125; <span class="keyword">else</span> &#123;</div><div class="line">    noSSLv3Factory = mSSLContext.getSSLSocket().getSocketFactory();</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>对于4.4以下的系统，使用自定义的TLSSocketFactory，开启对TLS1.1和TLS1.2的支持，核心代码：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TLSSocketFactory</span> <span class="keyword">extends</span> <span class="title">SSLSocketFactory</span> </span>&#123;</div><div class="line"></div><div class="line">	<span class="keyword">private</span> SSLSocketFactory internalSSLSocketFactory;</div><div class="line"></div><div class="line">	<span class="function"><span class="keyword">public</span> <span class="title">TLSSocketFactory</span><span class="params">()</span> <span class="keyword">throws</span> KeyManagementException, NoSuchAlgorithmException </span>&#123;</div><div class="line">		SSLContext context = SSLContext.getInstance(<span class="string">"TLS"</span>);</div><div class="line">		context.init(<span class="keyword">null</span>, <span class="keyword">null</span>, <span class="keyword">null</span>);</div><div class="line">		internalSSLSocketFactory = context.getSocketFactory();</div><div class="line">	&#125;</div><div class="line"></div><div class="line">	<span class="function"><span class="keyword">public</span> <span class="title">TLSSocketFactory</span><span class="params">(SSLSocketFactory delegate)</span> <span class="keyword">throws</span> KeyManagementException, NoSuchAlgorithmException </span>&#123;</div><div class="line">		internalSSLSocketFactory = delegate;</div><div class="line">	&#125;</div><div class="line"></div><div class="line">    ......</div><div class="line">    </div><div class="line">    <span class="meta">@Override</span></div><div class="line">	<span class="function"><span class="keyword">public</span> Socket <span class="title">createSocket</span><span class="params">(InetAddress address, <span class="keyword">int</span> port, InetAddress localAddress, <span class="keyword">int</span> localPort)</span> <span class="keyword">throws</span> IOException </span>&#123;</div><div class="line">		<span class="keyword">return</span> enableTLSOnSocket(internalSSLSocketFactory.createSocket(address, port, localAddress, localPort));</div><div class="line">	&#125;</div><div class="line"></div><div class="line">    <span class="comment">// 开启对TLS1.1和TLS1.2的支持</span></div><div class="line">	<span class="function"><span class="keyword">private</span> Socket <span class="title">enableTLSOnSocket</span><span class="params">(Socket socket)</span> </span>&#123;</div><div class="line">		<span class="keyword">if</span>(socket != <span class="keyword">null</span> &amp;&amp; (socket <span class="keyword">instanceof</span> SSLSocket)) &#123;</div><div class="line">			((SSLSocket)socket).setEnabledProtocols(<span class="keyword">new</span> String[] &#123;<span class="string">"TLSv1.1"</span>, <span class="string">"TLSv1.2"</span>&#125;);</div><div class="line">		&#125;</div><div class="line">		<span class="keyword">return</span> socket;</div><div class="line">	&#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>TLSSocketFactory的完整代码见附件中的代码压缩包。</p>
<hr>
<h2 id="5-代码实现"><a href="#5-代码实现" class="headerlink" title="5.代码实现"></a>5.代码实现</h2><p>&emsp;&emsp;本部分主要基于第四部分提出的Android应用中使用HTTPS遇到的一些常见的问题，给出一个比较系统的解决方案。完整代码见附件中的代码压缩包。</p>
<h3 id="1-整体结构"><a href="#1-整体结构" class="headerlink" title="(1) 整体结构"></a>(1) 整体结构</h3><p>&emsp;&emsp;不管是使用自签名证书，还是采取客户端身份验证，核心都是创建一个自己的KeyStore,然后使用这个KeyStore创建一个自定义的SSLContext。整体类图如下：</p>
<center><br><img src="http://occl9k36n.bkt.clouddn.com/2017_05_05_https_uml.png" alt="https uml"><br></center>

<p>类图中的MySSLContext可以应用在HTTPUrlConnection的方式与服务端连接的过程中:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="keyword">if</span> (JarConfig.__self_signed_https) &#123;</div><div class="line">    SSLContextByTrustAll mSSLContextByTrustAll = <span class="keyword">new</span> SSLContextByTrustAll();</div><div class="line">    MySSLContext mSSLContext = <span class="keyword">new</span> MySSLContext(mSSLContextByTrustAll);</div><div class="line">   SSLSocketFactory noSSLv3Factory;</div><div class="line">   <span class="keyword">if</span> (Build.VERSION.SDK_INT &lt;= Build.VERSION_CODES.KITKAT) &#123;</div><div class="line">        noSSLv3Factory = <span class="keyword">new</span> TLSSocketFactory(mSSLContext.getSSLSocket().getSocketFactory());</div><div class="line">    &#125; <span class="keyword">else</span> &#123;</div><div class="line">        noSSLv3Factory = mSSLContext.getSSLSocket().getSocketFactory();</div><div class="line">    &#125;</div><div class="line">    </div><div class="line">    httpsURLConnection.setSSLSocketFactory(noSSLv3Factory);</div><div class="line">    httpsURLConnection.setHostnameVerifier(MY_DOMAIN_VERIFY);</div><div class="line">&#125;<span class="keyword">else</span> &#123;</div><div class="line">    httpsURLConnection.setSSLSocketFactory((SSLSocketFactory) SSLSocketFactory.getDefault());</div><div class="line">    httpsURLConnection.setHostnameVerifier(DO_NOT_VERIFY);</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>&emsp;&emsp;核心是通过httpsURLConnection.setSSLSocketFactory使用自定义的校验逻辑。整体设计上使用策略模式决定采用哪种验证机制：</p>
<blockquote>
<ul>
<li>makeContextWithCilentAndServer 单向验证方式(自定义信任的证书集合)</li>
<li>makeContextWithServer 双向验证方式(自定义信任的证书集合，并使用客户端证书)</li>
<li>makeContextToTrustAll (<strong>信任所有的CA证书，不安全，仅供测试阶段使用</strong>)</li>
</ul>
</blockquote>
<h3 id="2-单向验证并自定义信任的证书集合"><a href="#2-单向验证并自定义信任的证书集合" class="headerlink" title="(2) 单向验证并自定义信任的证书集合"></a>(2) 单向验证并自定义信任的证书集合</h3><p>&emsp;&emsp;在App中，把服务端证书放到资源文件下(<strong>通常是asset目录下</strong>，因为证书对于每一个用户来说都是相同的，并且也不会经常发生改变)，但是也可以放在设备的外部存储上。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">SSLContextWithServer</span> <span class="keyword">implements</span> <span class="title">GetSSLSocket</span> </span>&#123;</div><div class="line"></div><div class="line">    <span class="comment">// 在这里进行服务器正式的名称的配置</span></div><div class="line">    <span class="keyword">private</span> String[] serverCertificateNames = &#123;<span class="string">"serverCertificateNames1"</span> ,<span class="string">"serverCertificateNames2"</span>&#125;;</div><div class="line"></div><div class="line">    <span class="meta">@Override</span></div><div class="line">    <span class="function"><span class="keyword">public</span> SSLContext <span class="title">getSSLSocket</span><span class="params">()</span> </span>&#123;</div><div class="line">        String[] caCertString = <span class="keyword">new</span> String[serverCertificateNames.length];</div><div class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span> ; i &lt; serverCertificateNames.length ; i++) &#123;</div><div class="line">            <span class="keyword">try</span> &#123;</div><div class="line">                caCertString[i] = readCaCert(serverCertificateNames[i]);</div><div class="line">            &#125; <span class="keyword">catch</span>(Exception e) &#123;</div><div class="line"></div><div class="line">            &#125;</div><div class="line">        &#125;</div><div class="line">        SSLContext mSSLContext = <span class="keyword">null</span>;</div><div class="line">        <span class="keyword">try</span> &#123;</div><div class="line">            mSSLContext = SSLContextFactory.getInstance().makeContextWithServer(caCertString);</div><div class="line">        &#125; <span class="keyword">catch</span>(Exception e) &#123;</div><div class="line"></div><div class="line">        &#125;</div><div class="line">        <span class="keyword">return</span> mSSLContext;</div><div class="line">    &#125;</div></pre></td></tr></table></figure>
<p>&emsp;&emsp;serverCertificateNames中定义了App所信任的证书名称(这些证书文件必须要放在指定的文件路径下，并其要保证名称相同)，而后就可以加载服务端证书链到keystore，通过获取到的可信任并带有服务端证书的keystore,就可以用它来初始化自定义的SSLContext了：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="meta">@Override</span></div><div class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">checkServerTrusted</span><span class="params">(X509Certificate[] chain, String authType)</span> <span class="keyword">throws</span> CertificateException </span>&#123;</div><div class="line">        <span class="keyword">try</span> &#123;</div><div class="line">            originalX509TrustManager.checkServerTrusted(chain, authType);</div><div class="line">        &#125; <span class="keyword">catch</span>(CertificateException originalException) &#123;</div><div class="line">            <span class="keyword">try</span> &#123;</div><div class="line">                X509Certificate[] reorderedChain = reorderCertificateChain(chain);</div><div class="line">                CertPathValidator validator = CertPathValidator.getInstance(<span class="string">"PKIX"</span>);</div><div class="line">                CertificateFactory factory = CertificateFactory.getInstance(<span class="string">"X509"</span>);</div><div class="line">                CertPath certPath = factory.generateCertPath(Arrays.asList(reorderedChain));</div><div class="line">                PKIXParameters params = <span class="keyword">new</span> PKIXParameters(trustStore);</div><div class="line">                params.setRevocationEnabled(<span class="keyword">false</span>);</div><div class="line">                validator.validate(certPath, params);</div><div class="line">            &#125; <span class="keyword">catch</span>(Exception ex) &#123;</div><div class="line">                <span class="keyword">throw</span> originalException;</div><div class="line">            &#125;</div><div class="line">        &#125;</div><div class="line">    &#125;</div></pre></td></tr></table></figure>
<h3 id="3-跳过证书校验过程"><a href="#3-跳过证书校验过程" class="headerlink" title="(3) 跳过证书校验过程"></a>(3) 跳过证书校验过程</h3><p>&emsp;&emsp;和上面的过程类似，只不过这里提供的TrustManager不需要提供信任的证书集合，默认接受任意客户端证书即可：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">AcceptAllTrustManager</span> <span class="keyword">implements</span> <span class="title">X509TrustManager</span> </span>&#123;</div><div class="line"></div><div class="line">	<span class="meta">@Override</span></div><div class="line">	<span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">checkClientTrusted</span><span class="params">(X509Certificate[] chain, String authType)</span> <span class="keyword">throws</span> CertificateException </span>&#123;</div><div class="line">		<span class="comment">//do nothing，接受任意客户端证书</span></div><div class="line">	&#125;</div><div class="line"></div><div class="line">	<span class="meta">@Override</span></div><div class="line">	<span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">checkServerTrusted</span><span class="params">(X509Certificate[] chain, String authType)</span> <span class="keyword">throws</span> CertificateException </span>&#123;</div><div class="line">		<span class="comment">//do nothing，接受任意服务端证书</span></div><div class="line">	&#125;</div><div class="line"></div><div class="line">	<span class="meta">@Override</span></div><div class="line">	<span class="keyword">public</span> X509Certificate[] getAcceptedIssuers() &#123;</div><div class="line">		<span class="keyword">return</span> <span class="keyword">null</span>;</div><div class="line">	&#125;</div></pre></td></tr></table></figure>
<p>而后构造相应的SSLContext:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="function"><span class="keyword">public</span> SSLContext <span class="title">makeContextToTrustAll</span><span class="params">()</span> <span class="keyword">throws</span> Exception </span>&#123;</div><div class="line">        AcceptAllTrustManager tm = <span class="keyword">new</span> AcceptAllTrustManager();</div><div class="line">        SSLContext sslContext = SSLContext.getInstance(<span class="string">"TLS"</span>);</div><div class="line">        sslContext.init(<span class="keyword">null</span>, <span class="keyword">new</span> TrustManager[] &#123; tm &#125;, <span class="keyword">null</span>);</div><div class="line"></div><div class="line">        <span class="keyword">return</span> sslContext;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>完整的代码以及双向验证相关的实现可以参见附件中的代码链接。</p>
<hr>
<h2 id="参考文献"><a href="#参考文献" class="headerlink" title="参考文献"></a>参考文献</h2><p><a href="http://www.10tiao.com/html/182/201612/2247483961/1.html" target="_blank" rel="external">1. TLS协议分析(5) handshake协议 证书与密钥交换</a><br><a href="https://elliotsomething.github.io/2016/12/19/HTTPS%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3/" target="_blank" rel="external">2. HTTPS深入理解</a><br><a href="http://www.jianshu.com/p/67bcb140d804" target="_blank" rel="external">3. HTTPS原理介绍之内容加密</a><br><a href="http://km.oa.com/group/11879/articles/show/249476?kmref=search&amp;from_page=1&amp;no=1" target="_blank" rel="external">4. HTTPS原理探讨</a><br><a href="http://stackoverflow.com/questions/26649389/how-to-disable-sslv3-in-android-for-httpsurlconnection" target="_blank" rel="external">5. How to disable SSLv3 in android for HttpsUrlConnection?</a><br><a href="https://blog.dev-area.net/2015/08/13/android-4-1-enable-tls-1-1-and-tls-1-2/" target="_blank" rel="external">6. Android 4.1+ enable TLS 1.1 and TLS 1.2</a><br><a href="https://developer.android.com/training/articles/security-ssl.html#CommonHostnameProbs" target="_blank" rel="external">7. Google关于https的官方文档</a><br><a href="http://blog.csdn.net/hzl9966/article/details/52092551" target="_blank" rel="external">8. 使用HTTPS与SSL来保证安全性</a><br><a href="http://www.jianshu.com/p/7c1bc2daef8d" target="_blank" rel="external">9. Android App 安全的HTTPS 通信</a><br><a href="http://www.wxtlife.com/2016/03/27/%E8%AF%A6%E8%A7%A3https%E6%98%AF%E5%A6%82%E4%BD%95%E7%A1%AE%E4%BF%9D%E5%AE%89%E5%85%A8%E7%9A%84%EF%BC%9F/" target="_blank" rel="external">10. 详解https是如何确保安全的？</a><br><a href="https://book.douban.com/subject/25863515/" target="_blank" rel="external">11. 图解HTTP–第七章</a><br><a href="https://book.douban.com/subject/1440226/" target="_blank" rel="external">12. HTTP: The Definitive Guide</a><br><a href="https://book.douban.com/subject/26276786/" target="_blank" rel="external">13. Bulletproof SSL and TLS:<br>Understanding and Deploying SSL/TLS and PKI to Secure Servers and Web Applications</a></p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;&amp;emsp;&amp;emsp;本文首先分析HTTP协议在安全性上的不足，进而阐述HTTPS实现安全通信的关键技术点和原理。然后通过抓包分析HTTPS协议的握手以及通信过程。最后总结一下自己在开发过程中遇到的HTTPS相关的问题，并给出当前项目中对HTTPS问题的系统解决方案，以供
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title>ButterKnife的使用与原理(2)</title>
    <link href="http://haojen.github.io/2017/02/16/inside-learn-butter-knife2/"/>
    <id>http://haojen.github.io/2017/02/16/inside-learn-butter-knife2/</id>
    <published>2017-02-16T12:03:11.000Z</published>
    <updated>2017-02-16T12:05:12.051Z</updated>
    
    <content type="html"><![CDATA[<h2 id="1-前言"><a href="#1-前言" class="headerlink" title="1.前言"></a>1.前言</h2><p>&emsp;&emsp;本文是上一篇文章的续篇。上次大致介绍了ButterKnife的基本使用以及通过反射技术模拟实现了简易的ButterKnife效果。但是反射多少会有性能上的损耗。<strong>ButterKnife最新版本也不是通过反射来完成的，而是利用APT(Annotation Processing Tool)编译时解析技术，动态生成绑定事件或者View的java代码</strong>。网上关于ButterKnife源码分析的文章也很多。本文试图利用apt技术自己模拟实现一个简易的ButterKnife，来提升对ButterKnife，Dagger等类似框架的认识，并详细介绍注解处理器(Annotation Processor)的使用。</p>
<hr>
<h2 id="2-编译时解析注解技术简介"><a href="#2-编译时解析注解技术简介" class="headerlink" title="2.编译时解析注解技术简介"></a>2.编译时解析注解技术简介</h2><h3 id="1-Java编译器对Annotation的处理"><a href="#1-Java编译器对Annotation的处理" class="headerlink" title="(1) Java编译器对Annotation的处理"></a>(1) Java编译器对Annotation的处理</h3><p><a href="http://openjdk.java.net/groups/compiler/doc/compilation-overview/index.html" target="_blank" rel="external">OpenJDK对java编译过程的解释</a></p>
<p>Java编译过程图解：</p>
<center><br><img src="http://openjdk.java.net/groups/compiler/doc/compilation-overview/javac-flow.png" alt="java代码编译过程"><br></center>

<p>Java编译主要分为三个过程：</p>
<blockquote>
<ul>
<li>(1) Parse and Enter<br>解析源文件,    生成抽象语法树(AST),就像DOM树一样,    编译器会在这个阶段检查语法错误；</li>
<li>(2) Annotation Processing<br><strong>调用自定义的AnnotationProcessor,生成新文件,包括新的源代码</strong>,如果在这个阶段有生成新的代码文件,    则会重回第一步,    直到没有新的代码文件生成为止；</li>
<li>(3) Analyse and Generate<br>根据AST生成.class文件,在这一步会检查语法错误,    例如调用了没有定义的方法,    出现了不可到达的语句等,    编译器会在这一部分中做很多优化来提升运行效率(例如将for循环改成使用iterator的循环)；    </li>
</ul>
</blockquote>
<p>&emsp;&emsp;<strong>注解处理器(AnnotationProcessor)是javac的一个工具，用来在编译时扫描和处理注解</strong>。它允许我们在Java编译器编译代码之前，执行一段我们的代码。这些生成的Java代码是在生成的.java文件中，所以不能用来修改已经存在的Java类–例如向已有的类中添加方法。<strong>这些生成的Java文件，会同其他普通的手动编写的Java源代码一样被javac编译</strong>。</p>
<h3 id="2-apt-Annotation-Processor-Tool-相关技术简介"><a href="#2-apt-Annotation-Processor-Tool-相关技术简介" class="headerlink" title="(2) apt(Annotation Processor Tool)相关技术简介"></a>(2) apt(Annotation Processor Tool)相关技术简介</h3><p>&emsp;&emsp;从注解类型被引入的Java SE5.0到java SE7.0,自定义注解类型的处理方式发生了很大的变化：</p>
<blockquote>
<ul>
<li>(1) Java 5使用的是apt工具和Oracle私有的Mirror API来处理；</li>
<li>(2) Java 6引入了<strong>可插拔式注解处理机制，以及相应的javax,annotation.processing和javax.lang.model包</strong>。</li>
<li>(3) Java 7把java 5中的apt工具和对应的Mirror API声明为废弃，不推荐使用；</li>
</ul>
</blockquote>
<p>&emsp;&emsp;关于编译时处理Annotation，主要问题在于自己如何实现apt(Annotation Processor Tool)的processor，以及编译环境如何使用它。<strong>一个通用的流程如下所示:</strong></p>
<blockquote>
<ul>
<li>(1) 自定义Annotation；</li>
<li>(2) 通过apt提供的接口，自定义Annotation processor类，实现主要的处理方法；</li>
<li>(3) 注册自定义的Processor，以便apt识别；</li>
<li>(4) 在使用的module内指定使用apt来编译该processor。</li>
</ul>
</blockquote>
<p>下面我们就会按照该流程利用apt技术简单的模拟ButterKnife的效果。</p>
<h2 id="3-定义注解"><a href="#3-定义注解" class="headerlink" title="3.定义注解"></a>3.定义注解</h2><p>&emsp;&emsp;在一个单独的Android Module中定义需要使用的三个注解，只不过这里<strong>@Retention需要定义成：RetentionPolicy.CLASS</strong>，因为我们采用的是编译时解析的方式：</p>
<center><br><img src="http://occl9k36n.bkt.clouddn.com/2017_02_05_annotation_module_overview.png" alt="定义注解的module的项目概览"><br></center>

<figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="meta">@Target</span>(ElementType.TYPE)</div><div class="line"><span class="meta">@Retention</span>(RetentionPolicy.CLASS)</div><div class="line"><span class="keyword">public</span> <span class="meta">@interface</span> ContentView &#123;</div><div class="line">    <span class="function"><span class="keyword">int</span> <span class="title">value</span><span class="params">()</span></span>;</div><div class="line">&#125;</div><div class="line"></div><div class="line"><span class="meta">@Target</span>(ElementType.FIELD)</div><div class="line"><span class="meta">@Retention</span>(RetentionPolicy.CLASS)</div><div class="line"><span class="keyword">public</span> <span class="meta">@interface</span> BindView &#123;</div><div class="line">    <span class="comment">// 用来绑定View的id</span></div><div class="line">    <span class="function"><span class="keyword">int</span> <span class="title">value</span><span class="params">()</span></span>;</div><div class="line">&#125;</div><div class="line"></div><div class="line"><span class="meta">@Target</span>(ElementType.METHOD)</div><div class="line"><span class="meta">@Retention</span>(RetentionPolicy.CLASS)</div><div class="line"><span class="keyword">public</span> <span class="meta">@interface</span> OnClick &#123;</div><div class="line">    <span class="keyword">int</span>[] value();</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<h2 id="4-定义注解处理器"><a href="#4-定义注解处理器" class="headerlink" title="4.定义注解处理器"></a>4.定义注解处理器</h2><p>在使用ButterKnife的时候，我们在dependencies需要加入：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line">compile <span class="string">'com.jakewharton:butterknife:8.0.1'</span></div><div class="line">annotationProcessor <span class="string">'com.jakewharton:butterknife-compiler:8.0.1'</span></div></pre></td></tr></table></figure>
<p>第二行就是声明注解处理器。这里，我们同样需要自己实现一个注解处理器。新建一个Java Module如下：</p>
<center><br><img src="http://occl9k36n.bkt.clouddn.com/2017_02_11_processor_module.png" alt="processor module概览"><br></center>

<p>在如上的概览中，BindViewProcessor是要实现的核心。</p>
<h3 id="1-虚处理器AbstractProcessor"><a href="#1-虚处理器AbstractProcessor" class="headerlink" title="(1)虚处理器AbstractProcessor"></a>(1)虚处理器AbstractProcessor</h3><p>每一个注解处理器都是继承于AbstractProcessor，如下所示：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MyProcessor</span> <span class="keyword">extends</span> <span class="title">AbstractProcessor</span> </span>&#123;</div><div class="line"></div><div class="line">    <span class="meta">@Override</span></div><div class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">synchronized</span> <span class="keyword">void</span> <span class="title">init</span><span class="params">(ProcessingEnvironment env)</span></span>&#123; &#125;</div><div class="line"></div><div class="line">    <span class="meta">@Override</span></div><div class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">process</span><span class="params">(Set&lt;? extends TypeElement&gt; annoations, RoundEnvironment env)</span> </span>&#123; &#125;</div><div class="line"></div><div class="line">    <span class="meta">@Override</span></div><div class="line">    <span class="function"><span class="keyword">public</span> Set&lt;String&gt; <span class="title">getSupportedAnnotationTypes</span><span class="params">()</span> </span>&#123; &#125;</div><div class="line"></div><div class="line">    <span class="meta">@Override</span></div><div class="line">    <span class="function"><span class="keyword">public</span> SourceVersion <span class="title">getSupportedSourceVersion</span><span class="params">()</span> </span>&#123; &#125;</div><div class="line"></div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p><strong>每一个自定义的注解处理器都需要实现这四个方法</strong>:</p>
<blockquote>
<ul>
<li>(1) init(ProcessingEnvironment env):<br>每一个注解处理器类都必须有一个空的构造函数。然而，这里有一个特殊的init()方法，它会被注解处理工具调用，并输入ProcessingEnviroment参数。ProcessingEnviroment提供很多有用的工具类Elements, Types和Filer。后续我们都会使用到。</li>
<li>(2) process(Set&lt;? extends TypeElement&gt; annotations, RoundEnvironment env):<br><strong>这相当于每个处理器的主函数main()。处理注解的关键逻辑都在这里实现</strong>。</li>
<li>(3) getSupportedAnnotationTypes():<br>这里需要指定注解处理器要解析哪些注解。</li>
<li>(4) getSupportedSourceVersion():<br>用来指定使用的Java版本。</li>
</ul>
</blockquote>
<h3 id="2-注册自定义的注解处理器"><a href="#2-注册自定义的注解处理器" class="headerlink" title="(2)注册自定义的注解处理器"></a>(2)注册自定义的注解处理器</h3><p>&emsp;&emsp;<strong>自定义的注解处理器必须注册到javac中，才能让编译器处理我们自定义的注解</strong>。在JavaSE开发平台中，必须提供一个.jar文件。就像其他.jar文件一样，打包注解处理器到此文件中。并且在jar文件中，需要打包一个特定的文件javax.annotation.processing.Processor到META-INF/services路径下。整个jar包的目录结构如下：</p>
<center><br><img src="http://occl9k36n.bkt.clouddn.com/2017_02_11_processor_jar_overview.png" alt="处理器jar包的格式"><br></center>

<p>不过Google开发了一个AutoService注解处理器，<strong>用来帮助我们生成META-INF/services/javax.annotation.processing.Processor文件</strong>。有木有感觉很神奇！！<strong>我们可以在注解处理器中使用注解</strong>。使用方式如下：</p>
<p>在注解处理器Module对应的build文件中，增加依赖：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line">compile <span class="string">'com.google.auto.service:auto-service:1.0-rc2'</span></div></pre></td></tr></table></figure>
<p>在自定义注解处理器文件中增加相应的代码：<br><figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="meta">@AutoService</span>(Processor.class)</div><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">BindViewProcessor</span> <span class="keyword">extends</span> <span class="title">AbstractProcessor</span> </span>&#123;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p>这样我们就完成了注册自定义的注解处理器的过程。</p>
<h3 id="3-init-ProcessingEnvironment-processingEnv-方法的实现"><a href="#3-init-ProcessingEnvironment-processingEnv-方法的实现" class="headerlink" title="(3) init(ProcessingEnvironment processingEnv)方法的实现"></a>(3) init(ProcessingEnvironment processingEnv)方法的实现</h3><figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="meta">@Override</span></div><div class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">synchronized</span> <span class="keyword">void</span> <span class="title">init</span><span class="params">(ProcessingEnvironment processingEnv)</span> </span>&#123;</div><div class="line">        <span class="keyword">super</span>.init(processingEnv);</div><div class="line">        mElementUtils = processingEnv.getElementUtils();</div><div class="line">        mMessager = processingEnv.getMessager();</div><div class="line">        mFiler = processingEnv.getFiler();</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>在init()中获得如下引用：</p>
<blockquote>
<ul>
<li>Elements：一个用来处理Element的工具类(解析注解元素的关键)</li>
<li>Types：一个用来处理TypeMirror的工具类</li>
<li>Filer：用来创建文件(生成java代码的关键)</li>
</ul>
</blockquote>
<h3 id="4-getSupportedAnnotationTypes-方法的实现"><a href="#4-getSupportedAnnotationTypes-方法的实现" class="headerlink" title="(4) getSupportedAnnotationTypes()方法的实现"></a>(4) getSupportedAnnotationTypes()方法的实现</h3><figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="function"><span class="keyword">public</span> Set&lt;String&gt; <span class="title">getSupportedAnnotationTypes</span><span class="params">()</span> </span>&#123;</div><div class="line">    Set&lt;String&gt; types = <span class="keyword">new</span> LinkedHashSet&lt;&gt;();</div><div class="line">    types.add(BindView.class.getCanonicalName()); </div><div class="line">    types.add(OnClick.class.getCanonicalName());</div><div class="line">    types.add(ContentView.class.getCanonicalName());</div><div class="line">    <span class="keyword">return</span> types;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>比较简单，声明我们要处理的三个注解即可以。</p>
<h3 id="5-getSupportedSourceVersion-方法的实现"><a href="#5-getSupportedSourceVersion-方法的实现" class="headerlink" title="(5) getSupportedSourceVersion()方法的实现"></a>(5) getSupportedSourceVersion()方法的实现</h3><figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="meta">@Override</span></div><div class="line"><span class="function"><span class="keyword">public</span> SourceVersion <span class="title">getSupportedSourceVersion</span><span class="params">()</span> </span>&#123;</div><div class="line">    <span class="keyword">return</span> SourceVersion.latestSupported();</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>用来指定使用的Java版本。通常这里返回SourceVersion.latestSupported()。</p>
<h2 id="5-注解处理器核心方法process-的实现"><a href="#5-注解处理器核心方法process-的实现" class="headerlink" title="5.注解处理器核心方法process()的实现"></a>5.注解处理器核心方法process()的实现</h2><p>&emsp;&emsp;因为process(Set&lt;? extends TypeElement&gt; set, RoundEnvironment roundEnvironment)方法比较复杂，也是最关键的一个方法，所以拿出来单独来讲解。在process()方法中，我们要做的就是解析自定义的注解，并生成最终的代码。</p>
<h3 id="1-整体实现"><a href="#1-整体实现" class="headerlink" title="(1) 整体实现"></a>(1) 整体实现</h3><p>代码实现为：<br><figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="meta">@Override</span></div><div class="line">   <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">process</span><span class="params">(Set&lt;? extends TypeElement&gt; set, RoundEnvironment roundEnvironment)</span> </span>&#123;</div><div class="line">       mAnnotatedClassMap.clear();</div><div class="line">       <span class="keyword">try</span> &#123;</div><div class="line">           <span class="comment">// (1)处理注解@ContentView</span></div><div class="line">           processContentView(roundEnvironment);</div><div class="line">           <span class="comment">// (2)处理注解@BindView</span></div><div class="line">           processBindView(roundEnvironment);</div><div class="line">           <span class="comment">// (3)处理注解@OnClick</span></div><div class="line">           processOnClick(roundEnvironment);</div><div class="line">       &#125; <span class="keyword">catch</span> (IllegalArgumentException e) &#123;</div><div class="line">           error(e.getMessage());</div><div class="line">           <span class="keyword">return</span> <span class="keyword">true</span>;</div><div class="line">       &#125;</div><div class="line">       <span class="keyword">try</span> &#123;</div><div class="line">           <span class="keyword">for</span>(ButterKnifeAnnotatedClass annotatedClass : mAnnotatedClassMap.values()) &#123;</div><div class="line">               info(<span class="string">"generating file for %s"</span>, annotatedClass.getFullClassName());</div><div class="line">               <span class="comment">// (4)生成代码</span></div><div class="line">               annotatedClass.generateFinder().writeTo(mFiler);</div><div class="line">           &#125;</div><div class="line">       &#125; <span class="keyword">catch</span> (Exception e) &#123;</div><div class="line">           e.printStackTrace();</div><div class="line">           error(<span class="string">"Generate file failed,reason:%s"</span>, e.getMessage());</div><div class="line">       &#125;</div><div class="line">       <span class="keyword">return</span> <span class="keyword">true</span>;</div><div class="line">   &#125;</div></pre></td></tr></table></figure></p>
<p>代码注释中大致描述了process方法的整个处理流程。</p>
<h3 id="2-Elements的作用"><a href="#2-Elements的作用" class="headerlink" title="(2) Elements的作用"></a>(2) Elements的作用</h3><p>在init()方法中，我们通过</p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line">Elements mElementUtils = processingEnv.getElementUtils();</div></pre></td></tr></table></figure>
<p>&emsp;&emsp;得到一个Elements的引用。它是后续处理的关键。<br>&emsp;&emsp;在注解处理过程中，我们扫描所有的Java源文件。<strong>源代码的每一个部分都是一个特定类型的Element</strong>。对于编译器来说，代码中的元素结构是基本不变的，例如，组成代码的基本元素有包，类，函数，字段，字段参数，变量。JDK中为这些元素定义了一个基类，也就是Element类，它有如下几个子类：</p>
<blockquote>
<ul>
<li>(1) PackageElement——包元素，包含了某个包下的信息，可以获取到包名等；</li>
<li>(2) TypeElement——类型元素，如某个字段属于某种类型；</li>
<li>(3) ExecutableElement——可执行元素，代表了函数类型的元素；</li>
<li>(4) VariableElement——变量元素；</li>
<li>(5) TypeParameterElement——类型参数元素。</li>
</ul>
</blockquote>
<p>通过注释来说明各种Element：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="keyword">package</span> com.example;    <span class="comment">// PackageElement</span></div><div class="line"></div><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Foo</span> </span>&#123;        <span class="comment">// TypeElement</span></div><div class="line"></div><div class="line">    <span class="keyword">private</span> <span class="keyword">int</span> a;      <span class="comment">// VariableElement</span></div><div class="line">    <span class="keyword">private</span> Foo other;  <span class="comment">// VariableElement</span></div><div class="line"></div><div class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Foo</span> <span class="params">()</span> </span>&#123;&#125;    <span class="comment">// ExecuteableElement</span></div><div class="line"></div><div class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setA</span> <span class="params">(  // ExecuteableElement</span></span></div><div class="line">                     <span class="keyword">int</span> newA   // TypeElement</div><div class="line">                     ) &#123;&#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<h3 id="3-处理-BindView注解"><a href="#3-处理-BindView注解" class="headerlink" title="(3) 处理@BindView注解"></a>(3) 处理@BindView注解</h3><figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="comment">// 处理@BindView相关的注解</span></div><div class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">processBindView</span><span class="params">(RoundEnvironment roundEnvironment)</span> </span>&#123;</div><div class="line">        <span class="keyword">for</span>(Element element : roundEnvironment.getElementsAnnotatedWith(BindView.class)) &#123;</div><div class="line">            ButterKnifeAnnotatedClass annotatedClass = getButterKnifeAnnotatedClass(element);</div><div class="line">            BindViewField bindViewField = <span class="keyword">new</span> BindViewField(element);</div><div class="line">            annotatedClass.addBindViewField(bindViewField);</div><div class="line">        &#125;</div><div class="line">    &#125;</div></pre></td></tr></table></figure>
<p>roundEnvironment.getElementsAnnotatedWith(BindView.class))返回所有被注解了@BindView的元素的列表。注意这里<strong>返回的是Element的列表。由于Element可以是类、方法、变量等。所以后续必须检查这些Element是否满足条件。</strong>接着通过element生成了BindViewField。为什么要定义一个BindViewField后续再讨论，先看一下BindViewField的创建过程。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">BindViewField</span> </span>&#123;</div><div class="line"></div><div class="line">    <span class="keyword">private</span> VariableElement mFieldElement;</div><div class="line"></div><div class="line">    <span class="keyword">private</span> <span class="keyword">int</span> mResId;</div><div class="line"></div><div class="line">    <span class="function"><span class="keyword">public</span> <span class="title">BindViewField</span><span class="params">(Element element)</span> <span class="keyword">throws</span> IllegalArgumentException </span>&#123;</div><div class="line">        <span class="keyword">if</span>(element.getKind() != ElementKind.FIELD) &#123;</div><div class="line">            <span class="comment">// 判断使用该注解的元素是否是Field</span></div><div class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> IllegalArgumentException(String.format(<span class="string">"Only field can be annotated with @%s"</span>,</div><div class="line">                    BindView.class.getSimpleName()));</div><div class="line">        &#125;</div><div class="line">        mFieldElement = (VariableElement)element;</div><div class="line">        <span class="comment">// 获取注解和值</span></div><div class="line">        BindView bindView = mFieldElement.getAnnotation(BindView.class);</div><div class="line">        mResId = bindView.value();</div><div class="line">        <span class="keyword">if</span>(mResId &lt; <span class="number">0</span>) &#123;</div><div class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> IllegalArgumentException(String.format(<span class="string">"value() in %s for field %s is not valid"</span>,</div><div class="line">                    BindView.class.getSimpleName(), mFieldElement.getSimpleName()));</div><div class="line">        &#125;</div><div class="line">    &#125;</div><div class="line"></div><div class="line">    <span class="function"><span class="keyword">public</span> Name <span class="title">getFieldName</span><span class="params">()</span> </span>&#123;</div><div class="line">        <span class="keyword">return</span> mFieldElement.getSimpleName();</div><div class="line">    &#125;</div><div class="line"></div><div class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">getResId</span><span class="params">()</span> </span>&#123;</div><div class="line">        <span class="keyword">return</span> mResId;</div><div class="line">    &#125;</div><div class="line"></div><div class="line">    <span class="function"><span class="keyword">public</span> TypeMirror <span class="title">getFieldType</span><span class="params">()</span> </span>&#123;</div><div class="line">        <span class="keyword">return</span> mFieldElement.asType();</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>处理过程其实很容易懂。因为@BindView指定了@Target(ElementType.FIELD)，所以在处理时需要判断<br><figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="keyword">if</span>(element.getKind() != ElementKind.FIELD)</div></pre></td></tr></table></figure></p>
<p>最后处理相应的注解，获取到注解的value()字段值–即绑定的资源id。</p>
<h3 id="4-处理-ContentView和-OnClick"><a href="#4-处理-ContentView和-OnClick" class="headerlink" title="(4) 处理@ContentView和@OnClick"></a>(4) 处理@ContentView和@OnClick</h3><p>&emsp;&emsp;基于同样的过程，分别通过roundEnvironment.getElementsAnnotatedWith()方法返回所有被注解了@ContentView和@OnClick的元素列表，并分别定义BindLayout和OnClickMethod两个数据模型类，处理相应的注解信息。实现如下：</p>
<p>BindLayout的实现：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">BindLayout</span> </span>&#123;</div><div class="line"></div><div class="line">    <span class="keyword">private</span> TypeElement mTypeElement;</div><div class="line"></div><div class="line">    <span class="keyword">private</span> <span class="keyword">int</span> mLayoutId;</div><div class="line"></div><div class="line">    <span class="function"><span class="keyword">public</span> <span class="title">BindLayout</span><span class="params">(Element element)</span> <span class="keyword">throws</span> IllegalArgumentException </span>&#123;</div><div class="line">        mTypeElement = (TypeElement)element;</div><div class="line"></div><div class="line">        <span class="comment">// 获取注解和值</span></div><div class="line">        ContentView contentView = mTypeElement.getAnnotation(ContentView.class);</div><div class="line">        mLayoutId = contentView.value();</div><div class="line">        <span class="keyword">if</span>(mLayoutId &lt; <span class="number">0</span>) &#123;</div><div class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> IllegalArgumentException(String.format(<span class="string">"value() in %s for field %s is not valid"</span>,</div><div class="line">                    ContentView.class.getSimpleName(), mTypeElement.getSimpleName()));</div><div class="line">        &#125;</div><div class="line">    &#125;</div><div class="line"></div><div class="line">    <span class="comment">// 获取layout的id，即注解的value值</span></div><div class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">getLayoutId</span><span class="params">()</span> </span>&#123;</div><div class="line">        <span class="keyword">return</span> mLayoutId;</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>OnClickMethod的实现：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">OnClickMethod</span> </span>&#123;</div><div class="line"></div><div class="line">    <span class="keyword">private</span> Name mMethodName;</div><div class="line"></div><div class="line">    <span class="keyword">public</span> <span class="keyword">int</span>[] ids;</div><div class="line"></div><div class="line">    <span class="function"><span class="keyword">public</span> <span class="title">OnClickMethod</span><span class="params">(Element element)</span> </span>&#123;</div><div class="line">        <span class="keyword">if</span>(element.getKind() != ElementKind.METHOD) &#123;</div><div class="line">            <span class="comment">// 判断使用该注解的元素是否是METHOD</span></div><div class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> IllegalArgumentException(String.format(<span class="string">"Only method can be annotated with @%s"</span>,</div><div class="line">                    OnClick.class.getSimpleName()));</div><div class="line">        &#125;</div><div class="line">        ExecutableElement methodElement = (ExecutableElement)element;</div><div class="line">        mMethodName = methodElement.getSimpleName();</div><div class="line">        ids = methodElement.getAnnotation(OnClick.class).value();</div><div class="line">        <span class="keyword">if</span>(ids == <span class="keyword">null</span>) &#123;</div><div class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> IllegalArgumentException(String.format(<span class="string">"Must set valid ids for @%s"</span>, OnClick.class.getSimpleName()));</div><div class="line">        &#125; <span class="keyword">else</span> &#123;</div><div class="line">            <span class="keyword">for</span>(<span class="keyword">int</span> id : ids) &#123;</div><div class="line">                <span class="keyword">if</span>(id &lt; <span class="number">0</span>) &#123;</div><div class="line">                    <span class="keyword">throw</span> <span class="keyword">new</span> IllegalArgumentException(String.format(<span class="string">"Must set valid ids for @%s"</span>, OnClick.class.getSimpleName()));</div><div class="line">                &#125;</div><div class="line">            &#125;</div><div class="line">        &#125;</div><div class="line"></div><div class="line">        List&lt;? extends VariableElement&gt; parameters = methodElement.getParameters();</div><div class="line">        <span class="keyword">if</span>(parameters.size() &gt; <span class="number">0</span>) &#123;</div><div class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> IllegalArgumentException(String.format(<span class="string">"The method annotated with @%s must have no parameters"</span>, OnClick.class.getSimpleName()));</div><div class="line">        &#125;</div><div class="line">    &#125;</div><div class="line"></div><div class="line">    <span class="function"><span class="keyword">public</span> Name <span class="title">getMethodName</span><span class="params">()</span> </span>&#123;</div><div class="line">        <span class="keyword">return</span> mMethodName;</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<h3 id="5-数据模型"><a href="#5-数据模型" class="headerlink" title="(5) 数据模型"></a>(5) 数据模型</h3><p>&emsp;&emsp;在之前已经分别定义了BindLayout，BindViewField，OnClickMethod来分别存储获取到的注解的信息。接下来通过ButterKnifeAnnotatedClass将所有的注解信息进行组合。回顾一下整个注解处理器的Module的结构：</p>
<center><br><img src="http://occl9k36n.bkt.clouddn.com/2017_02_11_processor_module.png" alt="processor module概览"><br></center>

<p>&emsp;&emsp;可以看到每一个待处理的注解元素均对应着一个Model文件，最后通过ButterKnifeAnnotatedClass将这些信息进行组合，最后生成代码也是通过ButterKnifeAnnotatedClass来进行，大致实现如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ButterKnifeAnnotatedClass</span> </span>&#123;</div><div class="line"></div><div class="line">    <span class="comment">// 类名</span></div><div class="line">    <span class="keyword">public</span> TypeElement mClassElement;</div><div class="line"></div><div class="line">    <span class="comment">// 元素辅助类</span></div><div class="line">    <span class="keyword">public</span> Elements mElementsUtils;</div><div class="line"></div><div class="line">    <span class="comment">// 被@ContentView注解标记的变量</span></div><div class="line">    <span class="keyword">public</span> BindLayout mBindLayout;</div><div class="line"></div><div class="line">    <span class="comment">// 成员变量集合，被@BindView注解标记的变量集合</span></div><div class="line">    <span class="keyword">public</span> List&lt;BindViewField&gt; mBindViewFieldList;</div><div class="line"></div><div class="line">    <span class="comment">// 成员变量集合，被@OnClick注解标记的变量集合</span></div><div class="line">    <span class="keyword">public</span> List&lt;OnClickMethod&gt; mOnClickMethodList;</div><div class="line"></div><div class="line">    <span class="function"><span class="keyword">public</span> <span class="title">ButterKnifeAnnotatedClass</span><span class="params">(TypeElement classElement, Elements elementUtils)</span> </span>&#123;</div><div class="line">        <span class="keyword">this</span>.mClassElement = classElement;</div><div class="line">        <span class="keyword">this</span>.mElementsUtils = elementUtils;</div><div class="line">        <span class="keyword">this</span>.mBindViewFieldList = <span class="keyword">new</span> ArrayList&lt;&gt;();</div><div class="line">        <span class="keyword">this</span>.mOnClickMethodList = <span class="keyword">new</span> ArrayList&lt;&gt;();</div><div class="line">    &#125;</div><div class="line">    </div><div class="line">    <span class="comment">// 添加BindLayout</span></div><div class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setBindLayoutField</span><span class="params">(BindLayout field)</span> </span>&#123;</div><div class="line">        mBindLayout = field;</div><div class="line">    &#125;</div><div class="line"></div><div class="line">    <span class="comment">// 添加BindViewField</span></div><div class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">addBindViewField</span><span class="params">(BindViewField field)</span> </span>&#123;</div><div class="line">        mBindViewFieldList.add(field);</div><div class="line">    &#125;</div><div class="line"></div><div class="line">    <span class="comment">// 添加OnClickMethod</span></div><div class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">addOnClickMethod</span><span class="params">(OnClickMethod method)</span> </span>&#123;</div><div class="line">        mOnClickMethodList.add(method);</div><div class="line">    &#125;</div><div class="line">    </div><div class="line">    ......</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>可以看到所有处理得到的注解信息最后均保存在ButterKnifeAnnotatedClass数据模型中。</p>
<h2 id="6-代码生成"><a href="#6-代码生成" class="headerlink" title="6.代码生成"></a>6.代码生成</h2><h3 id="1-生成代码的方法"><a href="#1-生成代码的方法" class="headerlink" title="(1) 生成代码的方法"></a>(1) 生成代码的方法</h3><p>&emsp;&emsp;现在所有注解的类信息均保存为相应的数据模型，并且组合到了ButterKnifeAnnotatedClass中，接下来就需要生成生成Java文件了，生成java代码的方法目前主要有如下的几种方式：</p>
<blockquote>
<ul>
<li>使用Filer提供的Writer对象，通过连接字符串来生成的Java代码(这个Filer就是在init()方法中获取到的),实际操作起来比较繁琐；</li>
<li>使用JavaWriter生成java源文件；</li>
<li>使用codemodel生成Java源文件;</li>
<li>使用Square公司javapoet生成Java源文件;</li>
</ul>
</blockquote>
<p>关于后三个开源项目的使用对比，有兴趣的同学可以参加链接–<a href="http://www.jianshu.com/p/ae0fa4761dd8" target="_blank" rel="external">生成Java源文件 (javawriter, javapoet, codemodel)</a>。在本文中，我们采用通过javapoet的技术进行相应的实现。</p>
<h3 id="2-javapoet的使用"><a href="#2-javapoet的使用" class="headerlink" title="(2) javapoet的使用"></a>(2) javapoet的使用</h3><p>&emsp;&emsp;javapoet是大名鼎鼎的square公司开源的一个项目，<strong>javapoet要比JavaWriter稍微复杂，此库定义了一系列的数据结构来表示Java源文件中某些固定的概念，如class，interface，annoation，field，method等。javapoet对java源文件的结构进行了建模</strong>，常用的几个包括：</p>
<blockquote>
<ul>
<li>MethodSpec：代表一个构造函数或方法声明。</li>
<li>TypeSpec：代表一个类，接口，或者枚举声明。</li>
<li>FieldSpec：代表一个成员变量，一个字段声明。</li>
<li>JavaFile：包含一个顶级类的Java文件。</li>
</ul>
</blockquote>
<p>比如我们想生成一个简单的HelloWorld代码如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="keyword">package</span> com.example.helloworld;</div><div class="line"></div><div class="line"><span class="keyword">public</span> <span class="keyword">final</span> <span class="class"><span class="keyword">class</span> <span class="title">HelloWorld</span> </span>&#123;</div><div class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</div><div class="line">    System.out.println(<span class="string">"Hello, JavaPoet!"</span>);</div><div class="line">  &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>可以通过如下代码实现：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line">MethodSpec main = MethodSpec.methodBuilder(<span class="string">"main"</span>)</div><div class="line">    .addModifiers(Modifier.PUBLIC, Modifier.STATIC)</div><div class="line">    .returns(<span class="keyword">void</span>.class)</div><div class="line">    .addParameter(String[].class, <span class="string">"args"</span>)</div><div class="line">    .addStatement(<span class="string">"$T.out.println($S)"</span>, System.class, <span class="string">"Hello, JavaPoet!"</span>)</div><div class="line">    .build();</div><div class="line"></div><div class="line">TypeSpec helloWorld = TypeSpec.classBuilder(<span class="string">"HelloWorld"</span>)</div><div class="line">    .addModifiers(Modifier.PUBLIC, Modifier.FINAL)</div><div class="line">    .addMethod(main)</div><div class="line">    .build();</div><div class="line"></div><div class="line">JavaFile javaFile = JavaFile.builder(<span class="string">"com.example.helloworld"</span>, helloWorld)</div><div class="line">    .build();</div><div class="line"></div><div class="line">javaFile.writeTo(System.out);</div></pre></td></tr></table></figure>
<p>更多关于Javapoet的使用方式可以参见<a href="https://github.com/square/javapoet" target="_blank" rel="external">javapoet的Github地址</a>。</p>
<h3 id="3-生成最终的代码"><a href="#3-生成最终的代码" class="headerlink" title="(3) 生成最终的代码"></a>(3) 生成最终的代码</h3><p>首先在注解处理器的Module中添加依赖：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line">compile <span class="string">'com.squareup:javapoet:1.7.0'</span></div></pre></td></tr></table></figure>
<p>这里在ButterKnifeAnnotatedClass类的generateFinder()方法中实现最终代码的生成：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="comment">// 生成代码的关键部分</span></div><div class="line"><span class="function"><span class="keyword">public</span> JavaFile <span class="title">generateFinder</span><span class="params">()</span> </span>&#123;</div><div class="line">    <span class="comment">//构建方法</span></div><div class="line">    MethodSpec.Builder methodBuilder = MethodSpec.methodBuilder(<span class="string">"inject"</span>)</div><div class="line">            .addModifiers(Modifier.PUBLIC)                  </div><div class="line">            .addAnnotation(Override.class)                  </div><div class="line">            .addParameter(TypeName.get(mClassElement.asType()), <span class="string">"host"</span>, Modifier.FINAL) </div><div class="line">            .addParameter(TypeName.OBJECT, <span class="string">"source"</span>)                        </div><div class="line">            .addParameter(TypeUtil.FINDER, <span class="string">"finder"</span>);                           </div><div class="line"></div><div class="line">    <span class="comment">// 生成绑定布局文件相关的代码</span></div><div class="line">    <span class="keyword">if</span>(mBindLayout != <span class="keyword">null</span>) &#123;</div><div class="line">        <span class="comment">// 如果 layoutId 不为 0 ，那说明有绑定，添加一句 setContentView</span></div><div class="line">        <span class="comment">// 要注意的是，这句要比 view 注入在前面</span></div><div class="line">        <span class="keyword">if</span>(mBindLayout.getLayoutId() != <span class="number">0</span>) &#123;</div><div class="line">            methodBuilder.addStatement(<span class="string">"host.setContentView($L)"</span>, mBindLayout.getLayoutId());</div><div class="line">        &#125;</div><div class="line">    &#125;</div><div class="line"></div><div class="line">    <span class="comment">// 生成View注入相关的代码</span></div><div class="line">    <span class="keyword">for</span>(BindViewField field : mBindViewFieldList) &#123;</div><div class="line">        <span class="comment">// 添加一行</span></div><div class="line">        methodBuilder.addStatement(<span class="string">"host.$N=($T)finder.findView(source,$L)"</span>, field.getFieldName()</div><div class="line">                , ClassName.get(field.getFieldType()), field.getResId());</div><div class="line">    &#125;</div><div class="line"></div><div class="line">    <span class="comment">// 添加声明的Listener</span></div><div class="line">    <span class="keyword">if</span>(mOnClickMethodList.size() &gt; <span class="number">0</span>) &#123;</div><div class="line">        methodBuilder.addStatement(<span class="string">"$T listener"</span>, TypeUtil.ONCLICK_LISTENER);</div><div class="line">    &#125;</div><div class="line"></div><div class="line">    <span class="keyword">for</span>(OnClickMethod method : mOnClickMethodList) &#123;</div><div class="line">        TypeSpec listener = TypeSpec.anonymousClassBuilder(<span class="string">""</span>)</div><div class="line">                .addSuperinterface(TypeUtil.ONCLICK_LISTENER)</div><div class="line">                .addMethod(MethodSpec.methodBuilder(<span class="string">"onClick"</span>)</div><div class="line">                        .addAnnotation(Override.class)</div><div class="line">                        .addModifiers(Modifier.PUBLIC)</div><div class="line">                        .returns(TypeName.VOID)</div><div class="line">                        .addParameter(TypeUtil.ANDROID_VIEW, <span class="string">"view"</span>)</div><div class="line">                        .addStatement(<span class="string">"host.$N()"</span>, method.getMethodName())</div><div class="line">                        .build())</div><div class="line">                .build();</div><div class="line">        methodBuilder.addStatement(<span class="string">"listener = $L "</span>, listener);</div><div class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> id : method.ids) &#123;</div><div class="line">            methodBuilder.addStatement(<span class="string">"finder.findView(source,$L).setOnClickListener(listener)"</span>, id);</div><div class="line">        &#125;</div><div class="line">    &#125;</div><div class="line"></div><div class="line">    String packageName = getPackageName(mClassElement);</div><div class="line">    String className = getClassName(mClassElement,packageName);</div><div class="line">    ClassName bindClassName = ClassName.get(packageName,className);</div><div class="line"></div><div class="line">    <span class="comment">// 构建类</span></div><div class="line">    TypeSpec finderClass = TypeSpec.classBuilder(bindClassName.simpleName()+ <span class="string">"$$Injector"</span>)</div><div class="line">            .addModifiers(Modifier.PUBLIC)</div><div class="line">            .addSuperinterface(ParameterizedTypeName.get(TypeUtil.INJECTOR, TypeName.get(mClassElement.asType())))</div><div class="line">            .addMethod(methodBuilder.build())</div><div class="line">            .build();</div><div class="line">    <span class="keyword">return</span> JavaFile.builder(packageName, finderClass).build();</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>尝试编译运行一下程序，在<strong>resource文件夹下的apt文件夹目录下</strong>，就生成了相应的java文件，如下截图所示：</p>
<center><br><img src="http://occl9k36n.bkt.clouddn.com/2017_02_13_apt_code_review.png" alt="生成的apt代码概览"><br></center>

<h2 id="7-调用生成的代码"><a href="#7-调用生成的代码" class="headerlink" title="7.调用生成的代码"></a>7.调用生成的代码</h2><p>回顾一下，在使用ButterKnife的Activity文件中，通常会有如下代码：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line">ButterKnife.bind(<span class="keyword">this</span>);</div></pre></td></tr></table></figure>
<p>&emsp;&emsp;这里实质上就是运行时动态的调用生成的代码，同样的，在我们自己实现的简易的ButterKnife框架中，也需要这样的一个过程。如上一节的最后一部分，在resource文件夹下的apt文件夹目录下生成了最终的代码，它和其他手写的Java类没有任何区别。<strong>最后要做的工作就是像其他Java对象一样去对他进行实例化。</strong><br>&emsp;&emsp;查看ButterKnife的源码，会发现<strong>ButterKnife内部最后是使用反射机制来实例化对象的</strong>。虽然反射机制会带来一定的的性能问题。然而这种方式不需要手动去创建对象，从而提高开发者的开发速度。ButterKnife中有一个哈希表HashMap来缓存实例化过的对象。所以只是使用反射机制实例化一次，第二次使用的时候，就直接冲哈希表中获得。<br>&emsp;&emsp;参照ButterKnife的实现，最终调用代码如下所示：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MyButterKnife</span> </span>&#123;</div><div class="line"></div><div class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> MyFinder MY_FINDER = <span class="keyword">new</span> MyFinder();</div><div class="line"></div><div class="line">    <span class="keyword">private</span> <span class="keyword">static</span> Map&lt;String, Injector&gt; FINDER_MAP = <span class="keyword">new</span> HashMap&lt;&gt;();</div><div class="line"></div><div class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">bind</span><span class="params">(Activity activity)</span> </span>&#123;</div><div class="line">        bind(activity, activity, MY_FINDER);</div><div class="line">    &#125;</div><div class="line"></div><div class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">bind</span><span class="params">(Object host, Object source, Finder finder)</span> </span>&#123;</div><div class="line">        String className = host.getClass().getName();</div><div class="line">        <span class="keyword">try</span> &#123;</div><div class="line">            Injector injector = FINDER_MAP.get(className);</div><div class="line">            <span class="keyword">if</span> (injector == <span class="keyword">null</span>) &#123;</div><div class="line">                Class&lt;?&gt; finderClass = Class.forName(className + <span class="string">"$$Injector"</span>);</div><div class="line">                injector = (Injector) finderClass.newInstance();</div><div class="line">                FINDER_MAP.put(className, injector);</div><div class="line">            &#125;</div><div class="line">            injector.inject(host, source, finder);</div><div class="line">        &#125; <span class="keyword">catch</span> (Exception e) &#123;</div><div class="line">            e.printStackTrace();</div><div class="line">        &#125;</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>在相应的Activity文件中，尝试使用我们自己实现的简易ButterKnife即可：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="meta">@ContentView</span>(R.layout.activity_apt_impl)</div><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">AptImplActivity</span> <span class="keyword">extends</span> <span class="title">AppCompatActivity</span> </span>&#123;</div><div class="line"></div><div class="line">    <span class="meta">@BindView</span>(R.id.button1)</div><div class="line">    Button mButton1;</div><div class="line"></div><div class="line">    <span class="meta">@BindView</span>(R.id.button2)</div><div class="line">    Button mButton2;</div><div class="line"></div><div class="line">    <span class="meta">@OnClick</span>(&#123;R.id.button1&#125;)</div><div class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">click</span><span class="params">()</span> </span>&#123;</div><div class="line">        Toast.makeText(<span class="keyword">this</span>, <span class="string">"点击了第一个按钮"</span>, Toast.LENGTH_SHORT).show();</div><div class="line">    &#125;</div><div class="line"></div><div class="line">    <span class="meta">@OnClick</span>(&#123;R.id.button2&#125;)</div><div class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">click2</span><span class="params">()</span> </span>&#123;</div><div class="line">        Toast.makeText(<span class="keyword">this</span>, <span class="string">"点击了第二个按钮"</span>, Toast.LENGTH_SHORT).show();</div><div class="line">    &#125;</div><div class="line"></div><div class="line">    <span class="meta">@Override</span></div><div class="line">    <span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">onCreate</span><span class="params">(Bundle savedInstanceState)</span> </span>&#123;</div><div class="line">        <span class="keyword">super</span>.onCreate(savedInstanceState);</div><div class="line">        <span class="comment">//setContentView(R.layout.activity_apt_impl);</span></div><div class="line">        MyButterKnife.bind(<span class="keyword">this</span>);</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<h2 id="8-结语"><a href="#8-结语" class="headerlink" title="8.结语"></a>8.结语</h2><p>&emsp;&emsp;本文尝试从自己实现一个简易ButterKnife的角度，来对ButterKnife的原理和java注解机制进行比较深入的分析学习。其实本文的实现过程也就是目前ButterKnife的简易实现版本，网上关于ButterKnife的源码分析文章也很多，在最后的参考文献部分也列出了一些比较好的文章，有兴趣的同学可以参考。到这里，关于java注解机制的原理和ButterKnife相关系列的问题就总结完毕了。另外两篇同系列的文章参见：</p>
<p><a href="https://caofengbin.github.io/2017/01/19/annotation-base-use/" target="_blank" rel="external">Java注解机制基础</a><br><a href="https://caofengbin.github.io/2017/01/20/inside-learn-butter-knife/" target="_blank" rel="external">ButterKnife的使用与原理(1)</a></p>
<hr>
<h2 id="参考文献"><a href="#参考文献" class="headerlink" title="参考文献"></a>参考文献</h2><p><a href="https://www.zhangningning.com.cn/blog/Android/android_rentention.html" target="_blank" rel="external">1.Android中使用注解</a><br><a href="http://www.jianshu.com/p/ae0fa4761dd8" target="_blank" rel="external">2.生成Java源文件 (javawriter, javapoet, codemodel)</a><br><a href="https://github.com/square/javapoet" target="_blank" rel="external">3.javapoet的Github地址</a><br><a href="http://hannesdorfmann.com/annotation-processing/annotationprocessing101" target="_blank" rel="external">4.annotationprocessing101</a><br><a href="http://www.jianshu.com/p/95f12f72f69a" target="_blank" rel="external">5.javapoet——让你从重复无聊的代码中解放出来</a><br><a href="http://openjdk.java.net/groups/compiler/doc/compilation-overview/index.html" target="_blank" rel="external">6.Compilation Overview</a><br><a href="https://zhuanlan.zhihu.com/p/22248846" target="_blank" rel="external">7.深入理解 ButterKnife，让你的程序学会写代码</a><br><a href="https://zhuanlan.zhihu.com/p/20201755" target="_blank" rel="external">8.ButterKnife源码解析1</a><br><a href="https://zhuanlan.zhihu.com/p/20231567" target="_blank" rel="external">9.ButterKnife源码解析2</a></p>
]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;1-前言&quot;&gt;&lt;a href=&quot;#1-前言&quot; class=&quot;headerlink&quot; title=&quot;1.前言&quot;&gt;&lt;/a&gt;1.前言&lt;/h2&gt;&lt;p&gt;&amp;emsp;&amp;emsp;本文是上一篇文章的续篇。上次大致介绍了ButterKnife的基本使用以及通过反射技术模拟实现了简易
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title>2016 top Android Libraty</title>
    <link href="http://haojen.github.io/2017/01/21/top-anroid-library/"/>
    <id>http://haojen.github.io/2017/01/21/top-anroid-library/</id>
    <published>2017-01-21T07:40:26.000Z</published>
    <updated>2017-01-21T08:59:59.522Z</updated>
    
    <content type="html"><![CDATA[<h2 id="0-前言"><a href="#0-前言" class="headerlink" title="0.前言"></a>0.前言</h2><p>&emsp;&emsp;本文内容主要转载于<a href="http://stormzhang.com/2017/01/16/top-10-android-library-of-2016/" target="_blank" rel="external">代码张的博客–2016 TOP 10 ANDROID LIBRARY</a>，记录下2016年比较火的安卓开源库，作为自己新年的一个学习计划。</p>
<h2 id="1-RxJava"><a href="#1-RxJava" class="headerlink" title="1.RxJava"></a>1.RxJava</h2><p><a href="https://github.com/ReactiveX/RxJava" target="_blank" rel="external">RxJava的官方地址</a><br>&emsp;&emsp;2016 年 Android 界最火的莫过于 RxJava 了，如果你还不知道 RxJava，你所在公司，或者你所在的项目还没有使用 RxJava，那真的是有点 out 了，RxJava 不仅大大简化了代码，甚至可以说改变了我们的开发方式。<br>&emsp;&emsp;RxJava 是一种函数式、响应式的异步操作库，它让你的代码更加简洁，真正的让你的代码写到爽！由于 RxJava 用过的都说好，基于此，GitHub 上衍生了一堆比如 RxAndroid、RxBus、RxPermission 等之类的开源库，足以说明它的影响力。<br>&emsp;&emsp;关于 RxJava 的文章网上一大堆，它的使用方法与好处我就不多说了，请自行去搜索了解，总之，<strong>身为 Android 开发者，到现在你还不知道 RxJava，简直了！</strong></p>
<h2 id="2-Retrofit"><a href="#2-Retrofit" class="headerlink" title="2.Retrofit"></a>2.Retrofit</h2><p><a href="https://github.com/square/retrofit" target="_blank" rel="external">Retrofit的官方地址</a></p>
<p>&emsp;&emsp;如果有人问我，Android 界最好用的网络请求库是什么？在之前可能会有人回答 android-async-http、Volley、OkHttp（准确说，OkHttp 是一个 http 请求客户端） 之类的，但是 16 年过后，我会告诉你 Retrofit 是最好用的网络请求库。<br>&emsp;&emsp;Retrofit 完全 RESTful 风格的 api 网络请求库，解耦更彻底，源码设计超多的设计模式，值得大家学习，另外扩展性非常好，支持各种配置来满足你的需求，最最重要的是，如果你的项目使用了 RxJava，那么 Retrofit 可以完美结合，我只能说 Prefect！再次验证了那句话：<strong>Square 出品，必属精品！</strong></p>
<h2 id="3-EventBus"><a href="#3-EventBus" class="headerlink" title="3.EventBus"></a>3.EventBus</h2><p><a href="https://github.com/greenrobot/EventBus" target="_blank" rel="external">EventBus的官方地址</a></p>
<p>&emsp;&emsp;试想这么一个场景，在 A 页面打开 B 页面，然后 B 页面打开了 C 页面，C 页面又打开了 D 页面，而且还需要传递参数，在 D 页面修改了一些信息，然后这些信息更新之后，A、B、C 页面很可能都需要对应的进行数据更新，碰到这种需求该怎么处理？<br>&emsp;&emsp;有人说用 startActivityForResult()，你可以试下，有多么难处理，还有人想到用广播，这个当然可以，因为广播是全局的，主要进行注册都可以通知到每一个页面，但是我很不喜欢用广播，每次用广播都要走那一套流程，很麻烦，而且很重。<br>&emsp;&emsp;而如果你知道 Eventbus，那么一切都非常的简单。<br>&emsp;&emsp;<strong>EventBus 是一个事件管理平台，以事件驱动的方式来简化事件传递逻辑，可以把它想象成轻量级的 BroadcastReceiver，不过，EventBus 并不是 16 年才开始进入大众视野的，很早就开源了，只是这个库太实用了，时至今日，它仍然很火，使用起来非常方便。</strong><br>&emsp;&emsp;值得注意的是：EventBus 固然好用，但是不要过度使用，因为一旦你的代码大量使用 EventBus，会致使代码可读性稍差，而且出了问题不太好定位。所以建议只在特定的场景使用，切莫贪杯！</p>
<h2 id="4-Glide，Fresco"><a href="#4-Glide，Fresco" class="headerlink" title="4.Glide，Fresco"></a>4.Glide，Fresco</h2><p>&emsp;&emsp;图片加载可能跟网络请求一样，基本是所有 App 开发必备的功能，选择一款成熟稳定的图片加载库重要性不言而喻，目前<strong>主流的图片加载有 Picasso、Glide、Fresco，Glide 是 Google 员工基于 Picasso 基础上进行开发的，所以自然各方面比 Picasso 更有优势，而且支持 Gif，所以推荐大家优先选择 Glide 库</strong>，官方地址：<br><a href="https://github.com/bumptech/glide" target="_blank" rel="external">Glide的官方地址</a><br>&emsp;&emsp;如果你的项目需要大量使用图片，比如是类似 Instagram 一类的图片社交 App ，那么推荐使用 Fresco。Fresco 是 Facebook 作品，关于内存的占用优化更好，但是同时包也更大，门槛也更高，初级工程师不建议使用。官方地址：<br><a href="https://github.com/facebook/fresco" target="_blank" rel="external">Fresco的官方地址</a></p>
<h2 id="5-LeakCanary"><a href="#5-LeakCanary" class="headerlink" title="5.LeakCanary"></a>5.LeakCanary</h2><p><a href="https://github.com/square/leakcanary" target="_blank" rel="external">LeakCanary的官方地址</a><br>&emsp;&emsp;开发者最关心的除了完成功能外，其次就是会不会造成内存泄露了，其实检测内存泄露在 Java 领域有很多种方法与工具，但是针对 Android 都不够方便，而<strong>良心公司 Square 开源了一款针对 Android 平台的内存泄露检测工具 LeakCanary，集成简单，使用方便，平时测试的过程中就自动记录了内存泄露的位置</strong>，甚至帮你定位到代码级别，强烈推荐。</p>
<h2 id="6-ButterKnife"><a href="#6-ButterKnife" class="headerlink" title="6.ButterKnife"></a>6.ButterKnife</h2><p><a href="https://github.com/JakeWharton/butterknife" target="_blank" rel="external">ButterKnife的官方地址</a><br>&emsp;&emsp;我想应该没有人没听过这个库了吧？ButterKnife 是 Android 之神 JakeWharton 的大作，已经开源了很长时间，然而在 2016 年它的使用热度依然不减，<strong>它可以让你避免无休止的 findViewById() 代码</strong>，具体用法我就不多说了，使用起来比较简单。</p>
<h2 id="7-Realm"><a href="#7-Realm" class="headerlink" title="7.Realm"></a>7.Realm</h2><p><a href="https://realm.io/" target="_blank" rel="external">Realm的官方地址</a><br> &emsp;&emsp;Android 界的 ORM 框架有很多，比如 GreeDao、SugarORM、ActiveAndroid 等等，但是我推荐大家的 ORM 框架以上都不是，是叫做 Realm。<br>&emsp;&emsp;Realm 是一种面向移动端的新型轻量数据库，而且是开源的，<strong>跟 SQLite 完全不一样，性能上秒杀 SQLite</strong>，支持 Java、Android、iOS 各平台，我们在实际项目中采用过，体验下来各方面都很不错，所以推荐大家尝试下 Realm。</p>
<h2 id="8-Dagger2"><a href="#8-Dagger2" class="headerlink" title="8.Dagger2"></a>8.Dagger2</h2><p><a href="https://github.com/google/dagger" target="_blank" rel="external">Dagger2的官方地址</a><br>&emsp;&emsp;依赖注入的概念估计大家都听过，不理解的不妨搜索了解下，<strong>Android 领域比较著名的依赖注入库莫过于 Dagger 了，基于注解，使用起来异常方便</strong>。<br>&emsp;&emsp;Dagger 起初是 Square 开源的，后来 Google 在此技术上进行了改进与优化，<strong>去除了反射，编译时进行依赖注入，性能上有大幅提升，取名 Dagger 2</strong>，Square 之前开源的 Dagger 已不建议使用。其实之前大家对 Dagger 的关注程度没有那么高，一般都是属于中、高级工程师才会关注使用，但是 16 年 Android 的架构被提上日程，各种 MVP、MVVM、Clean 架构等讨论的较多，而 Dagger 作为承载这些架构重要的一环被越来越多的开发者使用，所以 16 年我们看到 Dagger 的身影越来越多.</p>
<h2 id="9-android-architecture"><a href="#9-android-architecture" class="headerlink" title="9.android-architecture"></a>9.android-architecture</h2><p><a href="https://github.com/googlesamples/android-architecture" target="_blank" rel="external">android-architecture的官方地址</a><br>&emsp;&emsp;2016 年 Android 架构被越来越多的开发者关注，国内外关于架构的探讨比较活跃，大家熟知的 MVC、MVP、MVVM、Clean 等，就在大家争执哪个更好，Android 开发到底该怎样架构的时候，<strong>Google 开源了一个 Android 架构的官方指导，涉及 mvp、mvp-loaders、databinding、mvp-clean、mvp-dagger、mvp-contentproviders、mvp-rxjava 等，分别在各自指定的分支下，有非常大的参考意义，可以算是 Android 界的一大步。</strong></p>
<h2 id="10-awesome-android-ui"><a href="#10-awesome-android-ui" class="headerlink" title="10.awesome-android-ui"></a>10.awesome-android-ui</h2><p><a href="https://github.com/googlesamples/android-architecture" target="_blank" rel="external">android-architecture的官方地址</a><strong>这个库感觉超级实用</strong><br>&emsp;&emsp;Android 开发中除了我们以上用到的各种实用库之外，我们往往还会涉及到各种 UI 效果的实现，对于移动开发，界面开发其中是很重要的一环，而 16 年针对 Android 开发有人开源整理了这么一个库，里面网罗了所有你见过的、没见过的各种 UI 效果，涉及 Material、Layout、Button、List、ViewPager、Dialog、Menu、Parallax、Progress 等等，而且有相对应的截图、gif 展示，以后应对设计师各种效果的时候有很大的参考帮助作用。</p>
]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;0-前言&quot;&gt;&lt;a href=&quot;#0-前言&quot; class=&quot;headerlink&quot; title=&quot;0.前言&quot;&gt;&lt;/a&gt;0.前言&lt;/h2&gt;&lt;p&gt;&amp;emsp;&amp;emsp;本文内容主要转载于&lt;a href=&quot;http://stormzhang.com/2017/01/16
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title>ButterKnife的使用与原理(1)</title>
    <link href="http://haojen.github.io/2017/01/20/inside-learn-butter-knife/"/>
    <id>http://haojen.github.io/2017/01/20/inside-learn-butter-knife/</id>
    <published>2017-01-20T06:21:52.000Z</published>
    <updated>2017-01-20T09:24:42.722Z</updated>
    
    <content type="html"><![CDATA[<h2 id="1-概述"><a href="#1-概述" class="headerlink" title="1.概述"></a>1.概述</h2><p>&emsp;&emsp;ButterKnife是一个<strong>依托 Java 的注解机制来实现辅助代码生成的框架</strong>。对于注解机制还不是太熟悉或者了解的同学，可以参见笔者自己总结的一篇文章–<a href="https://caofengbin.github.io/2017/01/19/annotation-base-use/" target="_blank" rel="external">Java注解机制基础</a>。本文大致讲述ButterKnife常用的特性和使用过程中的一些要点。并分别通过反射机制以及apt(Annotation Processing Tool)模拟实现ButterKnife的效果。希望可以让大家了解到 Java 注解机制的强大之处，并对 ButterKnife，Dagger  这样类似的框架有一定的认识。</p>
<hr>
<h2 id="2-ButtrerKnife的简介与使用"><a href="#2-ButtrerKnife的简介与使用" class="headerlink" title="2.ButtrerKnife的简介与使用"></a>2.ButtrerKnife的简介与使用</h2><center><br><img src="https://raw.githubusercontent.com/JakeWharton/butterknife/master/website/static/logo.png" alt="ButterKnife的logo)"><br></center>

<p>&emsp;&emsp;作为一名Android开发，我们总是会写大量的findViewById和点击事件，像初始view、设置view监听这样简单而重复的操作，当然不会偷懒的程序员不是好程序员，自然也出现了相应的解决方案–依赖注入。而ButterKnife则是依赖注入中相对简单易懂的一个开源框架，让开发人员从这些烦人臃肿的代码中解脱出来。ButtrerKnife目前最新的版本已经更新到了8.4.0，本文的内容也是基于最新版本进行。</p>
<h3 id="1-ButterKnife特性"><a href="#1-ButterKnife特性" class="headerlink" title="(1)ButterKnife特性"></a>(1)ButterKnife特性</h3><p>先看一下ButterKnife项目的文件概览：</p>
<center><br><img src="http://occl9k36n.bkt.clouddn.com/2017_01_14_ButterKnife_overreview.png" alt="ButterKnife概览)"><br></center>

<p>可以发现ButterKnife主要支持：</p>
<blockquote>
<ul>
<li>(1)View注入：@BindView</li>
<li>(2)常用的资源类型的注入：@BindBool,@BindColor,@BindDimen,@BindString等；</li>
<li>(3)View事件回调函数注入：<br>View：@OnClick，@OnFocusChanged，@OnLongClick，@OnTouch<br>TextView：@OnEditorAction，@OnTextChanged<br>AdapterView：@OnItemClick，@OnItemLongClick，OnItemSelected，<br>CompoundButton：@OnCheckedChanged，<br>ViewPager：@OnPageChange</li>
</ul>
</blockquote>
<h3 id="2-添加相关的依赖"><a href="#2-添加相关的依赖" class="headerlink" title="(2)添加相关的依赖"></a>(2)添加相关的依赖</h3><p>由于 <strong>Butterknife 用到了注解处理器</strong>(后文会详细的讲解注解处理器相关的内容)，所以，比起一般的框架，配置稍微多了些，需要在Gradle文件中配置：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line"></div><div class="line">compile <span class="string">'com.jakewharton:butterknife:(insert latest version)'</span></div><div class="line">annotationProcessor <span class="string">'com.jakewharton:butterknife-compiler:(insert latest version)'</span></div></pre></td></tr></table></figure>
<p>本文主要基于8.0的版本，所以配置如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line">buildscript &#123;</div><div class="line">     repositories &#123;</div><div class="line">       mavenCentral()</div><div class="line">     &#125;</div><div class="line">     dependencies &#123;</div><div class="line">       classpath <span class="string">'com.android.tools.build:gradle:1.3.1'</span></div><div class="line">       classpath <span class="string">'com.neenbedankt.gradle.plugins:android-apt:1.8'</span></div><div class="line">     &#125;</div><div class="line"> &#125;</div><div class="line"> apply plugin: <span class="string">'com.neenbedankt.android-apt'</span></div><div class="line"> ...</div><div class="line"> dependencies &#123;</div><div class="line">     compile <span class="string">'com.jakewharton:butterknife:8.0.1'</span></div><div class="line">    annotationProcessor <span class="string">'com.jakewharton:butterknife-compiler:8.0.1'</span></div><div class="line"> &#125;</div></pre></td></tr></table></figure>
<h3 id="3-在Activity中注入示例"><a href="#3-在Activity中注入示例" class="headerlink" title="(3)在Activity中注入示例"></a>(3)在Activity中注入示例</h3><figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="class"><span class="keyword">class</span> <span class="title">ExampleActivity</span> <span class="keyword">extends</span> <span class="title">Activity</span> </span>&#123;</div><div class="line">  <span class="meta">@BindView</span>(R.id.title) TextView title;</div><div class="line">  <span class="meta">@BindView</span>(R.id.subtitle) TextView subtitle;</div><div class="line">  <span class="meta">@BindView</span>(R.id.footer) TextView footer;</div><div class="line"></div><div class="line">  <span class="meta">@Override</span> <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">onCreate</span><span class="params">(Bundle savedInstanceState)</span> </span>&#123;</div><div class="line">    <span class="keyword">super</span>.onCreate(savedInstanceState);</div><div class="line">    setContentView(R.layout.simple_activity);</div><div class="line">    ButterKnife.bind(<span class="keyword">this</span>);</div><div class="line">    <span class="comment">// TODO Use fields...</span></div><div class="line">  &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<h3 id="4-注入资源类型示例"><a href="#4-注入资源类型示例" class="headerlink" title="(4)注入资源类型示例"></a>(4)注入资源类型示例</h3><figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="class"><span class="keyword">class</span> <span class="title">ExampleActivity</span> <span class="keyword">extends</span> <span class="title">Activity</span> </span>&#123;</div><div class="line">  <span class="meta">@BindString</span>(R.string.title) String title;</div><div class="line">  <span class="meta">@BindDrawable</span>(R.drawable.graphic) Drawable graphic;</div><div class="line">  <span class="meta">@BindColor</span>(R.color.red) <span class="keyword">int</span> red; <span class="comment">// int or ColorStateList field</span></div><div class="line">  <span class="meta">@BindDimen</span>(R.dimen.spacer) Float spacer; <span class="comment">// int (for pixel size) or float (for exact value) field</span></div><div class="line">  <span class="comment">// ...</span></div><div class="line">&#125;</div></pre></td></tr></table></figure>
<h3 id="5-在Fragment中注入示例"><a href="#5-在Fragment中注入示例" class="headerlink" title="(5)在Fragment中注入示例"></a>(5)在Fragment中注入示例</h3><figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">FancyFragment</span> <span class="keyword">extends</span> <span class="title">Fragment</span> </span>&#123;</div><div class="line">  <span class="meta">@BindView</span>(R.id.button1) Button button1;</div><div class="line">  <span class="meta">@BindView</span>(R.id.button2) Button button2;</div><div class="line"></div><div class="line">  <span class="meta">@Override</span> <span class="function"><span class="keyword">public</span> View <span class="title">onCreateView</span><span class="params">(LayoutInflater inflater, ViewGroup container, Bundle savedInstanceState)</span> </span>&#123;</div><div class="line">    View view = inflater.inflate(R.layout.fancy_fragment, container, <span class="keyword">false</span>);</div><div class="line">    ButterKnife.bind(<span class="keyword">this</span>, view);</div><div class="line">    <span class="comment">// TODO Use fields...</span></div><div class="line">    <span class="keyword">return</span> view;</div><div class="line">  &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<h3 id="6-注入回调函数示例"><a href="#6-注入回调函数示例" class="headerlink" title="(6)注入回调函数示例"></a>(6)注入回调函数示例</h3><p>下面是几种注入回调函数的方法示例:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="comment">// 带有 Button 参数</span></div><div class="line"><span class="meta">@OnClick</span>(R.id.submit)</div><div class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">sayHi</span><span class="params">(Button button)</span> </span>&#123;</div><div class="line">  button.setText(<span class="string">"Hello!"</span>);</div><div class="line">&#125;</div><div class="line"> </div><div class="line"><span class="comment">// 不带参数</span></div><div class="line"><span class="meta">@OnClick</span>(R.id.submit)</div><div class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">submit</span><span class="params">()</span> </span>&#123;</div><div class="line">  <span class="comment">// TODO submit data to server...</span></div><div class="line">&#125;</div><div class="line"> </div><div class="line"><span class="comment">// 同时注入多个 View 事件</span></div><div class="line"><span class="meta">@OnClick</span>(&#123; R.id.door1, R.id.door2, R.id.door3 &#125;)</div><div class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">pickDoor</span><span class="params">(DoorView door)</span> </span>&#123;</div><div class="line">  <span class="keyword">if</span> (door.hasPrizeBehind()) &#123;</div><div class="line">    Toast.makeText(<span class="keyword">this</span>, <span class="string">"You win!"</span>, LENGTH_SHORT).show();</div><div class="line">  &#125; <span class="keyword">else</span> &#123;</div><div class="line">    Toast.makeText(<span class="keyword">this</span>, <span class="string">"Try again"</span>, LENGTH_SHORT).show();</div><div class="line">  &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<h3 id="7-BINDING-RESET"><a href="#7-BINDING-RESET" class="headerlink" title="(7)BINDING RESET"></a>(7)BINDING RESET</h3><p>如果需要<strong>在界面销毁的时候，把注入的View设置为 Null</strong>，可以采用如下的方式：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">FancyFragment</span> <span class="keyword">extends</span> <span class="title">Fragment</span> </span>&#123;</div><div class="line">  <span class="meta">@BindView</span>(R.id.button1) Button button1;</div><div class="line">  <span class="meta">@BindView</span>(R.id.button2) Button button2;</div><div class="line">  <span class="keyword">private</span> Unbinder unbinder;</div><div class="line"></div><div class="line">  <span class="meta">@Override</span> </div><div class="line">  <span class="function"><span class="keyword">public</span> View <span class="title">onCreateView</span><span class="params">(LayoutInflater inflater, ViewGroup container, Bundle savedInstanceState)</span> </span>&#123;</div><div class="line">    View view = inflater.inflate(R.layout.fancy_fragment, container, <span class="keyword">false</span>);</div><div class="line">    unbinder = ButterKnife.bind(<span class="keyword">this</span>, view);</div><div class="line">    <span class="comment">// TODO Use fields...</span></div><div class="line">    <span class="keyword">return</span> view;</div><div class="line">  &#125;</div><div class="line"></div><div class="line">  <span class="meta">@Override</span> <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">onDestroyView</span><span class="params">()</span> </span>&#123;</div><div class="line">    <span class="keyword">super</span>.onDestroyView();</div><div class="line">    unbinder.unbind();</div><div class="line">  &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>可以看到<strong>ButterKnife返回了一个Unbinder实例</strong>，主要利用Unbinder的unbind()方法。</p>
<p>更多ButterKnife的相关使用示例可以参见<a href="https://jakewharton.github.io/butterknife/" target="_blank" rel="external">ButterKnife官方文档链接</a>以及<a href="https://github.com/JakeWharton/butterknife" target="_blank" rel="external">GitHub上的ButterKnife链接</a></p>
<hr>
<h2 id="3-使用ButtrerKnife的相关注意事项"><a href="#3-使用ButtrerKnife的相关注意事项" class="headerlink" title="3.使用ButtrerKnife的相关注意事项"></a>3.使用ButtrerKnife的相关注意事项</h2><h3 id="1-ButterKnife的优势"><a href="#1-ButterKnife的优势" class="headerlink" title="(1)ButterKnife的优势"></a>(1)ButterKnife的优势</h3><p>通过上文的描述使用，我们可以发现ButterKnife的优势主要体现在如下几点：</p>
<blockquote>
<ul>
<li>(1)强大的View及资源绑定，以及多个事件处理功能，简化代码，提升开发效率；</li>
<li>(2)方便的处理Adapter里的ViewHolder绑定问题；</li>
<li>(3)<strong>使用apt技术在编译时生成代码，运行时不会影响应用的效率；</strong></li>
<li>(4)<strong>增加的代码的清晰度和可读性；</strong></li>
</ul>
</blockquote>
<h3 id="2-ButterKnife使用的几个注意点"><a href="#2-ButterKnife使用的几个注意点" class="headerlink" title="(2)ButterKnife使用的几个注意点"></a>(2)ButterKnife使用的几个注意点</h3><blockquote>
<ul>
<li>(1)在Activity中注入View时，ButterKnife.bind(this)的调用必须在setContentView();之后，且父类bind之后，子类不需要再bind；</li>
<li>(2)在Fragment中注入View时，需要在onCreateView()方法中，使用ButterKnife.bind(this, mRootView);</li>
<li>(3)<strong>在Fragmet生命周期中，onDestroy()方法中需要调用unbind()方法；</strong></li>
<li>(4)setContentView不能通过注解实现，ButterKnife中没有提供相应注解；</li>
<li>(5)<strong>对需要注入的成员，不能用private或者static修饰</strong>，否则会报错(后文原理部分会详细的分析原因)；</li>
<li>(6)<strong>ButterKnife7.0中一个比较大的API变化</strong>：<br>在7.0以前的版本中，叫@InjectView，在7.0版本中，叫@Bind，在8.0版本中，改成了@BindView；<br>在7.0以前的版本中使用ButterKnife.inject(this);  来进行注入<br>在7.0以后的版本中使用ButterKnife.bind(this);  来进行注入</li>
<li>(7)ButterKnife不能在项目中的library module中使用，这是因为library中的R字段的id值不是final类型的，但是我们的应用module却是final类型的。</li>
</ul>
</blockquote>
<h3 id="3-插件使用"><a href="#3-插件使用" class="headerlink" title="(3)插件使用"></a>(3)插件使用</h3><p>&emsp;&emsp;在Android Studio下有一个ButterKnife的插件android-butterknife-zelezny, 该插件可以帮助我们自动生成上述注入代码，<br>安装地址为：<a href="https://github.com/avast/android-butterknife-zelezny" target="_blank" rel="external">android-butterknife-zelezny安装地址</a><br>下面截图演示了具体的实现效果：</p>
<center><br><img src="https://raw.githubusercontent.com/avast/android-butterknife-zelezny/master/img/butter_knife_zelezny_onclick_anim.gif" alt="插件使用效果)"><br></center>

<hr>
<h2 id="4-使用反射模拟实现ButterKnife的效果"><a href="#4-使用反射模拟实现ButterKnife的效果" class="headerlink" title="4.使用反射模拟实现ButterKnife的效果"></a>4.使用反射模拟实现ButterKnife的效果</h2><p>&emsp;&emsp;ButterKnife是一个<strong>依托Java的注解机制</strong>来实现辅助代码生成的框架。关键之处在于如何处理这些自定义的注解来生成代码。根据自定义注解时指定的Retention值不同，注解的解析方式主要分为两种：<strong>运行时解析和编译时解析</strong>。在本节，我们主要采用运行时解析的方式，来模拟实现ButterKnife的效果，加深对这个框架及java注解机制的理解。</p>
<h3 id="1-反射相关API"><a href="#1-反射相关API" class="headerlink" title="(1)反射相关API"></a>(1)反射相关API</h3><p>&emsp;&emsp;运行时解析注解利用的核心技术就是java反射，核心接口为：AnnotatedElement，引用一下官方文档对他的解释：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line">Interface AnnotatedElement</div><div class="line">Represents an annotated element of the program currently running in <span class="keyword">this</span> VM. This <span class="class"><span class="keyword">interface</span> <span class="title">allows</span> <span class="title">annotations</span> <span class="title">to</span> <span class="title">be</span> <span class="title">read</span> <span class="title">reflectively</span>. <span class="title">All</span> <span class="title">annotations</span> <span class="title">returned</span> <span class="title">by</span> <span class="title">methods</span> <span class="title">in</span> <span class="title">this</span> <span class="title">interface</span> <span class="title">are</span> <span class="title">immutable</span> <span class="title">and</span> <span class="title">serializable</span>. <span class="title">It</span> <span class="title">is</span> <span class="title">permissible</span> <span class="title">for</span> <span class="title">the</span> <span class="title">caller</span> <span class="title">to</span> <span class="title">modify</span> <span class="title">the</span> <span class="title">arrays</span> <span class="title">returned</span> <span class="title">by</span> <span class="title">accessors</span> <span class="title">for</span> <span class="title">array</span>-<span class="title">valued</span> <span class="title">enum</span> <span class="title">members</span></span>; it will have no affect on the arrays returned to other callers.</div></pre></td></tr></table></figure>
<p>&emsp;&emsp;<strong>AnnotatedElement接口表示包含注解的元素。java.lang包中的Class和Package，以及java.lang.reflect包中的Constructer,Field和Method都实现了AnnotatedElement接口</strong>。<br>AnnotatedElement接口中常用的几个方法：</p>
<blockquote>
<ul>
<li>(1) getAnnotations()：<br>Returns all annotations present on this element;</li>
<li>(2) getAnnotation(Class<t> annotationClass)：<br>Returns this element’s annotation for the specified type if such an annotation is present, else null.</t></li>
<li>(3) getDeclaredAnnotations()：<br>Returns all annotations that are directly present on this element.</li>
</ul>
</blockquote>
<p>更多详细信息可以参见<a href="https://docs.oracle.com/javase/8/docs/api/java/lang/reflect/AnnotatedElement.html" target="_blank" rel="external">Java处理注解相关的反射API</a></p>
<h3 id="2-实现效果"><a href="#2-实现效果" class="headerlink" title="(2)实现效果"></a>(2)实现效果</h3><p>&emsp;&emsp;基于上一节大致描述的反射相关技术，我们可以实现如下所示的效果，通过@ContentView实现了布局文件的注入，通过@BindView实现控件的注入，通过@OnClick实现简单的事件回调函数的注入。最后在onCreate方法中调用<strong>ViewInjectUtils.bind(this);</strong>进行相应的绑定</p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="meta">@ContentView</span>(R.layout.activity_test_annoattion)</div><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestAnnotationActivity</span> <span class="keyword">extends</span> <span class="title">AppCompatActivity</span> </span>&#123;</div><div class="line"></div><div class="line">    <span class="meta">@BindView</span>(R.id.testButton)</div><div class="line">    <span class="keyword">private</span> Button mButton;</div><div class="line"></div><div class="line">    <span class="meta">@OnClick</span>(R.id.testButton)</div><div class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">onClick</span><span class="params">(View view)</span> </span>&#123;</div><div class="line">        mButton.setText(<span class="string">"我是click后的文字内容"</span>);</div><div class="line">        Toast.makeText(<span class="keyword">this</span>, <span class="string">"按钮被点击了"</span>, Toast.LENGTH_SHORT).show();</div><div class="line">    &#125;</div><div class="line"></div><div class="line">    <span class="meta">@Override</span></div><div class="line">    <span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">onCreate</span><span class="params">(Bundle savedInstanceState)</span> </span>&#123;</div><div class="line">        <span class="keyword">super</span>.onCreate(savedInstanceState);</div><div class="line">        ViewInjectUtils.bind(<span class="keyword">this</span>);</div><div class="line">        mButton.setText(<span class="string">"我是click前的Button内容"</span>);</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<h3 id="3-详细实现原理"><a href="#3-详细实现原理" class="headerlink" title="(3)详细实现原理"></a>(3)详细实现原理</h3><p>首选是需要定义三个相关的注解</p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="meta">@Target</span>(ElementType.TYPE)</div><div class="line"><span class="meta">@Retention</span>(RetentionPolicy.RUNTIME)</div><div class="line"><span class="keyword">public</span> <span class="meta">@interface</span> ContentView &#123;</div><div class="line">    <span class="function"><span class="keyword">int</span> <span class="title">value</span><span class="params">()</span></span>;</div><div class="line">&#125;</div><div class="line"></div><div class="line"><span class="meta">@Target</span>(ElementType.FIELD)</div><div class="line"><span class="meta">@Retention</span>(RetentionPolicy.RUNTIME)</div><div class="line"><span class="keyword">public</span> <span class="meta">@interface</span> BindView &#123;</div><div class="line">    <span class="function"><span class="keyword">int</span> <span class="title">value</span><span class="params">()</span></span>;</div><div class="line">&#125;</div><div class="line"></div><div class="line"><span class="meta">@Target</span>(ElementType.METHOD)</div><div class="line"><span class="meta">@Retention</span>(RetentionPolicy.RUNTIME)</div><div class="line"><span class="keyword">public</span> <span class="meta">@interface</span> OnClick &#123;</div><div class="line">    <span class="keyword">int</span>[] value();</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>接下来就是<strong>ViewInjectUtils.bind(this);</strong>的具体实现了，整个注解的处理逻辑全在这个里面。</p>
<p>大致框架如下：<br><figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ViewInjectUtils</span> </span>&#123;</div><div class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">bind</span><span class="params">(Activity activity)</span> </span>&#123;</div><div class="line">        injectContentView(activity);</div><div class="line">        injectView(activity);</div><div class="line">        injectOnClice(activity);</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p>bind(Activity activity)方法里面很简单，就是依次处理一开始定义的三个注解：</p>
<h4 id="方法1：处理-ContentView"><a href="#方法1：处理-ContentView" class="headerlink" title="方法1：处理@ContentView"></a>方法1：处理@ContentView</h4><figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">injectContentView</span><span class="params">(Activity activity)</span> </span>&#123;</div><div class="line">        Class&lt;? extends Activity&gt; clazz = activity.getClass();</div><div class="line">        ContentView contentView = clazz.getAnnotation(ContentView.class);</div><div class="line">        <span class="keyword">if</span>(contentView != <span class="keyword">null</span>) &#123;</div><div class="line">            <span class="comment">//如果这个activity上存在@ContentView注解，就取出这个注解对应的value值</span></div><div class="line">            <span class="keyword">int</span> layoutId = contentView.value();</div><div class="line">            <span class="comment">//也就是布局文件的id。</span></div><div class="line">            <span class="keyword">try</span> &#123;</div><div class="line">                Method setViewMethod = clazz.getMethod(<span class="string">"setContentView"</span>,<span class="keyword">int</span>.class);</div><div class="line">                setViewMethod.invoke(activity,layoutId);</div><div class="line">            &#125; <span class="keyword">catch</span> (Exception e) &#123;</div><div class="line">                e.printStackTrace();</div><div class="line">            &#125;</div><div class="line">        &#125;</div><div class="line">    &#125;</div></pre></td></tr></table></figure>
<p>可以看到首先是拿到这个class的@ContentView注解，其value就是待注入的布局文件id，然后再通过反射调用setContentView方法，完成布局文件的注入。</p>
<h4 id="方法2：处理-BindView"><a href="#方法2：处理-BindView" class="headerlink" title="方法2：处理@BindView"></a>方法2：处理@BindView</h4><figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">injectView</span><span class="params">(Activity activity)</span> </span>&#123;</div><div class="line">    Class&lt;? extends Activity&gt; clazz = activity.getClass();</div><div class="line">    <span class="comment">// 获得activity的所有成员变量</span></div><div class="line">    Field[] fields = clazz.getDeclaredFields();</div><div class="line">    <span class="keyword">for</span>(Field field : fields) &#123;</div><div class="line">        <span class="comment">//获得每个成员变量上面的BindView注解</span></div><div class="line">        BindView bindView = field.getAnnotation(BindView.class);</div><div class="line">        <span class="keyword">if</span>(bindView != <span class="keyword">null</span>) &#123;</div><div class="line">            <span class="comment">// 得到相应的控件id</span></div><div class="line">            <span class="keyword">int</span> viewId = bindView.value();</div><div class="line">            View view = activity.findViewById(viewId);</div><div class="line">            <span class="keyword">try</span> &#123;</div><div class="line">                <span class="comment">// 允许访问私有字段</span></div><div class="line">                field.setAccessible(<span class="keyword">true</span>);</div><div class="line">                <span class="comment">// 将获得指定id的View设置给使用了@BindView的field。</span></div><div class="line">                field.set(activity,view);</div><div class="line">            &#125; <span class="keyword">catch</span> (IllegalAccessException e) &#123;</div><div class="line">                e.printStackTrace();</div><div class="line">            &#125;</div><div class="line">        &#125;</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>首先获得所有Field，然后遍历带有@Bindview注解的field，拿到@Bindview注解的value–即View的id，然后通过activity.findViewById(viewId);获得这个View，最后将它设置给field。</p>
<h4 id="方法3：处理-OnClick"><a href="#方法3：处理-OnClick" class="headerlink" title="方法3：处理@OnClick"></a>方法3：处理@OnClick</h4><p>处理@OnClick，我们需要用到动态代理的技术，</p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="comment">// 处理@OnClick注解</span></div><div class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">injectOnClick</span><span class="params">(<span class="keyword">final</span> Activity activity)</span> </span>&#123;</div><div class="line">        Class&lt;? extends Activity&gt; clazz = activity.getClass();</div><div class="line">        Method[] methods = clazz.getDeclaredMethods();</div><div class="line">        <span class="keyword">for</span>(<span class="keyword">final</span> Method tempMethod : methods) &#123;</div><div class="line">            <span class="comment">// 获得使用了@OnClick注解的方法</span></div><div class="line">            OnClick click = tempMethod.getAnnotation(OnClick.class);</div><div class="line">            <span class="keyword">if</span>(click != <span class="keyword">null</span>) &#123;</div><div class="line">                <span class="keyword">int</span>[] viewIds = click.value();</div><div class="line">                tempMethod.setAccessible(<span class="keyword">true</span>);</div><div class="line"></div><div class="line">                Object myListener = Proxy.newProxyInstance(View.OnClickListener.class.getClassLoader(),</div><div class="line">                        <span class="keyword">new</span> Class[]&#123;View.OnClickListener.class&#125;, <span class="keyword">new</span> InvocationHandler() &#123;</div><div class="line">                            <span class="meta">@Override</span></div><div class="line">                            <span class="function"><span class="keyword">public</span> Object <span class="title">invoke</span><span class="params">(Object proxy, Method method, Object[] args)</span> <span class="keyword">throws</span> Throwable </span>&#123;</div><div class="line">                                <span class="comment">// 调用带有onClick注解的method方法</span></div><div class="line">                                <span class="keyword">return</span> tempMethod.invoke(activity, args);</div><div class="line">                            &#125;</div><div class="line">                        &#125;);</div><div class="line"></div><div class="line">                <span class="keyword">try</span> &#123;</div><div class="line">                    <span class="keyword">for</span>(<span class="keyword">int</span> id : viewIds) &#123;</div><div class="line">                        View tempView = activity.findViewById(id);</div><div class="line">                        Method setOnClickerListener = tempView.getClass().getMethod(<span class="string">"setOnClickListener"</span>,</div><div class="line">                                View.OnClickListener.class);</div><div class="line">                        <span class="comment">// 通过动态代理调用自己设置的myListener</span></div><div class="line">                        setOnClickerListener.invoke(tempView, myListener);</div><div class="line">                    &#125;</div><div class="line">                &#125; <span class="keyword">catch</span> (Exception e) &#123;</div><div class="line">                    e.printStackTrace();</div><div class="line">                &#125;</div><div class="line">            &#125;</div><div class="line">        &#125;</div><div class="line">    &#125;</div></pre></td></tr></table></figure>
<p>当程序运行调用setOnClickListener的时候，会传入设置的代理对象myListener，在myListener中，invoke调用的是带有@OnClick注解的method方法。这样就实现了我们的目的。</p>
<p>&emsp;&emsp;到这里我们就通过反射机制模拟实现了ButterKnife的效果了。这种方式虽然简单，但是<strong>反射多多少少会有性能上的损耗。ButterKnife最新版本也不是通过反射来完成的，而是利用APT(Annotation Processing Tool)编译时解析技术</strong>，动态生成绑定事件或者View的java代码，然后在运行的时候，直接调用bind方法完成绑定。在下一篇文章中，会详细分析apt技术的关键点，并通过apt模拟实现ButterKnife的效果。</p>
<hr>
<h2 id="参考文献"><a href="#参考文献" class="headerlink" title="参考文献"></a>参考文献</h2><p><a href="http://stormzhang.com/openandroid/android/2014/01/12/android-butterknife/" target="_blank" rel="external">1.ButterKnife–View注入框架</a><br><a href="http://blog.csdn.net/ohhh1988/article/details/51330572" target="_blank" rel="external">2.android使用butterknife InjectView和BindView</a><br><a href="https://jakewharton.github.io/butterknife/" target="_blank" rel="external">3.ButtrerKnife官方文档链接</a><br><a href="http://blog.csdn.net/itjianghuxiaoxiong/article/details/50177549" target="_blank" rel="external">4.ButterKnife使用详解</a></p>
]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;1-概述&quot;&gt;&lt;a href=&quot;#1-概述&quot; class=&quot;headerlink&quot; title=&quot;1.概述&quot;&gt;&lt;/a&gt;1.概述&lt;/h2&gt;&lt;p&gt;&amp;emsp;&amp;emsp;ButterKnife是一个&lt;strong&gt;依托 Java 的注解机制来实现辅助代码生成的框架&lt;/s
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title>Java注解机制基础</title>
    <link href="http://haojen.github.io/2017/01/19/annotation-base-use/"/>
    <id>http://haojen.github.io/2017/01/19/annotation-base-use/</id>
    <published>2017-01-19T12:18:39.000Z</published>
    <updated>2017-01-19T12:23:07.158Z</updated>
    
    <content type="html"><![CDATA[<p>&emsp;&emsp;最近整体研究了一下ButterKnife这个框架，顺带着系统回顾梳理了一遍Java注解相关的知识点。本文大致整理了一下注解的几个核心部分，内容参考了很多现有的资料，在参考文献中做出了说明。希望通过本文能对注解的基本原理做一个简要的梳理，为后续两篇文章深入的分析ButterKnife实现原理做好铺垫。</p>
<hr>
<h2 id="1-注解概述"><a href="#1-注解概述" class="headerlink" title="1.注解概述"></a>1.注解概述</h2><h3 id="1-定义"><a href="#1-定义" class="headerlink" title="(1)定义"></a>(1)定义</h3><p>&emsp;&emsp;注解(Annotation)是Java1.5中引入的一个重大修改之一，<strong>为我们在代码中添加信息提供了一种形式化的方法，使我们可以在稍后某个时刻非常方便的使用这些数据</strong>。<br>&emsp;&emsp;注解在一定程度上是把元数据与源代码结合在一起，而不是保存在外部文档中。<br>&emsp;&emsp;<strong>注解的含义可以理解为java中的元数据。元数据是描述数据的数据。</strong>如下所示：</p>
<figure class="highlight"><table><tr><td class="code"><pre><div class="line">&lt;string name="app_version"&gt;5.4&lt;/string&gt;</div></pre></td></tr></table></figure>
<p>&emsp;&emsp;这里的”app_version”就是描述数据”5.4”的数据，这是在配置文件中写的，注解是在源码中写的。<strong>另外一种开发人员很熟悉的元数据是注释(comment)</strong>。注释用来描述源代码中的类，域和方法的作用等。</p>
<h3 id="2-注解与注释的不同"><a href="#2-注解与注释的不同" class="headerlink" title="(2)注解与注释的不同"></a>(2)注解与注释的不同</h3><blockquote>
<ul>
<li>注解会影响代码的行为，而注释不会；</li>
<li>编译器对代码进行处理时，注释会被直接删除，而<strong>注解可能会被保留在字节代码中</strong>；</li>
</ul>
</blockquote>
<h3 id="3-注解的作用"><a href="#3-注解的作用" class="headerlink" title="(3)注解的作用"></a>(3)注解的作用</h3><blockquote>
<ul>
<li>格式检查：告诉编译器信息，比如被@Override标记的方法如果不是父类的某个方法，IDE会报错；</li>
<li>减少配置：运行时动态处理，得到注解信息，实现代替配置文件的功能；</li>
<li>减少重复工作：<strong>减轻编写“样板”代码的负担</strong>，比如第三方框架ButterKnife，通过注解@BindView减少对findViewById的调用，类似的还有(Lombok,Dagger,AndroidAnnotation等)；</li>
</ul>
</blockquote>
<h3 id="4-注解是如何工作的"><a href="#4-注解是如何工作的" class="headerlink" title="(4)注解是如何工作的"></a>(4)注解是如何工作的</h3><p>&emsp;&emsp;注解仅仅是元数据，和业务逻辑无关，所以查看注解类时，发现里面没有任何逻辑处理：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="meta">@Retention</span>(RetentionPolicy.CLASS)</div><div class="line"><span class="meta">@Target</span>(&#123;ElementType.FIELD&#125;)</div><div class="line"><span class="keyword">public</span> <span class="meta">@interface</span> InjectView &#123;</div><div class="line">    <span class="function"><span class="keyword">int</span> <span class="title">value</span><span class="params">()</span></span>;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>&emsp;&emsp;如果注解不包含业务逻辑处理，必然有人来实现这些逻辑。<strong>注解的逻辑实现是元数据的用户来处理的，注解仅仅提供它定义的属性（类/方法/变量/参数/包）的信息，注解的用户来读取这些信息并实现必要的逻辑</strong>。当使用java中的注解时（比如@Override、@Deprecated、@SuppressWarnings）JVM就是用户，由编译器来负责处理。如果是程序中自定义的注解，就需要开发者或者第三方库自己去进行处理了。</p>
<h3 id="5-注解和配置文件的区别"><a href="#5-注解和配置文件的区别" class="headerlink" title="(5)注解和配置文件的区别"></a>(5)注解和配置文件的区别</h3><p>&emsp;&emsp;通过上面的描述可以发现，其实注解干的很多事情，通过配置文件也可以干，比如为类设置配置属性；但注解和配置文件是有很多区别的，<strong>在实际编程过程中，注解和配置文件配合使用在工作效率、低耦合、可拓展性方面才会达到权衡</strong>。</p>
<p><strong>配置文件：</strong><br>a.使用场合</p>
<blockquote>
<ul>
<li>外部依赖的配置，比如build.gradle中的依赖配置；</li>
<li>同一个项目团队内部达成一致的时候；</li>
<li><strong>非代码类的资源文件</strong>(比如图片，布局，数据，签名文件等)；</li>
</ul>
</blockquote>
<p>b.优点</p>
<blockquote>
<ul>
<li>降低耦合，配置集中，容易扩展，比如Android应用多语言支持；</li>
<li>对象之间的关系一目了然，比如strings.xml；</li>
<li>xml配置文件比注解功能齐全，支持的类型更多，比如drawable、style等；</li>
</ul>
</blockquote>
<p>c.缺点</p>
<blockquote>
<ul>
<li>繁琐；</li>
<li>类型不安全，比如R.java中的都是资源ID，用TextView的setText方法时传入int值时无法检测出该值是否为资源ID，但@StringRes可以；</li>
</ul>
</blockquote>
<p><strong>注解：</strong><br>a.使用场合</p>
<blockquote>
<ul>
<li>动态配置信息；</li>
<li><strong>代为实现程序逻辑</strong>（比如ButterKnife中的@BindView代替实现findViewById）；</li>
<li>代码格式检查，比如@Override、@Deprecated、@NonNull、@StringRes等，便于IDE能够检查出代码错误；</li>
</ul>
</blockquote>
<p>b.优点</p>
<blockquote>
<ul>
<li>在class文件中，提高程序的内聚性；</li>
<li><strong>减少重复工作，提升开发效率</strong>，比如findViewById；</li>
</ul>
</blockquote>
<p>c.缺点</p>
<blockquote>
<ul>
<li>如果对Annotation修改，<strong>需要重新编译整个工程</strong>；</li>
<li>业务类之间的关系不如xml配置那样一目了然；</li>
<li>程序中过多的Annotation，对于代码的简洁度有一定影响；</li>
<li>扩展性较差；</li>
</ul>
</blockquote>
<h3 id="6-注解类型"><a href="#6-注解类型" class="headerlink" title="(6)注解类型"></a>(6)注解类型</h3><p>&emsp;&emsp;很多资料对Java Annotation进行了分类，大部分是分为：Java内置、元注解、自定注解。这里因为考虑到Android库提供的注解，本文将其总结归纳为如下几种类型：</p>
<blockquote>
<ul>
<li>Java标准库中内置的注解(Built-in Java Annotations)</li>
<li>元注解(Meta-annotations)</li>
<li>Android Support Annotations</li>
<li>自定义注解</li>
</ul>
</blockquote>
<hr>
<h2 id="2-Java标准库中内置的注解"><a href="#2-Java标准库中内置的注解" class="headerlink" title="2.Java标准库中内置的注解"></a>2.Java标准库中内置的注解</h2><p>&emsp;&emsp;Java内置注解；定义在java.lang中，Java1.5中内置了三个：@Deprecated、@Overrride、@SuppressWarnings。java1.7中增加了@SafeVarargs，java1.8中增加了@FunctionsalInterface。详细介绍如下：</p>
<h3 id="1-java-lang-Override"><a href="#1-java-lang-Override" class="headerlink" title="(1)java.lang.Override"></a>(1)java.lang.Override</h3><p>&emsp;&emsp;用于表示一个方法声明覆写父类型中的对应方法。Override注解的作用是<strong>避免开发人员没有正确区分方法重载和覆写而带来的错误</strong>。当需要覆写一个方法的时候，可以在方法声明前面加上Override注解。如果这个方法实际上并没有覆写父类型中的方法，而是进行了重载，那么编译器将产生相应的错误信息。如下所示的代码：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="class"><span class="keyword">class</span> <span class="title">User</span> </span>&#123;</div><div class="line"></div><div class="line">	<span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">equals</span><span class="params">(User user)</span> </span>&#123;</div><div class="line">		<span class="keyword">return</span> <span class="keyword">true</span>;</div><div class="line">	&#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>&emsp;&emsp;<strong>User类的equals方法本意是覆写Object类中的equals方法，提供自己的对象比较方式的实现</strong>，而实际上User类中的equals方法并没有覆写Object类的equals方法，<strong>而是提供了一个重载的形式。这种错误在编程过程中很难发现</strong>。如果加上了Override注解，那么编译器会给出错误的信息，开发人员会意识到这个错误，并根据需要进行相应的修改。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="meta">@Target</span>(&#123;ElementType.METHOD&#125;)</div><div class="line"><span class="meta">@Retention</span>(RetentionPolicy.SOURCE)</div><div class="line"><span class="keyword">public</span> <span class="meta">@interface</span> Override &#123;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<h3 id="2-java-lang-Deprecated"><a href="#2-java-lang-Deprecated" class="headerlink" title="(2)java.lang.Deprecated"></a>(2)java.lang.Deprecated</h3><p>&emsp;&emsp;<strong>属于标记注解，不需要设置属性值</strong>；可以对构造方法、变量、方法、包、参数标记，告知用户和编译器被标记的内容已不建议被使用，如果被使用，编译器会报警告，但不会报错，程序也能正常运行：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="meta">@Documented</span></div><div class="line"><span class="meta">@Retention</span>(RetentionPolicy.RUNTIME)</div><div class="line"><span class="meta">@Target</span>(&#123;ElementType.CONSTRUCTOR, ElementType.FIELD, ElementType.LOCAL_VARIABLE, ElementType.METHOD, ElementType.PACKAGE, ElementType.PARAMETER, ElementType.TYPE&#125;)</div><div class="line"><span class="keyword">public</span> <span class="meta">@interface</span> Deprecated &#123;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<h3 id="3-java-lang-SuppressWarnings"><a href="#3-java-lang-SuppressWarnings" class="headerlink" title="(3)java.lang.SuppressWarnings"></a>(3)java.lang.SuppressWarnings</h3><p>&emsp;&emsp;可以对构造方法、变量、方法、包、参数标记，<strong>用于告知编译器忽略指定的警告</strong>，不用在编译完成后出现警告信息：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="meta">@Target</span>(&#123;ElementType.TYPE, ElementType.FIELD, ElementType.METHOD, ElementType.PARAMETER, ElementType.CONSTRUCTOR, ElementType.LOCAL_VARIABLE&#125;)</div><div class="line"><span class="meta">@Retention</span>(RetentionPolicy.SOURCE)</div><div class="line"><span class="keyword">public</span> <span class="meta">@interface</span> SuppressWarnings &#123;</div><div class="line">    String[] value();</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<h3 id="4-SafeVarargs-Java-SE-7"><a href="#4-SafeVarargs-Java-SE-7" class="headerlink" title="(4)@SafeVarargs(Java SE 7)"></a>(4)@SafeVarargs(Java SE 7)</h3><p>&emsp;&emsp;作用：Claims to the compiler that the annotation target does nothing potentially unsafe to its varargs argument.。<br>&emsp;&emsp;@SafeVarargs用于<strong>指明那些使用可变长度参数的方法和构造器是安全的</strong>。这些方法能被传入长度可变的参数。这些参数可以是泛型的。如果它们是泛型参数，使用@SafeVarargs注释就可以抑制警告信息。使用该注解的前提是，开发人员必须确保这个方法的实现中对泛型类型参数的处理不会引发类型安全问题。<br>使用示例：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="meta">@SafeVarargs</span></div><div class="line"><span class="keyword">public</span> <span class="keyword">static</span> &lt;T&gt; <span class="function"><span class="keyword">void</span> <span class="title">displayElements</span><span class="params">(T... array)</span> </span>&#123;</div><div class="line">    <span class="keyword">for</span> (T element : array) &#123;</div><div class="line">        System.out.println(element.getClass().getName() + <span class="string">":"</span> + element);</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>在这里，方法displayElements的形参是<strong>可变长度的泛型类型</strong>。</p>
<h3 id="5-FunctionalInterface-Java-SE-8"><a href="#5-FunctionalInterface-Java-SE-8" class="headerlink" title="(5)@FunctionalInterface(Java SE 8)"></a>(5)@FunctionalInterface(Java SE 8)</h3><p>&emsp;&emsp;作用：An informative annotation type used to indicate that an interface type declaration is intended to be a functional interface as defined by the Java Language Specification. <strong>帮助编译器检查函数式接口的合法性</strong>。</p>
<hr>
<h2 id="3-Java标准库中的元注解-Meta-annotations"><a href="#3-Java标准库中的元注解-Meta-annotations" class="headerlink" title="3.Java标准库中的元注解(Meta-annotations)"></a>3.Java标准库中的元注解(Meta-annotations)</h2><p>&emsp;&emsp;所谓<strong>元注解，就是负责注解其他注解的，主要是描述注解的一些属性，任何注解都离不开元注解</strong>，元注解的用户是JDK，JDK已经帮助我们实现了这些元注解的逻辑。常见的四个类型是@Documented、@Inherited、@Retention、@Target。在Java 8新增了@Repeatable。具体描述如下：</p>
<h3 id="1-Target："><a href="#1-Target：" class="headerlink" title="(1)@Target："></a>(1)@Target：</h3><p><strong>作用</strong>：用于描述注解的使用范围，即被描述的注解可以用在什么地方(Indicates the contexts in which an annotation type is applicable)；<br><strong>取值</strong>：</p>
<blockquote>
<ul>
<li>CONSTRUCTOR：构造器；</li>
<li>FIELD：实例；</li>
<li>LOCAL_VARIABLE：局部变量；</li>
<li>METHOD：方法；</li>
<li>PACKAGE：包；</li>
<li>PARAMETER：参数;</li>
<li>TYPE：类、接口(包括注解类型) 或enum声明；</li>
</ul>
</blockquote>
<p><strong>ElementType 常量在 Target 注解中至多只能出现一次</strong>，如下是非法的：@Target({ElementType.FIELD, ElementType.METHOD, ElementType.FIELD})</p>
<h3 id="2-Retention："><a href="#2-Retention：" class="headerlink" title="(2)@Retention："></a>(2)@Retention：</h3><p><strong>作用</strong>：表示<strong>需要在什么级别保存该注解信息，用于描述注解的生命周期</strong>，即被描述的注解在什么范围内有效(Indicates how long annotations with the annotated type are to be retained.)；<br><strong>取值</strong>：</p>
<blockquote>
<ul>
<li>RetentionPolicy.SOURCE：<br>The marked annotation is retained only in the source level and is ignored by the compiler.<br>该注解只保留到代码层，编译器将对其忽略。因此其不会出现在生成的class文件中；</li>
<li>RetentionPolicy.CLASS：<br>The marked annotation is retained by the compiler at compile time, but is ignored by the Java Virtual Machine (JVM).<br>该注解保留在编译器中，因此能出现在生成的class文件中，但是被JVM忽略，所以不能在运行时获取注解内容。</li>
<li>RetentionPolicy.RUNTIME：<br>The marked annotation is retained by the JVM so it can be used by the runtime environment.<br>该注解能保留在JVM中，可以在运行时通过反射的方法获取具体内容。</li>
</ul>
</blockquote>
<p>如果<strong>自定义注解时不进行指定，默认为RetentionPolicy.CLASS</strong>。</p>
<h3 id="3-Documented："><a href="#3-Documented：" class="headerlink" title="(3)@Documented："></a>(3)@Documented：</h3><p><strong>作用</strong>：指示该注解是否默认通过Javadoc或者类似的工具进行文档化，是一种语义元注解(Indicates that annotations with a type are to be documented by javadoc and similar tools by default)。<br><strong>取值</strong>：它属于标记注解，没有成员；</p>
<h3 id="4-Inherited："><a href="#4-Inherited：" class="headerlink" title="(4)@Inherited："></a>(4)@Inherited：</h3><p><strong>作用</strong>：<strong>指示注解类型被自动继承</strong>。如果在解析注解时发现了该字段，并且在该类中没有该类型的注解，则对其父类进行查询。举个例子，如果一个类中，没有A注解，但是其父类是有A注解的，并且A注解是被@Inherited注解的（不妨认为保留时态是Runtime），那么使用反射获取子类的A注解时，因为获取不到，所以会去其父类查询到A注解。使用了@Inherited注解的类，这个注解是可以被用于其子类。<br><strong>取值</strong>：它属于标记注解，没有成员；</p>
<h3 id="5-Repetable-Java-8中新增加的"><a href="#5-Repetable-Java-8中新增加的" class="headerlink" title="(5)@Repetable(Java 8中新增加的):"></a>(5)@Repetable(Java 8中新增加的):</h3><p><strong>作用</strong>：指示该注解是否可以多次使用(used to indicate that the annotation type whose declaration it (meta-)annotates is repeatable.)。<strong>在这个注解出现前，一个位置要想注两个相同的注解，是不可能的，编译会出错误</strong>。所以要想使一个注解可以被注入两次，需要声明一个高级注解，这个注解中的成员类型为需要多次注入的注解的注解数组。<br><strong>取值</strong>：它属于标记注解，没有成员；</p>
<p>使用示例：<br>在没有该注解以前：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="keyword">public</span> <span class="meta">@interface</span> Authority &#123;</div><div class="line">     <span class="function">String <span class="title">role</span><span class="params">()</span></span>;</div><div class="line">&#125;</div><div class="line"></div><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">RepeatAnnotationUseOldVersion</span> </span>&#123;</div><div class="line">    <span class="meta">@Authority</span>(role=<span class="string">"Admin"</span>)</div><div class="line">    <span class="meta">@Authority</span>(role=<span class="string">"Manager"</span>)</div><div class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">doSomeThing</span><span class="params">()</span></span>&#123;</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>类似于这样的使用编译器是会报错的。</p>
<p>通常的做法是：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="keyword">public</span> <span class="meta">@interface</span> Authority &#123;</div><div class="line">     <span class="function">String <span class="title">role</span><span class="params">()</span></span>;</div><div class="line">&#125;</div><div class="line"> </div><div class="line"><span class="keyword">public</span> <span class="meta">@interface</span> Authorities &#123;</div><div class="line">    Authority[] value();</div><div class="line">&#125;</div><div class="line"> </div><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">RepeatAnnotationUseOldVersion</span> </span>&#123;</div><div class="line">    <span class="meta">@Authorities</span>(&#123;<span class="meta">@Authority</span>(role=<span class="string">"Admin"</span>),<span class="meta">@Authority</span>(role=<span class="string">"Manager"</span>)&#125;)</div><div class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">doSomeThing</span><span class="params">()</span></span>&#123;</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>在java 8以后：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="meta">@Repeatable</span>(Authorities.class)</div><div class="line"><span class="keyword">public</span> <span class="meta">@interface</span> Authority &#123;</div><div class="line">     <span class="function">String <span class="title">role</span><span class="params">()</span></span>;</div><div class="line">&#125;</div><div class="line"> </div><div class="line"><span class="keyword">public</span> <span class="meta">@interface</span> Authorities &#123;</div><div class="line">    Authority[] value();</div><div class="line">&#125;</div><div class="line"></div><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">RepeatAnnotationUseNewVersion</span> </span>&#123;</div><div class="line">    <span class="meta">@Authority</span>(role=<span class="string">"Admin"</span>)</div><div class="line">    <span class="meta">@Authority</span>(role=<span class="string">"Manager"</span>)</div><div class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">doSomeThing</span><span class="params">()</span></span>&#123; &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>&emsp;&emsp;<strong>在注解Authority上告诉该注解，如果多次用Authority注解了某个方法，则自动把多次注解Authority作为Authorities注解的成员数组的一个值，当取注解时，可以直接取Authorities，即可取到两个Authority注解</strong>。要求：@Repeatable注解的值的注解类Authorities.class，成员变量一定是被注解的注解Authority的数组。<br>&emsp;&emsp;不同的地方是，创建重复注解Authority时，加上@Repeatable,指向存储注解Authorities，在使用时候，直接可以重复使用Authority注解。从上面例子看出，java 8里面做法更适合常规的思维，可读性强一点。其实从原理上看和第一种是一模一样的，只是增加了可读性。</p>
<p>&emsp;&emsp;其实在Java 8中，Annotation还得到了很多很好的扩展。更多Java 8对Annotation提供的改进和支持可参阅：<a href="http://www.ibm.com/developerworks/cn/java/j-lo-java8annotation/index.html" target="_blank" rel="external">Java 8 Annotation 新特性在软件质量和开发效率方面的提升</a></p>
<hr>
<h2 id="4-Android-Support-Annotations"><a href="#4-Android-Support-Annotations" class="headerlink" title="4.Android Support Annotations"></a>4.Android Support Annotations</h2><p>&emsp;&emsp;Android support library从19.1版本开始引入了一个新的<strong>注解库</strong>，它包含很多有用的元注解，开发者可以用他们来修饰代码，帮助发现bug。Support library自己本身也用到了这些注解，所以作为support library的用户，Android Studio已经基于这些注解校验了开发者的代码并且标注其中潜在的问题。<br>&emsp;&emsp;这些注解是作为一个support包提供给开发者使用，要使用他们，需要在build.gradle中添加对android support-annotations的依赖：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line">compile <span class="string">'com.android.support:support-annotations:22.2.0'</span></div></pre></td></tr></table></figure>
<p>提供的注释概览如下：</p>
<center><br><img src="http://occl9k36n.bkt.clouddn.com/2017_01_09_android_annotation_ovewview.png" alt="Android Annotation概览)"><br></center>

<p>下面分类说明如下：</p>
<h3 id="1-Nullness-Annotations："><a href="#1-Nullness-Annotations：" class="headerlink" title="(1)Nullness Annotations："></a>(1)Nullness Annotations：</h3><p>作用：check the nullness of a given variable, parameter, or return value.<br>@Nullable：用于标记方法参数或者返回值可以为空；<br>@NonNull：用于标记方法参数或者返回值不能为空，如果为空编译器会报警告</p>
<h3 id="2-Resource-Annotations："><a href="#2-Resource-Annotations：" class="headerlink" title="(2)Resource Annotations："></a>(2)Resource Annotations：</h3><p>&emsp;&emsp;主要用于<strong>标记方法的参数必须要是指定的资源类型，如果不是，IDE就会给出警告</strong>。因为资源文件都是静态的，所以在编写代码时IDE就知道传值是否错误，可以避免传的资源id错误导致运行时异常。如下所示：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">abstract</span> <span class="keyword">void</span> <span class="title">setTitle</span><span class="params">(@StringRes <span class="keyword">int</span> resId)</span> </span>&#123; … &#125;</div></pre></td></tr></table></figure>
<p>在代码检查阶段，如果传递的引用类型不是R.string类型，编辑器会给出警告。<br>资源类型注解包括：<br>@AnimatorRes、@AnimRes、@AnyRes、@ArrayRes、@BoolRes、@ColorRes、@DimenRes、@DrawableRes、@FractionRes、@IdRes、@IntgerRes、@InterpolatorRes、@LayoutRes、@MenuRes、@PluralsRes、@RawRes、@StringRes、@StyleableRes、@StyleRes、@TransitionRes、@XmlRes。<br>@AnyRes：表示可以是任何资源类型；</p>
<h3 id="3-Thread-Annotations："><a href="#3-Thread-Annotations：" class="headerlink" title="(3)Thread Annotations："></a>(3)Thread Annotations：</h3><p>作用：用于标记指定的方法、类（如果一个类中的所有方法都有相同的线程需求，就可以对这个类进行注解，比如View.java就被@UIThread所标记）<strong>只能在指定的线程类中被调用</strong>。<br>类型：</p>
<blockquote>
<ul>
<li>@MainThread</li>
<li>@UiThread</li>
<li>@WorkerThread</li>
<li>@BinderThread</li>
<li>@AnyThread</li>
</ul>
</blockquote>
<p>&emsp;&emsp;典型使用：A common use of the thread annotation is to validate method overrides in the AsyncTask class class performs background operations and publishes results only on the UI thread.</p>
<h3 id="4-Value-Constraint-Annotations-值约束注解-："><a href="#4-Value-Constraint-Annotations-值约束注解-：" class="headerlink" title="(4)Value Constraint Annotations(值约束注解)："></a>(4)Value Constraint Annotations(值约束注解)：</h3><p>&emsp;&emsp;用于标记参数必须是指定类型的值，并且值的范围必须在约束的范围内，包括<strong>@Size、@IntRange、@FloatRange</strong>。</p>
<h4 id="IntRange"><a href="#IntRange" class="headerlink" title="@IntRange"></a>@IntRange</h4><p>&emsp;&emsp;标识一个interger或者lang型的参数在一个指定的范围之内；</p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setAlpha</span><span class="params">(@IntRange(from=<span class="number">0</span>,to=<span class="number">255</span>)</span> <span class="keyword">int</span> alpha) </span>&#123; … &#125;</div></pre></td></tr></table></figure>
<h4 id="FloatRange"><a href="#FloatRange" class="headerlink" title="@FloatRange"></a>@FloatRange</h4><p>&emsp;&emsp;标识一个float或者double型的参数在一个指定的范围之内；</p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setAlpha</span><span class="params">(@FloatRange(from=<span class="number">0.0</span>, to=<span class="number">1.0</span>)</span> <span class="keyword">float</span> alpha) </span>&#123;...&#125;</div></pre></td></tr></table></figure>
<h4 id="Size"><a href="#Size" class="headerlink" title="@Size"></a>@Size</h4><p>&emsp;&emsp;对于数据、集合以及字符串，<strong>可以用@Size注解参数来限定集合的大小</strong>(当参数是字符串的时候，可以限定字符串的长度)。</p>
<blockquote>
<ul>
<li>集合不能为空: @Size(min=1)；</li>
<li>字符串最大只能有23个字符: @Size(max=23)；</li>
<li>数组只能有2个元素: @Size(2)；</li>
</ul>
</blockquote>
<h3 id="5-Permission-Annotations-权限注解-："><a href="#5-Permission-Annotations-权限注解-：" class="headerlink" title="(5)Permission Annotations(权限注解)："></a>(5)Permission Annotations(权限注解)：</h3><p>如果方法需要调用者有特定的权限，可以使用@RequiresPermission注解。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="meta">@RequiresPermission</span>(Manifest.permission.SET_WALLPAPER)</div><div class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">abstract</span> <span class="keyword">void</span> <span class="title">setWallpaper</span><span class="params">(Bitmap bitmap)</span> <span class="keyword">throws</span> IOException</span>;</div></pre></td></tr></table></figure>
<p>如果至少需要权限集合中的一个，可以使用anyOf属性：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="meta">@RequiresPermission</span>(anyOf = &#123;</div><div class="line">    Manifest.permission.ACCESS_COARSE_LOCATION,</div><div class="line">    Manifest.permission.ACCESS_FINE_LOCATION&#125;)</div><div class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">abstract</span> Location <span class="title">getLastKnownLocation</span><span class="params">(String provider)</span></span>;</div></pre></td></tr></table></figure>
<p>如果你同时需要多个权限，可以用allOf属性：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="meta">@RequiresPermission</span>(allOf = &#123;</div><div class="line">    Manifest.permission.READ_HISTORY_BOOKMARKS, </div><div class="line">    Manifest.permission.WRITE_HISTORY_BOOKMARKS&#125;)</div><div class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">void</span> <span class="title">updateVisitedHistory</span><span class="params">(ContentResolver cr, String url, <span class="keyword">boolean</span> real)</span></span></div></pre></td></tr></table></figure>
<h3 id="6-CallSuper-Annotations-复写方法注解-："><a href="#6-CallSuper-Annotations-复写方法注解-：" class="headerlink" title="(6)CallSuper Annotations(复写方法注解)："></a>(6)CallSuper Annotations(复写方法注解)：</h3><p>&emsp;&emsp;如果你的API允许使用者重写你的方法，但你又需要你自己的方法(父方法)在重写的时候也被调用，这时候你可以使用@CallSuper标注：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="meta">@CallSuper</span></div><div class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">onCreate</span><span class="params">(Bundle savedInstanceState)</span> </span>&#123;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>用了这个后，当重写的方法没有调用父方法时，工具就会给予警告提示。</p>
<p><a href="https://developer.android.com/studio/write/annotations.html#call-super" target="_blank" rel="external">更多Annotation详细内容可以参见官方文档</a></p>
<hr>
<h2 id="5-自定义注解"><a href="#5-自定义注解" class="headerlink" title="5.自定义注解"></a>5.自定义注解</h2><h3 id="1-基本格式："><a href="#1-基本格式：" class="headerlink" title="(1)基本格式："></a>(1)基本格式：</h3><figure class="highlight java"><table><tr><td class="code"><pre><div class="line">元注解</div><div class="line"><span class="keyword">public</span> <span class="meta">@interface</span> 注解名&#123;</div><div class="line">    定义体；</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<h3 id="2-相关说明"><a href="#2-相关说明" class="headerlink" title="(2)相关说明"></a>(2)相关说明</h3><p>&emsp;&emsp;<strong>注解类型是一种特殊的接口，在声明时使用的是“@interface”而不是“interface”</strong>。<br>&emsp;&emsp;一个注解类型中可以包含多个元素。每个元素都可以看做注解中的配置项，通过方法声明的形式来定义。这些方法声明中不能有任何形式参数或类型参数，也不能有抛出受检异常的throws声明。<strong>方法的名称是元素的名称，方法的返回值类型决定了元素的类型</strong>。可以支持的返回值类型如下：</p>
<blockquote>
<ul>
<li>(1)所有基本数据类型（int,float,boolean,byte,double,char,long,short)；</li>
<li>(2)String类型；</li>
<li>(3)Class类型；</li>
<li>(4)enum类型；</li>
<li>(5)Annotation类型；</li>
<li>(6)以上所有类型的数组。</li>
</ul>
</blockquote>
<p>注意点：</p>
<blockquote>
<ul>
<li>(1)注解类中的方法只能用public或者默认这两个访问权修饰，不写public就是默认；</li>
<li>(2)<strong>如果注解类中只有一个成员，最好把方法名设置为”value”；</strong></li>
<li>(3)注解元素必须有确定的值，要么在定义注解的默认值中指定，要么在使用注解时指定，非基本类型的注解元素的值不可为null。因此,使用空字符串或0作为默认值是一种常用的做法。；</li>
</ul>
</blockquote>
<p>&emsp;&emsp;<strong>注解类型中可以没有任何元素，这种注解类型称为标记注解类型，是作标记用的</strong>。如果注解类型中只有一个元素，那么这个元素的名称应该根据惯例使用value。使用value的好处是可以简化注解的使用方式，<strong>可以为注解类型设定默认值。通过在方法声明的default关键词来指定。</strong></p>
<h3 id="3-注解的解析"><a href="#3-注解的解析" class="headerlink" title="(3)注解的解析"></a>(3)注解的解析</h3><p>&emsp;&emsp;当注解被添加到java源代码中后，并不会自动产生作用。某些注解甚至不会在字节代码中出现。<strong>创建和使用注解只是完成了第一步，重要的是如何对注解进行相应的处理。在一般情况下，创建和处理注解是Java标准库和第三方库应该做的事情，开发人员只需要使用注解就可以</strong>。<br>&emsp;&emsp;根据生存周期不同，可以分为两种解析方式：<strong>运行时解析和编译时解析</strong>。后续会对这两种解析技术进行详细的分析说明，并分别基于运行时解析和编译时解析简单的模拟实现ButterKnife的效果。</p>
<h2 id="参考文献"><a href="#参考文献" class="headerlink" title="参考文献"></a>参考文献</h2><p><a href="http://fyales.com/2015/08/21/Annotation/" target="_blank" rel="external">1.Java注解</a><br><a href="https://zhuanlan.zhihu.com/p/21410338" target="_blank" rel="external">2.深入浅出Java注解</a><br><a href="https://developer.android.com/studio/write/annotations.html#call-super" target="_blank" rel="external">3.Improve Code Inspection with Annotations官方文档</a></p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;&amp;emsp;&amp;emsp;最近整体研究了一下ButterKnife这个框架，顺带着系统回顾梳理了一遍Java注解相关的知识点。本文大致整理了一下注解的几个核心部分，内容参考了很多现有的资料，在参考文献中做出了说明。希望通过本文能对注解的基本原理做一个简要的梳理，为后续两篇文章
    
    </summary>
    
    
      <category term="Annotation" scheme="http://haojen.github.io/tags/Annotation/"/>
    
  </entry>
  
  <entry>
    <title>单例模式各版本的原理与实践</title>
    <link href="http://haojen.github.io/2016/12/31/SingleDesginPattern/"/>
    <id>http://haojen.github.io/2016/12/31/SingleDesginPattern/</id>
    <published>2016-12-31T09:16:41.000Z</published>
    <updated>2017-01-03T06:00:52.036Z</updated>
    
    <content type="html"><![CDATA[<h2 id="1-单例模式概述"><a href="#1-单例模式概述" class="headerlink" title="1.单例模式概述"></a>1.单例模式概述</h2><h3 id="1-引言"><a href="#1-引言" class="headerlink" title="(1)引言"></a>(1)引言</h3><p>&emsp;&emsp;单例模式是应用最广的模式之一，也是23种设计模式中最基本的一个。本文旨在总结通过Java实现单例模式的各个版本的优缺点及适用场景，详细分析如何实现线程安全的单例模式，并探讨单例模式的一些扩展。</p>
<h3 id="2-单例模式的定义"><a href="#2-单例模式的定义" class="headerlink" title="(2)单例模式的定义"></a>(2)单例模式的定义</h3><p><strong>&emsp;&emsp;Ensure a class has only one instance,and provide a global point of access to it.(确保某一个类只有一个实例，并且自行实例化并向整个系统提供这个实例)</strong><br>通用类图为：</p>
<center><br><img src="http://occl9k36n.bkt.clouddn.com/class_diagram_singleton_pattern_002.png" alt="类图源文件)"><br></center>

<p>&emsp;&emsp;Singleton类称为单例类，通过使用private的构造函数确保了在一个应用中只产生一个实例，并且是自行实例化的(在Singleton中自己使用new Singleton())。</p>
<h3 id="3-使用场景"><a href="#3-使用场景" class="headerlink" title="(3)使用场景"></a>(3)使用场景</h3><p>&emsp;&emsp;在一个系统中，要求一个类有且仅有一个对象，如果出现多个对象就会出现“不良反应”，可以采用单例模式，具体的场景如下：</p>
<blockquote>
<ul>
<li>要求生成唯一序列号的环境；</li>
<li>在整个项目中需要一个共享访问点或共享数据，例如一个Web页面上的计数器，可以不用把每次刷新都记录到数据库中，使用单例模式保持计数器的值，并确保是线程安全的；</li>
<li>创建一个对象需要消耗的资源过多，如要访问IO和数据库等资源；</li>
<li>需要定义大量的静态常量和静态方法(如工具类)的环境，可以采用单例模式(当然，也可以直接声明为static的方式)。</li>
</ul>
</blockquote>
<h3 id="4-优缺点"><a href="#4-优缺点" class="headerlink" title="(4)优缺点"></a>(4)优缺点</h3><p>单例模式的优点</p>
<blockquote>
<ul>
<li>由于单例模式<strong>在内存中只有一个实例，减少了内存开支</strong>，特别是一个对象需要频繁地创建销毁时，而且创建或销毁时性能又无法优化，单例模式的优势就非常明显；</li>
<li>由于单例模式只生成一个实例，所以减少了系统的性能开销，当一个对象的产生需要比较多的资源的时候，如读取配置，产生其他的依赖对象时，可以通过在应用启动的时候直接产生一个单例对象，然后用永久驻留内存的方式来解决；</li>
<li>单例模式可以避免对资源的多重占用，例如对一个写文件动作，由于只有一个实例存在内存中，避免对同一个资源文件的同时写操作；</li>
<li>单例模式可以在系统设置全局的访问点，优化和共享资源访问，例如可以设计一个单例类，负责所有数据表的映射处理</li>
</ul>
</blockquote>
<p>单例模式的缺点</p>
<blockquote>
<ul>
<li>单例模式一般没有接口，扩展困难，若要扩展，除了修改代码基本上没有第二种途径可以实现；</li>
<li><strong>单例模式与单一职责原则有冲突，一个类应该只实现一个逻辑，而不关心他是否是单例的</strong>，是不是要单例取决于环境，单例模式把“要单例”和业务逻辑融合在一个类中。</li>
</ul>
</blockquote>
<hr>
<h2 id="2-最基本的实现方式"><a href="#2-最基本的实现方式" class="headerlink" title="2.最基本的实现方式"></a>2.最基本的实现方式</h2><p>代码实现为：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Singleton</span> </span>&#123;</div><div class="line">	<span class="keyword">private</span> <span class="keyword">static</span> Singleton singleton;</div><div class="line">	<span class="comment">// 限制产生多个对象</span></div><div class="line">	<span class="function"><span class="keyword">private</span> <span class="title">Singleton</span><span class="params">()</span> </span>&#123;</div><div class="line">	&#125;</div><div class="line">	</div><div class="line">	<span class="comment">// 获得对象实例的方法</span></div><div class="line">	<span class="function"><span class="keyword">public</span> <span class="keyword">static</span> Singleton <span class="title">getSingleton</span><span class="params">()</span> </span>&#123;</div><div class="line">		<span class="keyword">if</span>(singleton == <span class="keyword">null</span>) &#123;</div><div class="line">			singleton = <span class="keyword">new</span> Singleton();</div><div class="line">		&#125;</div><div class="line">		<span class="keyword">return</span> singleton;</div><div class="line">	&#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>&emsp;&emsp;相信大多数同学在入门Java的阶段都见过这段代码。该方式在低并发的情况下尚不会出现问题，若系统压力增大，并发量增加时则可能在内存中出现多个实例，破坏设计的初衷。本文的后续就是围绕这种实现分析改进，探讨实现线程安全的单例模式的最佳实践。<br>&emsp;&emsp;为什么这种实现是线程不安全的呢？如一个线程A执行到<strong>singleton = new Singleton();</strong>这里，<strong>但还没有获得对象(对象初始化是需要时间的)</strong>，第二个线程B也在执行，执行到<strong>if(singleton == null)</strong>判断，那么线程B获得判断条件也是为真，于是继续运行下去，线程A获得了一个对象，线程B也获得了一个对象，在内存中就出现两个对象,造成单例模式的失效！！<br>&emsp;&emsp;所以根本原因在于可能存在多个线程并发的访问getSingleton()方法造成单例对象的多次创建，解决因多线程并发访问导致单例模式实效的最佳方法就是–不要使用多线程并发访问。(⊙o⊙)…</p>
<center><br><img src="http://occl9k36n.bkt.clouddn.com/expression003.jpg" alt="表情内容)"><br></center>

<hr>
<h2 id="3-饿汉式"><a href="#3-饿汉式" class="headerlink" title="3.饿汉式"></a>3.饿汉式</h2><h3 id="1-实现原理"><a href="#1-实现原理" class="headerlink" title="(1)实现原理"></a>(1)实现原理</h3><p>&emsp;&emsp;言归正传，上面说的问题其实就是对<strong>if(singleton == null)</strong>的判断失效造成<strong>singleton = new Singleton();</strong>可能会被多个线程并发的执行。饿汉式单例模式的实现的本质其实就是依赖类加载机制保证构造方法只会被执行一次。JVM在类的初始化阶段(即在Class被加载后，且被线程使用之前)，会执行类的初始化。<strong>在执行类的初始化期间，JVM会去获取一个锁。这个锁可以同步多个线程对同一个类的初始化</strong>。<br>饿汉式单例的实现代码为：<br><figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Singleton</span> </span>&#123;</div><div class="line">	<span class="keyword">private</span> <span class="keyword">static</span> Singleton singleton = <span class="keyword">new</span> Singleton();</div><div class="line">	</div><div class="line">	<span class="function"><span class="keyword">private</span> <span class="title">Singleton</span><span class="params">()</span> </span>&#123;</div><div class="line">	&#125;</div><div class="line">	</div><div class="line">	<span class="comment">// 获得对象实例的方法</span></div><div class="line">	<span class="function"><span class="keyword">public</span> <span class="keyword">static</span> Singleton <span class="title">getSingleton</span><span class="params">()</span> </span>&#123;</div><div class="line">		<span class="keyword">return</span> singleton;</div><div class="line">	&#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<h3 id="2-优缺点及适用场景"><a href="#2-优缺点及适用场景" class="headerlink" title="(2)优缺点及适用场景"></a>(2)优缺点及适用场景</h3><p>&emsp;&emsp;可以看到饿汉式的实现非常简单，<strong>适合那些在初始化时就要用到单例的情况</strong>，如果单例对象初始化非常快，而且占用内存非常小的时候这种方式是比较合适的，可以直接在应用启动时加载并初始化。<br>不适用的场景：</p>
<blockquote>
<ul>
<li>单例初始化的操作耗时比较长而应用对于启动速度又有要求;</li>
<li>单例的占用内存比较大;</li>
<li>单例只是在某个特定场景的情况下才会被使用，而一般情况下是不会使用的;</li>
</ul>
</blockquote>
<p>&emsp;&emsp;在上述的几种情况下使用饿汉式的单例模式是不合适的，这时候就需要<strong>用到懒汉式的方式去按需延迟加载单例</strong>。</p>
<hr>
<h2 id="4-利用同步锁机制实现的懒汉式"><a href="#4-利用同步锁机制实现的懒汉式" class="headerlink" title="4.利用同步锁机制实现的懒汉式"></a>4.利用同步锁机制实现的懒汉式</h2><p>实现代码为：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Singleton</span> </span>&#123;</div><div class="line">	<span class="keyword">private</span> <span class="keyword">static</span> Singleton singleton = <span class="keyword">null</span>;</div><div class="line">	</div><div class="line">	<span class="function"><span class="keyword">private</span> <span class="title">Singleton</span><span class="params">()</span> </span>&#123;</div><div class="line">	&#125;</div><div class="line">	</div><div class="line">	<span class="comment">// 获得对象实例的方法</span></div><div class="line">	<span class="function"><span class="keyword">public</span> <span class="keyword">static</span> Singleton <span class="title">getSingleton</span><span class="params">()</span> </span>&#123;</div><div class="line">		<span class="keyword">synchronized</span>(Singleton.class) &#123;</div><div class="line">			<span class="keyword">if</span>(singleton == <span class="keyword">null</span>) &#123;</div><div class="line">				singleton = <span class="keyword">new</span> Singleton();</div><div class="line">			&#125;</div><div class="line">		&#125;</div><div class="line">		<span class="keyword">return</span> singleton;</div><div class="line">	&#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>&emsp;&emsp;这种是最常见的懒汉式单例实现，<strong>使用同步锁synchronized(Singleton.class)防止多线程同时进入造成instance被多次实例化</strong>。但是他的缺陷也是非常明显的，就是每次在调用getSingleton()获取单例的实例的时候，都需要进行同步。<strong>事实上我们只想保证一次初始化成功，其余的快速返回而已，如果在getInstance频繁使用的地方就要考虑重新优化了。</strong></p>
<hr>
<h2 id="5-对同步锁机制实现懒汉式的改进–DCL"><a href="#5-对同步锁机制实现懒汉式的改进–DCL" class="headerlink" title="5.对同步锁机制实现懒汉式的改进–DCL"></a>5.对同步锁机制实现懒汉式的改进–DCL</h2><h3 id="1-原理与实现"><a href="#1-原理与实现" class="headerlink" title="(1)原理与实现"></a>(1)原理与实现</h3><p>&emsp;&emsp;由于synchronized（甚至是无竞争的synchronized）存在着巨大的性能开销。因此，人们想出了一个<strong>“聪明”的技巧：双重检查锁定（double-checked locking）。</strong>通过这种方式来降低同步的开销。下面是使用双重检查锁定来实现延迟初始化的代码实现：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Singleton</span> </span>&#123;</div><div class="line">	<span class="keyword">private</span> <span class="keyword">static</span> Singleton instance = <span class="keyword">null</span>;                 <span class="comment">//1</span></div><div class="line">	</div><div class="line">	<span class="comment">// 获得对象实例的方法</span></div><div class="line">	<span class="function"><span class="keyword">public</span> <span class="keyword">static</span> Singleton <span class="title">getSingleton</span><span class="params">()</span> </span>&#123;                  <span class="comment">//2</span></div><div class="line">		<span class="keyword">if</span>(instance == <span class="keyword">null</span>) &#123;                                <span class="comment">//3:第一次检查</span></div><div class="line">			<span class="keyword">synchronized</span>(Singleton.class) &#123;                   <span class="comment">//4:加锁</span></div><div class="line">				<span class="keyword">if</span>(instance == <span class="keyword">null</span>)                          <span class="comment">//5:第二次检查</span></div><div class="line">					instance = <span class="keyword">new</span> Singleton();               <span class="comment">//6:问题的根源产生</span></div><div class="line">			&#125;</div><div class="line">		&#125;</div><div class="line">		<span class="keyword">return</span> instance;</div><div class="line">	&#125;</div><div class="line">		</div><div class="line">	<span class="function"><span class="keyword">private</span> <span class="title">Singleton</span><span class="params">()</span> </span>&#123;</div><div class="line">	&#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>如上面的代码所示，它的“优点”如下：</p>
<blockquote>
<ul>
<li>在多个线程试图在同一时间创建对象时，会通过加锁来保证只有一个线程能创建对象；</li>
<li>在对象创建好了以后，执行getSingleton()方法将不需要获取锁，直接返回已经创建好的对象</li>
</ul>
</blockquote>
<p>&emsp;&emsp;<strong>双重检查模式看上去好像很完美，但这是一个错误的优化，在线程执行到上面所示的代码3处读取到singleton对象不为null时，singleton引用的对象可能还没有完成初始化</strong>。</p>
<h3 id="2-问题分析"><a href="#2-问题分析" class="headerlink" title="(2)问题分析"></a>(2)问题分析</h3><p>可能产生错误的场景</p>
<blockquote>
<ul>
<li>1.线程A进入getSingleton()方法;</li>
<li>2.因为此时instance为null，所以线程A进入synchronized块；</li>
<li>3.<strong>线程A执行 instance = new Singleton();  把实例变量instance设置成了非空。(注意，是在调用构造方法之前)</strong></li>
<li>4.线程A退出，线程B进入。</li>
<li>5.线程B检查instance是否为空，此时不为空（第三步的时候被线程A设置成了非空）。线程B返回instance的引用。(<strong>问题出现了，这时instance的引用并不是Singleton的实例，因为没有调用构造方法</strong>) </li>
<li>6.线程B退出，线程A进入；</li>
<li>7.线程A继续调用构造方法，完成instance的初始化，再返回。</li>
</ul>
</blockquote>
<h3 id="3-问题根源"><a href="#3-问题根源" class="headerlink" title="(3)问题根源"></a>(3)问题根源</h3><p>&emsp;&emsp;<strong>多线程问题，很大程度是由于非原子性造成的</strong>，如果我们每一个可能产生竞争的地方都是原子性的，那多线程需要考虑的东西就要少很多了。在上述程序中也是一样，我们看第六行代码：<br><figure class="highlight java"><table><tr><td class="code"><pre><div class="line">instance = <span class="keyword">new</span> Singleton();     <span class="comment">//6:问题的根源产生</span></div></pre></td></tr></table></figure></p>
<p>在JMM中，这行代码可以分解为3个过程：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line">memory = allocate();            <span class="comment">//#1为对象分配内存空间</span></div><div class="line">init(memory);                   <span class="comment">//#2初始化</span></div><div class="line">instance = memory;              <span class="comment">//#3设置instance,将其指向刚分配的内存空间。</span></div></pre></td></tr></table></figure>
<p>上面的3行代码，<strong>如果是顺序执行，不会带来问题。但是，在某些JIT编译器上，#2和#3可能发生重排序</strong>。也就是说，重排序后，上面三个过程变成了：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line"></div><div class="line">memory = allocate();            <span class="comment">//#1为对象分配内存空间</span></div><div class="line">instance = memory;              <span class="comment">//#2</span></div><div class="line">init(memory);                   <span class="comment">//#3初始化</span></div></pre></td></tr></table></figure>
<p>&emsp;&emsp;根据《The Java Language Specification, Java SE 7 Edition》一书中的内容：所有线程在执行java程序时必须要遵守intra-thread semantics。<strong>intra-thread semantics保证重排序不会改变单线程内的程序执行结果。换句话来说，intra-thread semantics允许那些在单线程内，不会改变单线程程序执行结果的重排序</strong>。上面三行伪代码的2和3之间虽然被重排序了，但这个重排序并不会违反intra-thread semantics。这个重排序在没有改变单线程程序的执行结果的前提下，可以提高程序的执行性能。<br>&emsp;&emsp;下面，再让我们看看多线程并发执行的时候的情况。请看下面的示意图：</p>
<center><br><img src="http://occl9k36n.bkt.clouddn.com/singleton_pattern_question_001.png" alt="表情内容)"><br></center>

<p>&emsp;&emsp;<strong>这里2和3虽然重排序了，但java内存模型的intra-thread semantics将确保2一定会排在4前面执行。因此线程A的intra-thread semantics没有改变。但2和3的重排序，将导致线程B在B1处判断出instance不为空，线程B接下来将访问instance引用的对象。此时，线程B将会访问到一个还未初始化的对象。</strong>。</p>
<p>&emsp;&emsp;分析清楚问题发生的根源之后，可以想出两个办法来实现线程安全的延迟初始化：</p>
<blockquote>
<ul>
<li>不允许2和3重排序；</li>
<li>允许2和3重排序，但不允许其他线程“看到”这个重排序。</li>
</ul>
</blockquote>
<p>&emsp;&emsp;后文介绍的解决方案就分别对应于上面这两点。</p>
<hr>
<h2 id="6-Java1-5以后安全的DCL版本"><a href="#6-Java1-5以后安全的DCL版本" class="headerlink" title="6.Java1.5以后安全的DCL版本"></a>6.Java1.5以后安全的DCL版本</h2><h3 id="1-实现代码"><a href="#1-实现代码" class="headerlink" title="(1)实现代码"></a>(1)实现代码</h3><figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Singleton</span> </span>&#123;</div><div class="line">	<span class="keyword">private</span> <span class="keyword">volatile</span> <span class="keyword">static</span> Singleton instance = <span class="keyword">null</span>;</div><div class="line">	</div><div class="line">	<span class="comment">// 获得对象实例的方法</span></div><div class="line">	<span class="function"><span class="keyword">public</span> <span class="keyword">static</span> Singleton <span class="title">getSingleton</span><span class="params">()</span> </span>&#123;</div><div class="line">		<span class="keyword">if</span>(instance == <span class="keyword">null</span>) &#123;</div><div class="line">			<span class="keyword">synchronized</span>(Singleton.class) &#123;</div><div class="line">				<span class="keyword">if</span>(instance == <span class="keyword">null</span>)</div><div class="line">					instance = <span class="keyword">new</span> Singleton();</div><div class="line">			&#125;</div><div class="line">		&#125;</div><div class="line">		<span class="keyword">return</span> instance;</div><div class="line">	&#125;</div><div class="line">		</div><div class="line">	<span class="function"><span class="keyword">private</span> <span class="title">Singleton</span><span class="params">()</span> </span>&#123;</div><div class="line">	&#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<h3 id="2-原理分析"><a href="#2-原理分析" class="headerlink" title="(2)原理分析"></a>(2)原理分析</h3><p>&emsp;&emsp;<strong>可以发现代码只做一点小的修改（把instance声明为volatile型），</strong>为什么volatile可以解决呢？回顾一下他的两层语义：</p>
<blockquote>
<ul>
<li>(1)可见性：指的是在一个线程中对该变量的修改会马上由工作内存（Work Memory）写回主内存（Main Memory）</li>
<li>(2)禁止指令重排序优化</li>
</ul>
</blockquote>
<p>&emsp;&emsp;当声明对象的引用为volatile后，“问题的根源”的三行伪代码中的2和3之间的重排序，在多线程环境中将会被禁止,从而在根本上解决了问题。但是很不幸，<strong>禁止指令重排优化这条语义直到jdk1.5以后才能正确工作。此前的JDK中即使将变量声明为volatile也无法完全避免重排序所导致的问题</strong>。所以，在jdk1.5版本前，双重检查锁形式的单例模式是无法保证线程安全的。<br>&emsp;&emsp;写到这里，可能有的同学会有疑问了：</p>
<center><br><img src="http://occl9k36n.bkt.clouddn.com/expression006.gif" alt="表情内容)"><br></center>

<hr>
<h2 id="7-Java1-4以前安全的DCL版本"><a href="#7-Java1-4以前安全的DCL版本" class="headerlink" title="7.Java1.4以前安全的DCL版本"></a>7.Java1.4以前安全的DCL版本</h2><p>&emsp;&emsp;额(⊙o⊙)…虽然现在的日常开发已经普遍在使用Java1.7甚至1.8了。不过尝试着探讨下在Jav1.4以前实现安全的DCL还是一个还有意思的话题。我自己也没有找到太确定的答案，这方面的资料也非常的少。下面给出的实现代码不一定能保证正确，贴出来仅供参考，欢迎有兴趣的同学在评论区留言分享一下经验。</p>
<center><br><img src="http://occl9k36n.bkt.clouddn.com/expression004.jpg" alt="表情内容)"><br></center>


<h3 id="2-实现代码及思路"><a href="#2-实现代码及思路" class="headerlink" title="(2)实现代码及思路"></a>(2)实现代码及思路</h3><figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Singleton</span> </span>&#123;</div><div class="line">    <span class="keyword">private</span> <span class="keyword">static</span> Singleton instance = <span class="keyword">null</span>;</div><div class="line">    <span class="comment">// 获得对象实例的方法</span></div><div class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> Singleton <span class="title">getSingleton</span><span class="params">()</span> </span>&#123;</div><div class="line">        <span class="keyword">if</span>(instance == <span class="keyword">null</span>) &#123;                                      <span class="comment">//1.第一次检查</span></div><div class="line">            <span class="keyword">synchronized</span>(Singleton.class) &#123;                         <span class="comment">//2.第一个synchronized块</span></div><div class="line">            	Singleton temp = instance;                          <span class="comment">//3.给临时变量temp赋值</span></div><div class="line">            	<span class="keyword">if</span>(temp == <span class="keyword">null</span>) &#123;                                  <span class="comment">//4.第二次检查</span></div><div class="line">            		<span class="keyword">synchronized</span>(Singleton.class) &#123;                 <span class="comment">//5.第二个synchronized块</span></div><div class="line">            			temp = <span class="keyword">new</span> Singleton();                     <span class="comment">//6.解决问题的关键地方</span></div><div class="line">            		&#125;</div><div class="line">            		instance = temp;                                <span class="comment">//7.把temp的引用赋值给instance</span></div><div class="line">            	&#125;</div><div class="line">            &#125;</div><div class="line">        &#125;</div><div class="line">        <span class="keyword">return</span> instance;</div><div class="line">    &#125;</div><div class="line">    </div><div class="line">    <span class="function"><span class="keyword">private</span> <span class="title">Singleton</span><span class="params">()</span> </span>&#123;</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>&emsp;&emsp;上面给出的代码中，很关键的地方在于在synchronized块中引入了一个临时变量Singleton temp，通过对temp的判空及相应的初始化，保证在代码7处，执行intance = temp;时，<strong>instance不为null且完成了初始化</strong>。</p>
<hr>
<h2 id="8-内部类方式"><a href="#8-内部类方式" class="headerlink" title="8.内部类方式"></a>8.内部类方式</h2><h3 id="1-实现"><a href="#1-实现" class="headerlink" title="(1)实现"></a>(1)实现</h3><p>&emsp;&emsp;在第五部分的结尾我们提到了两个办法来实现线程安全的延迟初始化，内部类方式正是基于第二种方法–<strong>线程之间重排序透明性</strong><br>实现代码为：<br><figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Singleton</span> </span>&#123;</div><div class="line">	<span class="comment">// 获得对象实例的方法</span></div><div class="line">	<span class="function"><span class="keyword">public</span> <span class="keyword">static</span> Singleton <span class="title">getSingleton</span><span class="params">()</span> </span>&#123;</div><div class="line">		<span class="keyword">return</span> SingletonHolder.instance;</div><div class="line">	&#125;</div><div class="line">	</div><div class="line">	<span class="comment">/**</span></div><div class="line">     * 静态内部类与外部类的实例没有绑定关系，而且只有被调用时才会</div><div class="line">     * 加载，从而实现了延迟加载</div><div class="line">     */</div><div class="line">	<span class="keyword">private</span> <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">SingletonHolder</span> </span>&#123;</div><div class="line">	    <span class="comment">/**</span></div><div class="line">         * 静态初始化器，由JVM来保证线程安全</div><div class="line">         */</div><div class="line">		<span class="keyword">private</span> <span class="keyword">static</span> Singleton instance = <span class="keyword">new</span> Singleton();</div><div class="line">	&#125;</div><div class="line">	</div><div class="line">	<span class="function"><span class="keyword">private</span> <span class="title">Singleton</span><span class="params">()</span> </span>&#123;</div><div class="line">	&#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<h3 id="2-原理分析-1"><a href="#2-原理分析-1" class="headerlink" title="(2)原理分析"></a>(2)原理分析</h3><p>&emsp;&emsp;在这种方式中，使用了一个专门的内部类来初始化Singleton，JVM将推迟SingletonHolder的初始化操作，直到开始使用这个类时才初始化。并且在初始化的过程中JVM会去获取一个用于同步多个线程对同一个类进行初始化的锁，这样就不需要额外的同步。<strong>这种方式不仅能够保证线程安全，也能保证单例对象的唯一性，同时也延迟实例化，是一种非常推荐的方式</strong>。</p>
<center><br><img src="http://occl9k36n.bkt.clouddn.com/expression007.JPG" alt="表情内容)"><br></center>

<hr>
<h2 id="9-枚举方式"><a href="#9-枚举方式" class="headerlink" title="9.枚举方式"></a>9.枚举方式</h2><h3 id="1-实现方法"><a href="#1-实现方法" class="headerlink" title="(1)实现方法"></a>(1)实现方法</h3><p>从Java1.5起，可以通过使用枚举机制来实现单例模式：<br><figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="keyword">public</span> <span class="keyword">enum</span> Singleton &#123;</div><div class="line">	<span class="comment">// 定义枚举元素，他就是Singleton的一个实例</span></div><div class="line">	INSTANCE;</div><div class="line">	</div><div class="line">	<span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">doSomething</span><span class="params">()</span> </span>&#123;</div><div class="line">		<span class="comment">// do something</span></div><div class="line">	&#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p>调用方式<br><figure class="highlight java"><table><tr><td class="code"><pre><div class="line">Singleton singleton = Singleton.INSTANCE;</div><div class="line">singleton.doSomething();</div></pre></td></tr></table></figure></p>
<p>可以看到实现的代码非常的简洁，按照Joshua Bloch大神的原话来说：<br><strong>While this approach has yet to be widely adopted,a single-element enum type is the best way to implement a singleton.</strong></p>
<h3 id="2-序列化与反序列化的问题"><a href="#2-序列化与反序列化的问题" class="headerlink" title="(2)序列化与反序列化的问题"></a>(2)序列化与反序列化的问题</h3><p>&emsp;&emsp;在上述的几种单例模式实现中，在一个情况下它们会出现重新创建对象的情况，那就是<strong>反序列化</strong>。<br>&emsp;&emsp;通过序列化可以将一个单例的实例对象写到磁盘，然后再读回来，从而有效地获得一个实例。<strong>即使构造函数是私有的，反序列化时依然可以通过特殊的途径去创建类的一个新的实例</strong>，相当于调用该类的构造函数。反序列化操作提供一个很特别的钩子函数，类中具有一个私有的、被实例化的方法readResolve()，这个方法可以让开发人员控制对象的反序列化。例如，上述几个实例中如果要杜绝单例对象在被反序列化时重新生成对象，那么必须加入如下方法：<br><figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="function"><span class="keyword">private</span> Object <span class="title">readResolve</span><span class="params">()</span> <span class="keyword">throws</span> ObjectStreamException </span>&#123;</div><div class="line">    <span class="keyword">return</span> INSTANCE;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p>也就是在readResolve方法中将实例对象返回，而不是默认的重新生成一个新的对象。</p>
<h3 id="3-Java反射攻击"><a href="#3-Java反射攻击" class="headerlink" title="(3)Java反射攻击"></a>(3)Java反射攻击</h3><p>&emsp;&emsp;下面我们基于内部类实现的单例模式的方式，来演示一下通过JAVA的反射机制来“攻击”单例模式：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestMain</span> </span>&#123;</div><div class="line">	<span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> NoSuchMethodException, SecurityException, InstantiationException, IllegalAccessException, IllegalArgumentException, InvocationTargetException </span>&#123;</div><div class="line">		Class&lt;?&gt; classType = Singleton.class;  </div><div class="line">        Constructor&lt;?&gt; c = classType.getDeclaredConstructor(<span class="keyword">null</span>);  </div><div class="line">        c.setAccessible(<span class="keyword">true</span>);  </div><div class="line">        Singleton singleton1 = (Singleton) c.newInstance();  </div><div class="line">        Singleton singleton2 = Singleton.getSingleton();  </div><div class="line">        System.out.println(singleton1 == singleton2);  </div><div class="line">	&#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>&emsp;&emsp;运行结果：false，可以看到，通过反射获取构造函数，然后调用setAccessible(true)就可以调用私有的构造函数，所有singleton1和singleton2是两个不同的对象。如果要抵御这种攻击，可以修改构造器，让它在被要求创建第二个实例的时候抛出异常。<br>修改原有代码为：<br><figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Singleton</span> </span>&#123;</div><div class="line">	</div><div class="line">	<span class="function"><span class="keyword">public</span> <span class="keyword">static</span> Singleton <span class="title">getSingleton</span><span class="params">()</span> </span>&#123;</div><div class="line">		<span class="keyword">return</span> SingletonHolder.instance;</div><div class="line">	&#125;</div><div class="line">	</div><div class="line">	<span class="keyword">private</span> <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">SingletonHolder</span> </span>&#123;</div><div class="line">		<span class="keyword">private</span> <span class="keyword">static</span> Singleton instance = <span class="keyword">new</span> Singleton();</div><div class="line">	&#125;</div><div class="line">	</div><div class="line">	<span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">boolean</span> flag = <span class="keyword">false</span>; </div><div class="line">	</div><div class="line">	<span class="function"><span class="keyword">private</span> <span class="title">Singleton</span><span class="params">()</span> </span>&#123;</div><div class="line">    	<span class="keyword">synchronized</span>(Singleton.class) &#123;</div><div class="line">    		<span class="keyword">if</span>(flag == <span class="keyword">false</span>) &#123;</div><div class="line">    			flag = !flag;</div><div class="line">    		&#125; <span class="keyword">else</span> &#123;</div><div class="line">    			<span class="keyword">throw</span> <span class="keyword">new</span> RuntimeException(<span class="string">"单例模式被破坏！"</span>);  </div><div class="line">    		&#125;</div><div class="line">    	&#125;</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p>再次运行上面的测试代码：得到的结果为：<br><figure class="highlight java"><table><tr><td class="code"><pre><div class="line">Exception in thread <span class="string">"main"</span> java.lang.RuntimeException: 单例模式被破坏！</div><div class="line">	at com.danli.Singleton.&lt;init&gt;(Singleton.java:<span class="number">29</span>)</div><div class="line">	at com.danli.Singleton.getSingleton(Singleton.java:<span class="number">12</span>)</div><div class="line">	at com.danli.TestMain.main(TestMain.java:<span class="number">23</span>)</div></pre></td></tr></table></figure></p>
<p>可以看到，成功的阻止了单例模式被破坏。</p>
<p>但是我们如果直接基于枚举方式实现的单例模式进行同样的代码测试，会直接得到结果：<br><figure class="highlight java"><table><tr><td class="code"><pre><div class="line">Exception in thread <span class="string">"main"</span> java.lang.NoSuchMethodException: com.danli.Singleton.&lt;init&gt;()</div><div class="line">	at java.lang.Class.getConstructor0(Class.java:<span class="number">2730</span>)</div><div class="line">	at java.lang.Class.getDeclaredConstructor(Class.java:<span class="number">2004</span>)</div><div class="line">	at com.danli.TestMain.main(TestMain.java:<span class="number">20</span>)</div></pre></td></tr></table></figure></p>
<p><strong>可以看到，枚举方式实现的单例自己是可以避免反射攻击的</strong>。</p>
<h3 id="4-枚举方式的优点"><a href="#4-枚举方式的优点" class="headerlink" title="(4)枚举方式的优点"></a>(4)枚举方式的优点</h3><p>饿汉式、懒汉式、双重校验锁(DCL)还是静态内部类都存在的缺点：</p>
<blockquote>
<ul>
<li>都需要额外的工作(Serializable、transient、readResolve())来实现序列化，否则每次反序列化一个序列化的对象实例时都会创建一个新的实例。</li>
<li>可能会有人使用反射强行调用我们的私有构造器（如果要避免这种情况，可以修改构造器，让它在创建第二个实例的时候抛异常）。</li>
</ul>
</blockquote>
<p>&emsp;&emsp;枚举类很好的解决了这两个问题，使用枚举除了线程安全和防止反射强行调用构造器之外，还提供了自动序列化机制，防止反序列化的时候创建新的对象。因此，<strong>《EffectiveJava》Item3中推荐尽可能地使用枚举来实现单例。</strong><br>&emsp;&emsp;但是在Android中却不推荐这种用法，在Android官网<a href="https://developer.android.com/topic/performance/memory.html" target="_blank" rel="external">Manage Your App’s Memory</a>中有这样一段话：</p>
<blockquote>
<ul>
<li>Enums often require more than twice as much memory as static constants. You should strictly avoid using enums on Android.</li>
</ul>
</blockquote>
<p>&emsp;&emsp;意思就是枚举类这种写法虽然简单方便，但是内存占用上是静态变量的两倍以上，所以尽可能的避免这种写法。<br>&emsp;&emsp;不过网上有的建议是<strong>如果程序不是大量采用枚举，那么这种性能的体现是很小的，基本不会受到影响，不用特别在意</strong>。如果程序出现了性能问题，理论上这个地方就是一个性能优化点。</p>
<hr>
<h2 id="10-单例模式的扩展"><a href="#10-单例模式的扩展" class="headerlink" title="10.单例模式的扩展"></a>10.单例模式的扩展</h2><h3 id="1-定义"><a href="#1-定义" class="headerlink" title="(1)定义"></a>(1)定义</h3><p>&emsp;&emsp;上文的几种实现方式里，一个类都只产生一个对象。万一有天产品提的需求中，需要一个类只产能产生两三个对象呢？该怎么实现？</p>
<center><br><img src="http://occl9k36n.bkt.clouddn.com/expression005.jpg" alt="表情内容)"><br></center>


<p>&emsp;&emsp;这种需要产生固定数量对象的模式就叫做<strong>多例模式</strong>，实际上就是单例模式的自然推广，作为对象的创建模式，多例模式有以下的特点：</p>
<blockquote>
<ul>
<li>多例类可有多个实例；</li>
<li>多例类必须自己创建，管理自己的实例，并向外界提供自己的实例。</li>
</ul>
</blockquote>
<h3 id="2-应用实例"><a href="#2-应用实例" class="headerlink" title="(2)应用实例"></a>(2)应用实例</h3><p>&emsp;&emsp;喜欢打麻将的同学(捂脸)都知道，每一桌麻将牌局都需要两个骰子，因此骰子就应该是多例类，这里就以这个场景为例来说明多例模式的应用。<br>实现代码为：<br><figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Die</span> </span>&#123;</div><div class="line"></div><div class="line">	<span class="keyword">private</span> <span class="keyword">static</span> Die die1 = <span class="keyword">new</span> Die();</div><div class="line">	<span class="keyword">private</span> <span class="keyword">static</span> Die die2 = <span class="keyword">new</span> Die();</div><div class="line">	</div><div class="line">	<span class="function"><span class="keyword">private</span> <span class="title">Die</span><span class="params">()</span> </span>&#123;</div><div class="line">		</div><div class="line">	&#125;</div><div class="line">	</div><div class="line">	<span class="function"><span class="keyword">public</span> <span class="keyword">static</span> Die <span class="title">getInstance</span><span class="params">(<span class="keyword">int</span> whichOne)</span> </span>&#123;</div><div class="line">		<span class="keyword">if</span>(whichOne == <span class="number">1</span>) &#123;</div><div class="line">			<span class="keyword">return</span> die1;</div><div class="line">		&#125; <span class="keyword">else</span> &#123;</div><div class="line">			<span class="keyword">return</span> die2;</div><div class="line">		&#125;</div><div class="line">	&#125;</div><div class="line"></div><div class="line">	<span class="function"><span class="keyword">public</span> <span class="keyword">synchronized</span> <span class="keyword">int</span> <span class="title">dice</span><span class="params">()</span> </span>&#123;</div><div class="line">		Random rand = <span class="keyword">new</span> Random(System.currentTimeMillis());</div><div class="line">		<span class="keyword">int</span> value = rand.nextInt(<span class="number">6</span>);</div><div class="line">		value += <span class="number">1</span>;</div><div class="line">		<span class="keyword">return</span> value;</div><div class="line">	&#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p>&emsp;&emsp;在多例类Die中，使用了饿汉式方式创建了两个Die的实例，根据静态工厂方法的参数，工厂方法返还两个实例中的一个，Die对象调用die()方法代表掷骰子，这个方法会返还一个1–6之间的随机数，相当于骰子的点数。</p>
<h3 id="3-实践原则"><a href="#3-实践原则" class="headerlink" title="(3)实践原则"></a>(3)实践原则</h3><p>&emsp;&emsp;<strong>一个多例类可以使用静态变量存储所有的实例，特别是实例数目不多的时候，可以使用一个个的静态变量存储一个个的实例。当数目较多的时候，就需要使用Map等集合存储这些实例</strong>。<br>&emsp;&emsp;使用这种模式可以让我们在设计时决定在内存中有多少个实例，方便系统进行扩展，修正单例可能存在的性能问题，提高系统的相应速度。例如读取文件，我们可以在系统启动时完成初始化工作，在内存中启动固定数量的reader实例，然后在需要读取文件时就可以快速响应。</p>
<hr>
<h2 id="11-小结"><a href="#11-小结" class="headerlink" title="11.小结"></a>11.小结</h2><p>&emsp;&emsp;最后总结一下，不管哪种方案，时刻牢记单例模式的三大要点：</p>
<blockquote>
<ul>
<li>线程安全</li>
<li>延迟加载</li>
<li>序列化与反序列化安全</li>
</ul>
</blockquote>
<p>&emsp;&emsp;本文详细的分析了懒汉式，饿汉式，双重检查锁定，静态内部类，枚举五种方式的具体实现原理和优缺点，并简要介绍了单例模式的扩展–多例模式。希望大家看完之后能对单例模式有进一步的了解，并在日常工作中结合具体需求选择适合的单例模式实现。</p>
<h2 id="参考文献"><a href="#参考文献" class="headerlink" title="参考文献"></a>参考文献</h2><p><a href="http://www.ibm.com/developerworks/java/library/j-dcl/index.html" target="_blank" rel="external">1.Double-checked locking and the Singleton pattern</a><br><a href="http://www.infoq.com/cn/articles/double-checked-locking-with-delay-initialization" target="_blank" rel="external">2.双重检查锁定与延迟初始化</a><br><a href="http://blog.csdn.net/u013256816/article/details/50525335" target="_blank" rel="external">3.如何防止单例模式被JAVA反射攻击</a><br><a href="http://stormzhang.com/designpattern/2016/03/27/android-design-pattern-singleton/" target="_blank" rel="external">4.Android设计模式之单例模式</a><br><a href="http://stormzhang.com/designpattern/2016/04/04/singleton-extend/" target="_blank" rel="external">5.单例模式的一些注意点</a><br><a href="https://book.douban.com/subject/25843319/" target="_blank" rel="external">6.《设计模式之禅》第7章相关内容</a><br><a href="https://book.douban.com/subject/1214074/" target="_blank" rel="external">7.《Java与模式》第16–18章相关内容</a><br><a href="https://book.douban.com/subject/2243615/" target="_blank" rel="external">8.《Head First 设计模式》第5章相关内容</a></p>
]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;1-单例模式概述&quot;&gt;&lt;a href=&quot;#1-单例模式概述&quot; class=&quot;headerlink&quot; title=&quot;1.单例模式概述&quot;&gt;&lt;/a&gt;1.单例模式概述&lt;/h2&gt;&lt;h3 id=&quot;1-引言&quot;&gt;&lt;a href=&quot;#1-引言&quot; class=&quot;headerlink&quot; 
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title>Android中的广播使用小结</title>
    <link href="http://haojen.github.io/2016/12/23/summary-use-broadcast/"/>
    <id>http://haojen.github.io/2016/12/23/summary-use-broadcast/</id>
    <published>2016-12-23T13:09:11.000Z</published>
    <updated>2016-12-31T09:20:48.869Z</updated>
    
    <content type="html"><![CDATA[<hr>
<h2 id="1-广播的分类"><a href="#1-广播的分类" class="headerlink" title="1.广播的分类"></a>1.广播的分类</h2><h3 id="1-按照发送的方式分类"><a href="#1-按照发送的方式分类" class="headerlink" title="(1)按照发送的方式分类"></a>(1)按照发送的方式分类</h3><blockquote>
<ul>
<li>标准广播<br>是一种异步的方式来进行传播的，广播发出去之后，所有的广播接收者<strong>几乎是同一时间</strong>收到消息的。他们之间没有先后顺序可言，而且这种广播是没法被截断的。</li>
<li>有序广播<br>是一种同步执行的广播，在广播发出去之后，<strong>同一时刻只有一个广播接收器可以收到消息</strong>。当广播中的逻辑执行完成后，广播才会继续传播。</li>
</ul>
</blockquote>
<h3 id="2-按照注册的方式分类"><a href="#2-按照注册的方式分类" class="headerlink" title="(2)按照注册的方式分类"></a>(2)按照注册的方式分类</h3><blockquote>
<ul>
<li>动态注册广播<br>顾名思义，就是在代码中注册的。</li>
<li>静态注册广播<br>动态注册要求程序必须在运行时才能进行，有一定的局限性，如果我们需要在程序还没启动的时候就可以接收到注册的广播，就需要静态注册了。主要是在AndroidManifest中进行注册。</li>
</ul>
</blockquote>
<h3 id="3-按照定义的方式分类"><a href="#3-按照定义的方式分类" class="headerlink" title="(3)按照定义的方式分类"></a>(3)按照定义的方式分类</h3><blockquote>
<ul>
<li>系统广播<br>Android系统中内置了多个系统广播，每个系统广播都具有特定的intent-filter，其中主要包括具体的action，系统广播发出后，将被相应的BroadcastReceiver接收。系统广播在系统内部当特定事件发生时，由系统自动发出。</li>
<li>自定义广播<br>由应用程序开发者自己定义的广播</li>
</ul>
</blockquote>
<hr>
<h2 id="2-动态注册广播的实现"><a href="#2-动态注册广播的实现" class="headerlink" title="2.动态注册广播的实现"></a>2.动态注册广播的实现</h2><p>一段比较典型的实现代码为：</p>
<h3 id="1-实现一个广播接收器"><a href="#1-实现一个广播接收器" class="headerlink" title="(1)实现一个广播接收器"></a>(1)实现一个广播接收器</h3><figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MyBroadcastReceiver</span> <span class="keyword">extends</span> <span class="title">BroadcastReceiver</span> </span>&#123;</div><div class="line"></div><div class="line">    <span class="meta">@Override</span></div><div class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">onReceive</span><span class="params">(Context context, Intent intent)</span> </span>&#123;</div><div class="line">        Toast.makeText(context, <span class="string">"received in MyBroadcastReceiver"</span>, Toast.LENGTH_SHORT).show();</div><div class="line">        abortBroadcast();</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>主要就是继承一个BroadcastReceiver，实现onReceive方法，在其中实现自己的业务逻辑就可以了。</p>
<h3 id="2-注册广播"><a href="#2-注册广播" class="headerlink" title="(2)注册广播"></a>(2)注册广播</h3><figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MainActivity</span> <span class="keyword">extends</span> <span class="title">AppCompatActivity</span> </span>&#123;</div><div class="line"></div><div class="line">    <span class="keyword">private</span> IntentFilter intentFilter;</div><div class="line">    <span class="keyword">private</span> MyBroadcastReceiver myBroadcastReceiver;</div><div class="line"></div><div class="line">    <span class="meta">@Override</span></div><div class="line">    <span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">onCreate</span><span class="params">(Bundle savedInstanceState)</span> </span>&#123;</div><div class="line">        <span class="keyword">super</span>.onCreate(savedInstanceState);</div><div class="line">        setContentView(R.layout.activity_main);</div><div class="line">        intentFilter = <span class="keyword">new</span> IntentFilter();</div><div class="line">        intentFilter.addAction(<span class="string">"android.net.conn.CONNECTIVITY_CHANGE"</span>);</div><div class="line">        myBroadcastReceiver = <span class="keyword">new</span> MyBroadcastReceiver();</div><div class="line">        registerReceiver(myBroadcastReceiver, intentFilter);</div><div class="line">        </div><div class="line">        Button button = (Button) findViewById(R.id.button);</div><div class="line">        button.setOnClickListener(<span class="keyword">new</span> View.OnClickListener() &#123;</div><div class="line">            <span class="meta">@Override</span></div><div class="line">            <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">onClick</span><span class="params">(View v)</span> </span>&#123;</div><div class="line">                Intent intent = <span class="keyword">new</span> Intent(<span class="string">"android.net.conn.CONNECTIVITY_CHANGE"</span>);</div><div class="line">                sendBroadcast(intent); <span class="comment">// 发送广播</span></div><div class="line">            &#125;</div><div class="line">        &#125;);</div><div class="line">    &#125;</div><div class="line"></div><div class="line">    <span class="meta">@Override</span></div><div class="line">    <span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">onDestroy</span><span class="params">()</span> </span>&#123;</div><div class="line">        <span class="keyword">super</span>.onDestroy();</div><div class="line">        unregisterReceiver(myBroadcastReceiver);</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>这样MyBroadcastReceiver就可以收到相应的广播消息了。</p>
<hr>
<h2 id="3-静态注册广播的实现"><a href="#3-静态注册广播的实现" class="headerlink" title="3.静态注册广播的实现"></a>3.静态注册广播的实现</h2><p>还是用上面的按个MyBroadcastReceiver，只不过这次采用静态注册的方式<br>在manifest文件中增加如下的代码：<br><figure class="highlight"><table><tr><td class="code"><pre><div class="line">&lt;receiver</div><div class="line">    android:name=".MyBroadcastReceiver"</div><div class="line">    android:enabled="true"</div><div class="line">    android:exported="true"&gt;</div><div class="line">    &lt;intent-filter&gt;</div><div class="line">        &lt;action android:name="android.net.conn.CONNECTIVITY_CHANGE" /&gt;</div><div class="line">    &lt;/intent-filter&gt;</div><div class="line">&lt;/receiver&gt;</div></pre></td></tr></table></figure></p>
<p>几个相关解释：</p>
<blockquote>
<ul>
<li>android:exported<br>此BroadcastReceiver能否接收其他App发出的广播(其默认值是由receiver中有无intent-filter决定的，如果有intent-filter，默认值为true，否则为false);</li>
<li>android:name<br>此broadcastReceiver类名;</li>
<li>android:permission<br>如果设置，<strong>具有相应权限的广播发送方发送的广播才能被此broadcastReceiver所接收</strong>;</li>
<li>android:process<br>broadcastReceiver运行所处的进程。默认为App的进程。可以指定独立的进程(Android四大组件都可以通过此属性指定自己的独立进程);</li>
</ul>
</blockquote>
<hr>
<h2 id="4-静态注册广播与动态注册广播的区别"><a href="#4-静态注册广播与动态注册广播的区别" class="headerlink" title="4.静态注册广播与动态注册广播的区别"></a>4.静态注册广播与动态注册广播的区别</h2><blockquote>
<ul>
<li>静态注册即使App退出，仍然能接收到广播</li>
<li>动态注册时，当Activity退出，就接收不到广播了</li>
<li>但是<strong>静态注册即使App退出，仍然能接收到广播这种说法自Android 3.1开始有可能不再成立</strong>。</li>
</ul>
</blockquote>
<p>说明：<br>Android 3.1开始系统在Intent与广播相关的flag增加了参数：<br>A. FLAG_INCLUDE_STOPPED_PACKAGES：包含已经停止的包（停止：即包所在的进程已经退出）<br>B. FLAG_EXCLUDE_STOPPED_PACKAGES：不包含已经停止的包<br>自Android3.1开始，系统本身增加了对所有App当前是否处于运行状态的跟踪。在发送广播时，不管是什么广播类型，<strong>系统默认直接增加了值为FLAG_EXCLUDE_STOPPED_PACKAGES的flag</strong>，导致即使是静态注册的广播接收器，对于其所在进程已经退出的App，同样无法接收到广播。<br>因此对于系统广播，由于是系统内部直接发出的，无法更改此intent的flag值，因此，<strong>从3.1开始对于静态注册的接收系统广播的BroadcastReceiver，如果App进程已经退出，将不能接收到广播</strong>。<br>但是<strong>对于自定义的广播，可以通过覆写此flag为FLAG_INCLUDE_STOPPED_PACKAGES，使得静态注册的BroadcastReceiver，即使所在App进程已经退出，也能接收到广播，并会启动应用进程</strong>，但此时的BroadcastReceiver是新建的。<br>实现代码为：<br><figure class="highlight java"><table><tr><td class="code"><pre><div class="line">Intent intent = <span class="keyword">new</span> Intent();</div><div class="line">intent.setAction(BROADCAST_ACTION);</div><div class="line">intent.addFlags(Intent.FLAG_INCLUDE_STOPPED_PACKAGES);</div><div class="line">sendBroadcast(intent);</div></pre></td></tr></table></figure></p>
<p>在3.1以前，不少App可能通过静态注册方式监听各种系统广播，以此进行一些业务上的处理（如即使App已经退出，仍然能接收到，可以启动service等..）,<strong>3.1后，静态注册接受广播方式的改变，将直接导致此类方案不再可行。于是，通过将Service与App本身设置成不同的进程已经成为实现此类需求的可行替代方案</strong>。</p>
<h3 id="API变化详情参见Android官方文档"><a href="#API变化详情参见Android官方文档" class="headerlink" title="API变化详情参见Android官方文档"></a><a href="https://developer.android.com/about/versions/android-3.1.html#launchcontrols" target="_blank" rel="external">API变化详情参见Android官方文档</a></h3><hr>
<h2 id="5-有序广播"><a href="#5-有序广播" class="headerlink" title="5.有序广播"></a>5.有序广播</h2><p>前面介绍过，有序广播是异步方式传播的。指的是发送出去的广播被BroadcastReceiver按照先后循序接收。有序广播的定义过程与普通广播无异，只是其的主要发送方式变为：<br><figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="comment">/**</span></div><div class="line"> * Broadcast the given intent to all interested BroadcastReceivers, delivering</div><div class="line"> * them one at a time to allow more preferred receivers to consume the</div><div class="line"> * broadcast before it is delivered to less preferred receivers.  This</div><div class="line"> * call is asynchronous; it returns immediately, and you will continue</div><div class="line"> * executing while the receivers are run.</div><div class="line"> * <span class="doctag">@param</span> intent The Intent to broadcast; all receivers matching this</div><div class="line"> *               Intent will receive the broadcast.</div><div class="line"> * <span class="doctag">@param</span> receiverPermission (optional) String naming a permissions that</div><div class="line"> *               a receiver must hold in order to receive your broadcast.</div><div class="line"> *               If null, no permission is required.</div><div class="line"> */</div><div class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">abstract</span> <span class="keyword">void</span> <span class="title">sendOrderedBroadcast</span><span class="params">(Intent intent,</span></span></div><div class="line">        @Nullable String receiverPermission);</div></pre></td></tr></table></figure></p>
<p>其他的几种重载的方法可以参见官方文档。<br>有序广播的主要特点：</p>
<blockquote>
<ul>
<li>同级别接收是随机的(结合下一条)</li>
<li>同级别动态注册（代码中注册）高于静态注册（AndroidManifest中注册）</li>
<li>排序规则为：<strong>将当前系统中所有有效的动态注册和静态注册的BroadcastReceiver按照priority属性值从大到小排序</strong></li>
<li>先接收的BroadcastReceiver可以对此有序广播进行截断，使后面的BroadcastReceiver不再接收到此广播，也可以对广播进行修改，使后面的BroadcastReceiver接收到广播后解析得到错误的参数值。当然，一般情况下，不建议对有序广播进行此类操作，尤其是针对系统中的有序广播。实现截断的代码为：<figure class="highlight java"><table><tr><td class="code"><pre><div class="line">abortBroadcast();</div></pre></td></tr></table></figure>
</li>
</ul>
</blockquote>
<hr>
<h2 id="6-标准广播"><a href="#6-标准广播" class="headerlink" title="6.标准广播"></a>6.标准广播</h2><p>标准广播的主要特点为：</p>
<blockquote>
<ul>
<li>同级别接收先后是随机的（无序的）</li>
<li>级别低的后接收到广播</li>
<li>接收器不能截断广播的继续传播，也不能处理广播</li>
<li>同级别动态注册（代码中注册）高于静态注册（AndroidManifest中注册）</li>
</ul>
</blockquote>
<hr>
<h2 id="7-广播的安全性问题"><a href="#7-广播的安全性问题" class="headerlink" title="7.广播的安全性问题"></a>7.广播的安全性问题</h2><p>由前文阐述可知，Android中的广播可以跨进程甚至跨App直接通信，且<strong>exported属性在有intent-filter的情况下默认值是true</strong>，由此将可能出现的安全隐患如下：</p>
<blockquote>
<ul>
<li>其他App可能会针对性的发出与当前App intent-filter相匹配的广播，由此导致当前App不断接收到广播并处理；</li>
<li>其他App可以注册与当前App一致的intent-filter用于接收广播，获取广播具体信息。</li>
</ul>
</blockquote>
<p>无论哪种情形，这些安全隐患都确实是存在的。由此，业界常见的一些<strong>增加安全性的方案</strong>包括：</p>
<blockquote>
<ul>
<li>对于同一App内部发送和接收广播，将exported属性人为设置成false，使得非本App内部发出的此广播不被接收；</li>
<li>在广播发送和接收时，都增加上相应的permission，用于权限验证；</li>
<li>发送广播时，指定特定广播接收器所在的包名，具体是通过intent.setPackage(packageName)指定，这样此广播将只会发送到此包中的App内与之相匹配的有效广播接收器中。</li>
<li>采用LocalBroadcastManager的方式</li>
</ul>
</blockquote>
<p>下文分析几种常见的处理方案。</p>
<hr>
<h2 id="8-本地广播LocalBroadcastManager"><a href="#8-本地广播LocalBroadcastManager" class="headerlink" title="8.本地广播LocalBroadcastManager"></a>8.本地广播LocalBroadcastManager</h2><h3 id="1-LocalBroadcastManager的概念"><a href="#1-LocalBroadcastManager的概念" class="headerlink" title="(1)LocalBroadcastManager的概念"></a>(1)LocalBroadcastManager的概念</h3><p><strong>为了解决安全性问题，Android在android.support.v4.content包中引入了LocalBroadcastManager。</strong>按照官方文档的描述,使用LocalBroadcastManager有如下的优势：<br><figure class="highlight java"><table><tr><td class="code"><pre><div class="line">Helper to register <span class="keyword">for</span> and send broadcasts of Intents to local objects within your process. <span class="function">This has a number of advantages over sending global broadcasts with <span class="title">sendBroadcast</span><span class="params">(Intent)</span>:</span></div><div class="line"><span class="params">(<span class="number">1</span>)</span>You know that the data you are broadcasting won't leave your app, so don't need to worry about leaking <span class="keyword">private</span> data.</div><div class="line"><span class="params">(<span class="number">2</span>)</span>It is not possible <span class="keyword">for</span> other applications to send these broadcasts to your app, so you don't need to worry about having security holes they can exploit.</div><div class="line"><span class="params">(<span class="number">3</span>)</span>It is more efficient than sending a global broadcast through the system.</div></pre></td></tr></table></figure></p>
<p>也就是说，<strong>使用该机制发出的广播只能够在应用程序内部进行传递，并且广播接收器也只能接收来自本地应用程序发出的广播</strong>，这样所有的安全性问题都不存在了。</p>
<h3 id="2-LocalBroadcastManager使用范例"><a href="#2-LocalBroadcastManager使用范例" class="headerlink" title="(2)LocalBroadcastManager使用范例"></a>(2)LocalBroadcastManager使用范例</h3><figure class="highlight java"><table><tr><td class="code"><pre><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MainActivity</span> <span class="keyword">extends</span> <span class="title">AppCompatActivity</span> </span>&#123;</div><div class="line">    <span class="keyword">private</span> IntentFilter intentFilter;</div><div class="line">    <span class="keyword">private</span> LocalReceiver localReceiver;</div><div class="line">    <span class="keyword">private</span> LocalBroadcastManager localBroadcastManager;</div><div class="line"></div><div class="line">    <span class="meta">@Override</span></div><div class="line">    <span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">onCreate</span><span class="params">(Bundle savedInstanceState)</span> </span>&#123;</div><div class="line">        <span class="keyword">super</span>.onCreate(savedInstanceState);</div><div class="line">        setContentView(R.layout.activity_main);</div><div class="line">        localBroadcastManager = LocalBroadcastManager.getInstance(<span class="keyword">this</span>); <span class="comment">// 获取实例</span></div><div class="line">        Button button = (Button) findViewById(R.id.button);</div><div class="line">        button.setOnClickListener(<span class="keyword">new</span> View.OnClickListener() &#123;</div><div class="line">            <span class="meta">@Override</span></div><div class="line">            <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">onClick</span><span class="params">(View v)</span> </span>&#123;</div><div class="line">                Intent intent = <span class="keyword">new</span> Intent(<span class="string">"com.example.broadcasttest.LOCAL_BROADCAST"</span>);</div><div class="line">                localBroadcastManager.sendBroadcast(intent); <span class="comment">// 发送本地广播</span></div><div class="line">            &#125;</div><div class="line">        &#125;);</div><div class="line">        intentFilter = <span class="keyword">new</span> IntentFilter();</div><div class="line">        intentFilter.addAction(<span class="string">"com.example.broadcasttest.LOCAL_BROADCAST"</span>);</div><div class="line">        localReceiver = <span class="keyword">new</span> LocalReceiver();</div><div class="line">        localBroadcastManager.registerReceiver(localReceiver, intentFilter); <span class="comment">// 注册本地广播监听器</span></div><div class="line">    &#125;</div><div class="line"></div><div class="line">    <span class="meta">@Override</span></div><div class="line">    <span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">onDestroy</span><span class="params">()</span> </span>&#123;</div><div class="line">        <span class="keyword">super</span>.onDestroy();</div><div class="line">        localBroadcastManager.unregisterReceiver(localReceiver);</div><div class="line">    &#125;</div><div class="line"></div><div class="line">    <span class="class"><span class="keyword">class</span> <span class="title">LocalReceiver</span> <span class="keyword">extends</span> <span class="title">BroadcastReceiver</span> </span>&#123;</div><div class="line">        <span class="meta">@Override</span></div><div class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">onReceive</span><span class="params">(Context context, Intent intent)</span> </span>&#123;</div><div class="line">            Toast.makeText(context, <span class="string">"received local broadcast"</span>, Toast.LENGTH_SHORT).show();</div><div class="line">        &#125;</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<h3 id="LocalBroadcastManager的官方文档链接"><a href="#LocalBroadcastManager的官方文档链接" class="headerlink" title="LocalBroadcastManager的官方文档链接"></a><a href="https://developer.android.com/reference/android/support/v4/content/LocalBroadcastManager.html" target="_blank" rel="external">LocalBroadcastManager的官方文档链接</a></h3><hr>
<h2 id="9-自定义广播权限"><a href="#9-自定义广播权限" class="headerlink" title="9.自定义广播权限"></a>9.自定义广播权限</h2><h3 id="1-一个自定义权限的示例"><a href="#1-一个自定义权限的示例" class="headerlink" title="(1)一个自定义权限的示例"></a>(1)一个自定义权限的示例</h3><figure class="highlight"><table><tr><td class="code"><pre><div class="line">&lt;permission</div><div class="line">     android:name="com.self.define.permission.test"</div><div class="line">     android:label="BroadcastReceiverPermission"</div><div class="line">     android:protectionLevel="signature"&gt;</div><div class="line"> &lt;/permission&gt;</div></pre></td></tr></table></figure>
<p>在自定义权限时，<strong>通常会指定protectionLevel属性</strong>，常用的如下：</p>
<blockquote>
<ul>
<li>normal：默认的，应用安装前，用户可以看到相应的权限，但无需用户主动授权。</li>
<li>dangerous：normal安全级别控制以外的任何危险操作。需要dangerous级别权限时，Android会明确要求用户进行授权。常见的如：网络使用权限，相机使用权限及联系人信息使用权限等。</li>
<li>signature：它要求权限声明应用和权限使用应用使用相同的keystore进行签名。如果使用同一keystore，则该权限由系统授予，否则系统会拒绝。并且权限授予时，不会通知用户。它常用于应用内部。<strong>把protectionLevel声明为signature。如果别的应用使用的不是同一个签名文件，就没办法使用该权限，从而保护了自己的接收者</strong>。</li>
</ul>
</blockquote>
<h3 id="2-广播接收者"><a href="#2-广播接收者" class="headerlink" title="(2)广播接收者"></a>(2)广播接收者</h3><p>如果采用静态注册的方式：<br><figure class="highlight"><table><tr><td class="code"><pre><div class="line"> &lt;receiver</div><div class="line">    android:name=".common.MyBroadcastReceiver"</div><div class="line">    android:exported="false"</div><div class="line">    android:permission="com.self.define.permission.test"&gt;</div><div class="line">    &lt;intent-filter&gt;</div><div class="line">        &lt;action android:name="action.name"/&gt;</div><div class="line">    &lt;/intent-filter&gt;</div><div class="line">&lt;/receiver&gt;</div></pre></td></tr></table></figure></p>
<p>如果采用动态注册的方式<br>相应的API有：<br><figure class="highlight java"><table><tr><td class="code"><pre><div class="line">(<span class="number">1</span>)registerReceiver(BroadcastReceiver receiver, IntentFilter filter, String broadcastPermission, Handler scheduler)</div><div class="line">Register to receive intent broadcasts, to run in the context of scheduler.</div><div class="line">(<span class="number">2</span>)sendBroadcast(Intent intent, String receiverPermission)</div><div class="line">Broadcast the given intent to all interested BroadcastReceivers, allowing an optional required permission to be enforced.</div><div class="line">(<span class="number">3</span>)sendOrderedBroadcast(Intent intent, String receiverPermission)</div><div class="line">Broadcast the given intent to all interested BroadcastReceivers, delivering them one at a time to allow more preferred receivers to consume the broadcast before it is delivered to less preferred receivers.</div></pre></td></tr></table></figure></p>
<p>receiver是动态注册时，需要创建自己的使用权限，并且将protectionLevel设置为signature。这样，当别的应用和receiver所在的应用使用的签名不一样时，便不会启动该receiver。例如：<br><figure class="highlight java"><table><tr><td class="code"><pre><div class="line">MyBroadcastReceiver receiver = <span class="keyword">new</span> MyBroadcastReceiver();</div><div class="line">IntentFilter intentFilter = <span class="keyword">new</span> IntentFilter();</div><div class="line">intentFilter.addAction(<span class="string">"com.example.broadcast.test"</span>);</div><div class="line">intentFilter.addCategory(Intent.CATEGORY_DEFAULT);</div><div class="line"><span class="comment">//注册receiver时，直接指定发送者应该具有的权限。不然外部应用依旧可以触及到receiver</span></div><div class="line">registerReceiver(receiver, intentFilter, <span class="string">"com.self.define.permission.test"</span>, <span class="keyword">null</span>);</div></pre></td></tr></table></figure></p>
<p>在注册的时候，最关键的一点是用<strong>registerReceiver(BroadcastReceiver, IntentFilter, String, Handler)</strong>进行注册，而不是平常用的是<strong>registerReceiver(BroadcastReceiver, IntentFilter)</strong>。相较于后者，前者在注册的时候要求了发送者必须具有的权限。如果发送者没有该权限，那么发送者发送的广播即使经过IntentFilter的过滤，也不会被receiver接收。此时如果再自定义一个权限，并且将权限的protectionLevel设置为signature，那么外部应用便无法使用该权限，也就无法触及到该receiver。<br>发送广播的代码为：<br><figure class="highlight java"><table><tr><td class="code"><pre><div class="line">Intent intent = <span class="keyword">new</span> Intent(<span class="string">"com.example.broadcast.test.permission"</span>);</div><div class="line">sendBroadcast(intent,<span class="string">"com.self.define.permission.test"</span>);</div></pre></td></tr></table></figure></p>
<p><strong>另外需要在manifest文件中定义权限并声明</strong><br><figure class="highlight"><table><tr><td class="code"><pre><div class="line">&lt;permission</div><div class="line">    android:name="com.self.define.permission.test"</div><div class="line">    android:label="BroadcastReceiverPermission"</div><div class="line">    android:protectionLevel="signature"&gt;</div><div class="line">&lt;/permission&gt;</div><div class="line">&lt;uses-permission android:name="com.self.define.permission.test"/&gt;</div></pre></td></tr></table></figure></p>
<p><strong>切记要在<application>同级的位置配置使用到的权限</application></strong></p>
<hr>
<h2 id="10-其他几点补充"><a href="#10-其他几点补充" class="headerlink" title="10.其他几点补充"></a>10.其他几点补充</h2><h3 id="1-不同注册方式onReceive-context-intent-中的context具体类型"><a href="#1-不同注册方式onReceive-context-intent-中的context具体类型" class="headerlink" title="(1)不同注册方式onReceive(context, intent)中的context具体类型"></a>(1)不同注册方式onReceive(context, intent)中的context具体类型</h3><p>两个参数的官方定义为：<br><figure class="highlight java"><table><tr><td class="code"><pre><div class="line">Context: The Context in which the receiver is running.</div><div class="line">Intent: The Intent being received.</div></pre></td></tr></table></figure></p>
<p>BroadcastReceiver本身不是Context，其内部也不含有Context，但在onReceive(Context context, Intent intent)中有context参数。这个context随着receiver的注册方式的不同而不同：<br>静态注册：context为ReceiverRestrictedContext<br>动态注册：context为Activity的context<br>LocalBroadcastManager的动态注册：context为Application的context</p>
<h3 id="2-ANR问题"><a href="#2-ANR问题" class="headerlink" title="(2)ANR问题"></a>(2)ANR问题</h3><p>官方文档的描述：<br>When it runs on the main thread you should never perform long-running operations in it (<strong>there is a timeout of 10 seconds that the system allows before considering the receiver to be blocked and a candidate to be killed</strong>). You cannot launch a popup dialog in your implementation of onReceive().</p>
<hr>
<h2 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h2><h6 id="BroadcastReceiver文档链接"><a href="#BroadcastReceiver文档链接" class="headerlink" title="BroadcastReceiver文档链接"></a><a href="https://developer.android.com/reference/android/content/BroadcastReceiver.html" target="_blank" rel="external">BroadcastReceiver文档链接</a></h6><h6 id="四大组件之广播-Broadcast"><a href="#四大组件之广播-Broadcast" class="headerlink" title="四大组件之广播(Broadcast)"></a><a href="http://www.jianshu.com/p/d1decd1634f6" target="_blank" rel="external">四大组件之广播(Broadcast)</a></h6><h6 id="BroadcastReceiver与自定义权限"><a href="#BroadcastReceiver与自定义权限" class="headerlink" title="BroadcastReceiver与自定义权限"></a><a href="http://www.jianshu.com/p/a0e47c64027d" target="_blank" rel="external">BroadcastReceiver与自定义权限</a></h6><h6 id="Android总结篇系列：Android广播机制"><a href="#Android总结篇系列：Android广播机制" class="headerlink" title="Android总结篇系列：Android广播机制"></a><a href="http://www.jianshu.com/p/79134d8b3eba" target="_blank" rel="external">Android总结篇系列：Android广播机制</a></h6><h6 id="面试题-BroadcastReceiver"><a href="#面试题-BroadcastReceiver" class="headerlink" title="面试题-BroadcastReceiver"></a><a href="http://www.jianshu.com/p/57a1899c17fb" target="_blank" rel="external">面试题-BroadcastReceiver</a></h6>]]></content>
    
    <summary type="html">
    
      &lt;hr&gt;
&lt;h2 id=&quot;1-广播的分类&quot;&gt;&lt;a href=&quot;#1-广播的分类&quot; class=&quot;headerlink&quot; title=&quot;1.广播的分类&quot;&gt;&lt;/a&gt;1.广播的分类&lt;/h2&gt;&lt;h3 id=&quot;1-按照发送的方式分类&quot;&gt;&lt;a href=&quot;#1-按照发送的方式分类&quot; clas
    
    </summary>
    
    
  </entry>
  
</feed>
