<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>Hexo</title>
  
  <link href="/atom.xml" rel="self"/>
  
  <link href="http://yoursite.com/"/>
  <updated>2016-12-31T09:32:02.341Z</updated>
  <id>http://yoursite.com/</id>
  
  <author>
    <name>John Doe</name>
    
  </author>
  
  <generator uri="http://hexo.io/">Hexo</generator>
  
  <entry>
    <title>单例模式各版本的原理与实践</title>
    <link href="http://yoursite.com/2016/12/31/SingleDesginPattern/"/>
    <id>http://yoursite.com/2016/12/31/SingleDesginPattern/</id>
    <published>2016-12-31T09:16:41.000Z</published>
    <updated>2016-12-31T09:32:02.341Z</updated>
    
    <content type="html"><![CDATA[<h2 id="1-单例模式概述"><a href="#1-单例模式概述" class="headerlink" title="1.单例模式概述"></a>1.单例模式概述</h2><h3 id="1-引言"><a href="#1-引言" class="headerlink" title="(1)引言"></a>(1)引言</h3><p>&emsp;&emsp;单例模式是应用最广的模式之一，也是23种设计模式中最基本的一个。本文旨在总结通过Java实现单例模式的各个版本的优缺点及适用场景，详细分析如何实现线程安全的单例模式，并探讨单例模式的一些扩展。</p>
<h3 id="2-单例模式的定义"><a href="#2-单例模式的定义" class="headerlink" title="(2)单例模式的定义"></a>(2)单例模式的定义</h3><p><strong>&emsp;&emsp;Ensure a class has only one instance,and provide a global point of access to it.(确保某一个类只有一个实例，并且自行实例化并向整个系统提供这个实例)</strong><br>通用类图为：</p>
<center><br><img src="http://occl9k36n.bkt.clouddn.com/class_diagram_singleton_pattern_002.png" alt="类图源文件)"><br></center>

<p>&emsp;&emsp;Singleton类称为单例类，通过使用private的构造函数确保了在一个应用中只产生一个实例，并且是自行实例化的(在Singleton中自己使用new Singleton())。</p>
<h3 id="3-使用场景"><a href="#3-使用场景" class="headerlink" title="(3)使用场景"></a>(3)使用场景</h3><p>&emsp;&emsp;在一个系统中，要求一个类有且仅有一个对象，如果出现多个对象就会出现“不良反应”，可以采用单例模式，具体的场景如下：</p>
<blockquote>
<ul>
<li>要求生成唯一序列号的环境；</li>
<li>在整个项目中需要一个共享访问点或共享数据，例如一个Web页面上的计数器，可以不用把每次刷新都记录到数据库中，使用单例模式保持计数器的值，并确保是线程安全的；</li>
<li>创建一个对象需要消耗的资源过多，如要访问IO和数据库等资源；</li>
<li>需要定义大量的静态常量和静态方法(如工具类)的环境，可以采用单例模式(当然，也可以直接声明为static的方式)。</li>
</ul>
</blockquote>
<h3 id="4-优缺点"><a href="#4-优缺点" class="headerlink" title="(4)优缺点"></a>(4)优缺点</h3><p>单例模式的优点</p>
<blockquote>
<ul>
<li>由于单例模式<strong>在内存中只有一个实例，减少了内存开支</strong>，特别是一个对象需要频繁地创建销毁时，而且创建或销毁时性能又无法优化，单例模式的优势就非常明显；</li>
<li>由于单例模式只生成一个实例，所以减少了系统的性能开销，当一个对象的产生需要比较多的资源的时候，如读取配置，产生其他的依赖对象时，可以通过在应用启动的时候直接产生一个单例对象，然后用永久驻留内存的方式来解决；</li>
<li>单例模式可以避免对资源的多重占用，例如对一个写文件动作，由于只有一个实例存在内存中，避免对同一个资源文件的同时写操作；</li>
<li>单例模式可以在系统设置全局的访问点，优化和共享资源访问，例如可以设计一个单例类，负责所有数据表的映射处理</li>
</ul>
</blockquote>
<p>单例模式的缺点</p>
<blockquote>
<ul>
<li>单例模式一般没有接口，扩展困难，若要扩展，除了修改代码基本上没有第二种途径可以实现；</li>
<li><strong>单例模式与单一职责原则有冲突，一个类应该只实现一个逻辑，而不关心他是否是单例的</strong>，是不是要单例取决于环境，单例模式把“要单例”和业务逻辑融合在一个类中。</li>
</ul>
</blockquote>
<hr>
<h2 id="2-最基本的实现方式"><a href="#2-最基本的实现方式" class="headerlink" title="2.最基本的实现方式"></a>2.最基本的实现方式</h2><figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div></pre></td><td class="code"><pre><div class="line"></div><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Singleton</span> </span>&#123;</div><div class="line">	<span class="keyword">private</span> <span class="keyword">static</span> Singleton singleton;</div><div class="line">	<span class="comment">// 限制产生多个对象</span></div><div class="line">	<span class="function"><span class="keyword">private</span> <span class="title">Singleton</span><span class="params">()</span> </span>&#123;</div><div class="line">	&#125;</div><div class="line">	</div><div class="line">	<span class="comment">// 获得对象实例的方法</span></div><div class="line">	<span class="function"><span class="keyword">public</span> <span class="keyword">static</span> Singleton <span class="title">getSingleton</span><span class="params">()</span> </span>&#123;</div><div class="line">		<span class="keyword">if</span>(singleton == <span class="keyword">null</span>) &#123;</div><div class="line">			singleton = <span class="keyword">new</span> Singleton();</div><div class="line">		&#125;</div><div class="line">		<span class="keyword">return</span> singleton;</div><div class="line">	&#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>&emsp;&emsp;相信大多数同学在入门Java的阶段都见过这段代码。该方式在低并发的情况下尚不会出现问题，若系统压力增大，并发量增加时则可能在内存中出现多个实例，破坏设计的初衷。本文的后续就是围绕这种实现分析改进，探讨实现线程安全的单例模式的最佳实践。<br>&emsp;&emsp;为什么这种实现是线程不安全的呢？如一个线程A执行到<strong>singleton = new Singleton();</strong>这里，<strong>但还没有获得对象(对象初始化是需要时间的)</strong>，第二个线程B也在执行，执行到<strong>if(singleton == null)</strong>判断，那么线程B获得判断条件也是为真，于是继续运行下去，线程A获得了一个对象，线程B也获得了一个对象，在内存中就出现两个对象,造成单例模式的失效！！<br>&emsp;&emsp;所以根本原因在于可能存在多个线程并发的访问getSingleton()方法造成单例对象的多次创建，解决因多线程并发访问导致单例模式实效的最佳方法就是–不要使用多线程并发访问。(⊙o⊙)…</p>
<center><br><img src="http://occl9k36n.bkt.clouddn.com/expression003.jpg" alt="表情内容)"><br></center>

<hr>
<h2 id="3-饿汉式"><a href="#3-饿汉式" class="headerlink" title="3.饿汉式"></a>3.饿汉式</h2><h3 id="1-实现原理"><a href="#1-实现原理" class="headerlink" title="(1)实现原理"></a>(1)实现原理</h3><p>&emsp;&emsp;言归正传，上面说的问题其实就是对<strong>if(singleton == null)</strong>的判断失效造成<strong>singleton = new Singleton();</strong>可能会被多个线程并发的执行。饿汉式单例模式的实现的本质其实就是依赖类加载机制保证构造方法只会被执行一次。JVM在类的初始化阶段(即在Class被加载后，且被线程使用之前)，会执行类的初始化。<strong>在执行类的初始化期间，JVM会去获取一个锁。这个锁可以同步多个线程对同一个类的初始化</strong>。<br>饿汉式单例的实现代码为：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Singleton</span> </span>&#123;</div><div class="line">	<span class="keyword">private</span> <span class="keyword">static</span> Singleton singleton = <span class="keyword">new</span> Singleton();</div><div class="line">	</div><div class="line">	<span class="function"><span class="keyword">private</span> <span class="title">Singleton</span><span class="params">()</span> </span>&#123;</div><div class="line">	&#125;</div><div class="line">	</div><div class="line">	<span class="comment">// 获得对象实例的方法</span></div><div class="line">	<span class="function"><span class="keyword">public</span> <span class="keyword">static</span> Singleton <span class="title">getSingleton</span><span class="params">()</span> </span>&#123;</div><div class="line">		<span class="keyword">return</span> singleton;</div><div class="line">	&#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<h3 id="2-优缺点及适用场景"><a href="#2-优缺点及适用场景" class="headerlink" title="(2)优缺点及适用场景"></a>(2)优缺点及适用场景</h3><p>&emsp;&emsp;可以看到饿汉式的实现非常简单，<strong>适合那些在初始化时就要用到单例的情况</strong>，如果单例对象初始化非常快，而且占用内存非常小的时候这种方式是比较合适的，可以直接在应用启动时加载并初始化。<br>不适用的场景：</p>
<blockquote>
<ul>
<li>单例初始化的操作耗时比较长而应用对于启动速度又有要求;</li>
<li>单例的占用内存比较大;</li>
<li>单例只是在某个特定场景的情况下才会被使用，而一般情况下是不会使用的;</li>
</ul>
</blockquote>
<p>&emsp;&emsp;在上述的几种情况下使用饿汉式的单例模式是不合适的，这时候就需要<strong>用到懒汉式的方式去按需延迟加载单例</strong>。</p>
<hr>
<h2 id="4-利用同步锁机制实现的懒汉式"><a href="#4-利用同步锁机制实现的懒汉式" class="headerlink" title="4.利用同步锁机制实现的懒汉式"></a>4.利用同步锁机制实现的懒汉式</h2><h3 id="1-原理与实现"><a href="#1-原理与实现" class="headerlink" title="(1)原理与实现"></a>(1)原理与实现</h3><p>实现代码：</p>
<figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Singleton</span> </span>&#123;</div><div class="line">	<span class="keyword">private</span> <span class="keyword">static</span> Singleton singleton = <span class="keyword">null</span>;</div><div class="line">	</div><div class="line">	<span class="function"><span class="keyword">private</span> <span class="title">Singleton</span><span class="params">()</span> </span>&#123;</div><div class="line">	&#125;</div><div class="line">	</div><div class="line">	<span class="comment">// 获得对象实例的方法</span></div><div class="line">	<span class="function"><span class="keyword">public</span> <span class="keyword">static</span> Singleton <span class="title">getSingleton</span><span class="params">()</span> </span>&#123;</div><div class="line">		<span class="keyword">synchronized</span>(Singleton.class) &#123;</div><div class="line">			<span class="keyword">if</span>(singleton == <span class="keyword">null</span>) &#123;</div><div class="line">				singleton = <span class="keyword">new</span> Singleton();</div><div class="line">			&#125;</div><div class="line">		&#125;</div><div class="line">		<span class="keyword">return</span> singleton;</div><div class="line">	&#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>这种是最常见的懒汉式单例实现，<strong>使用同步锁synchronized(Singleton.class)防止多线程同时进入造成instance被多次实例化</strong>。但是他的缺陷也是非常明显的，就是每次在调用getSingleton()获取单例的实例的时候，都需要进行同步。<strong>事实上我们只想保证一次初始化成功，其余的快速返回而已，如果在getInstance频繁使用的地方就要考虑重新优化了。</strong></p>
<hr>
<h2 id="5-对同步锁机制实现懒汉式的改进–DCL"><a href="#5-对同步锁机制实现懒汉式的改进–DCL" class="headerlink" title="5.对同步锁机制实现懒汉式的改进–DCL"></a>5.对同步锁机制实现懒汉式的改进–DCL</h2><h3 id="1-原理与实现-1"><a href="#1-原理与实现-1" class="headerlink" title="(1)原理与实现"></a>(1)原理与实现</h3><p>&emsp;&emsp;由于synchronized（甚至是无竞争的synchronized）存在着巨大的性能开销。因此，人们想出了一个<strong>“聪明”的技巧：双重检查锁定（double-checked locking）。</strong>通过这种方式来降低同步的开销。下面是使用双重检查锁定来实现延迟初始化的代码实现：</p>
<figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Singleton</span> </span>&#123;</div><div class="line">	<span class="keyword">private</span> <span class="keyword">static</span> Singleton instance = <span class="keyword">null</span>;                 <span class="comment">//1</span></div><div class="line">	</div><div class="line">	<span class="comment">// 获得对象实例的方法</span></div><div class="line">	<span class="function"><span class="keyword">public</span> <span class="keyword">static</span> Singleton <span class="title">getSingleton</span><span class="params">()</span> </span>&#123;                  <span class="comment">//2</span></div><div class="line">		<span class="keyword">if</span>(instance == <span class="keyword">null</span>) &#123;                                <span class="comment">//3:第一次检查</span></div><div class="line">			<span class="keyword">synchronized</span>(Singleton.class) &#123;                   <span class="comment">//4:加锁</span></div><div class="line">				<span class="keyword">if</span>(instance == <span class="keyword">null</span>)                          <span class="comment">//5:第二次检查</span></div><div class="line">					instance = <span class="keyword">new</span> Singleton();               <span class="comment">//6:问题的根源产生</span></div><div class="line">			&#125;</div><div class="line">		&#125;</div><div class="line">		<span class="keyword">return</span> instance;</div><div class="line">	&#125;</div><div class="line">		</div><div class="line">	<span class="function"><span class="keyword">private</span> <span class="title">Singleton</span><span class="params">()</span> </span>&#123;</div><div class="line">	&#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>如上面的代码所示，它的“优点”如下：</p>
<blockquote>
<ul>
<li>在多个线程试图在同一时间创建对象时，会通过加锁来保证只有一个线程能创建对象；</li>
<li>在对象创建好了以后，执行getSingleton()方法将不需要获取锁，直接返回已经创建好的对象</li>
</ul>
</blockquote>
<p>&emsp;&emsp;<strong>双重检查模式看上去好像很完美，但这是一个错误的优化，在线程执行到上面所示的代码3处读取到singleton对象不为null时，singleton引用的对象可能还没有完成初始化</strong>。</p>
<h3 id="2-问题分析"><a href="#2-问题分析" class="headerlink" title="(2)问题分析"></a>(2)问题分析</h3><p>可能产生错误的场景</p>
<blockquote>
<ul>
<li>1.线程A进入getSingleton()方法;</li>
<li>2.因为此时instance为null，所以线程A进入synchronized块；</li>
<li>3.<strong>线程A执行 instance = new Singleton();  把实例变量instance设置成了非空。(注意，是在调用构造方法之前)</strong></li>
<li>4.线程A退出，线程B进入。</li>
<li>5.线程B检查instance是否为空，此时不为空（第三步的时候被线程A设置成了非空）。线程B返回instance的引用。(<strong>问题出现了，这时instance的引用并不是Singleton的实例，因为没有调用构造方法</strong>) </li>
<li>6.线程B退出，线程A进入；</li>
<li>7.线程A继续调用构造方法，完成instance的初始化，再返回。</li>
</ul>
</blockquote>
<h3 id="3-问题根源"><a href="#3-问题根源" class="headerlink" title="(3)问题根源"></a>(3)问题根源</h3><p>&emsp;&emsp;<strong>多线程问题，很大程度是由于非原子性造成的</strong>，如果我们每一个可能产生竞争的地方都是原子性的，那多线程需要考虑的东西就要少很多了。在上述程序中也是一样，我们看第六行代码：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">instance = <span class="keyword">new</span> Singleton();     <span class="comment">//6:问题的根源产生</span></div></pre></td></tr></table></figure></p>
<p>在JMM中，这行代码可以分解为3个过程：</p>
<figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">memory = allocate();            <span class="comment">//#1为对象分配内存空间</span></div><div class="line">init(memory);                   <span class="comment">//#2初始化</span></div><div class="line">instance = memory;              <span class="comment">//#3设置instance,将其指向刚分配的内存空间。</span></div></pre></td></tr></table></figure>
<p>上面的3行代码，<strong>如果是顺序执行，不会带来问题。但是，在某些JIT编译器上，#2和#3可能发生重排序</strong>。也就是说，重排序后，上面三个过程变成了：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">memory = allocate();            <span class="comment">//#1为对象分配内存空间</span></div><div class="line">instance = memory;              <span class="comment">//#2</span></div><div class="line">init(memory);                   <span class="comment">//#3初始化</span></div></pre></td></tr></table></figure></p>
<p>&emsp;&emsp;根据《The Java Language Specification, Java SE 7 Edition》一书中的内容：所有线程在执行java程序时必须要遵守intra-thread semantics。<strong>intra-thread semantics保证重排序不会改变单线程内的程序执行结果。换句话来说，intra-thread semantics允许那些在单线程内，不会改变单线程程序执行结果的重排序</strong>。上面三行伪代码的2和3之间虽然被重排序了，但这个重排序并不会违反intra-thread semantics。这个重排序在没有改变单线程程序的执行结果的前提下，可以提高程序的执行性能。<br>&emsp;&emsp;下面，再让我们看看多线程并发执行的时候的情况。请看下面的示意图：</p>
<center><br><img src="http://occl9k36n.bkt.clouddn.com/singleton_pattern_question_001.png" alt="表情内容)"><br></center>

<p>&emsp;&emsp;<strong>这里2和3虽然重排序了，但java内存模型的intra-thread semantics将确保2一定会排在4前面执行。因此线程A的intra-thread semantics没有改变。但2和3的重排序，将导致线程B在B1处判断出instance不为空，线程B接下来将访问instance引用的对象。此时，线程B将会访问到一个还未初始化的对象。</strong>。</p>
<p>&emsp;&emsp;分析清楚问题发生的根源之后，可以想出两个办法来实现线程安全的延迟初始化：</p>
<blockquote>
<ul>
<li>不允许2和3重排序；</li>
<li>允许2和3重排序，但不允许其他线程“看到”这个重排序。</li>
</ul>
</blockquote>
<p>&emsp;&emsp;后文介绍的解决方案就分别对应于上面这两点。</p>
<hr>
<h2 id="6-Java1-5以后安全的DCL版本"><a href="#6-Java1-5以后安全的DCL版本" class="headerlink" title="6.Java1.5以后安全的DCL版本"></a>6.Java1.5以后安全的DCL版本</h2><h3 id="1-实现代码"><a href="#1-实现代码" class="headerlink" title="(1)实现代码"></a>(1)实现代码</h3><figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Singleton</span> </span>&#123;</div><div class="line">	<span class="keyword">private</span> <span class="keyword">volatile</span> <span class="keyword">static</span> Singleton instance = <span class="keyword">null</span>;</div><div class="line">	</div><div class="line">	<span class="comment">// 获得对象实例的方法</span></div><div class="line">	<span class="function"><span class="keyword">public</span> <span class="keyword">static</span> Singleton <span class="title">getSingleton</span><span class="params">()</span> </span>&#123;</div><div class="line">		<span class="keyword">if</span>(instance == <span class="keyword">null</span>) &#123;</div><div class="line">			<span class="keyword">synchronized</span>(Singleton.class) &#123;</div><div class="line">				<span class="keyword">if</span>(instance == <span class="keyword">null</span>)</div><div class="line">					instance = <span class="keyword">new</span> Singleton();</div><div class="line">			&#125;</div><div class="line">		&#125;</div><div class="line">		<span class="keyword">return</span> instance;</div><div class="line">	&#125;</div><div class="line">		</div><div class="line">	<span class="function"><span class="keyword">private</span> <span class="title">Singleton</span><span class="params">()</span> </span>&#123;</div><div class="line">	&#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<h3 id="2-原理分析"><a href="#2-原理分析" class="headerlink" title="(2)原理分析"></a>(2)原理分析</h3><p>&emsp;&emsp;<strong>可以发现代码只做一点小的修改（把instance声明为volatile型），</strong>为什么volatile可以解决呢？回顾一下他的两层语义：</p>
<blockquote>
<ul>
<li>(1)可见性：指的是在一个线程中对该变量的修改会马上由工作内存（Work Memory）写回主内存（Main Memory）</li>
<li>(2)禁止指令重排序优化</li>
</ul>
</blockquote>
<p>&emsp;&emsp;当声明对象的引用为volatile后，“问题的根源”的三行伪代码中的2和3之间的重排序，在多线程环境中将会被禁止,从而在根本上解决了问题。但是很不幸，<strong>禁止指令重排优化这条语义直到jdk1.5以后才能正确工作。此前的JDK中即使将变量声明为volatile也无法完全避免重排序所导致的问题</strong>。所以，在jdk1.5版本前，双重检查锁形式的单例模式是无法保证线程安全的。<br>&emsp;&emsp;写到这里，可能有的同学会有疑问了：</p>
<center><br><img src="http://occl9k36n.bkt.clouddn.com/expression006.gif" alt="表情内容)"><br></center>

<hr>
<h2 id="7-Java1-4以前安全的DCL版本"><a href="#7-Java1-4以前安全的DCL版本" class="headerlink" title="7.Java1.4以前安全的DCL版本"></a>7.Java1.4以前安全的DCL版本</h2><p>&emsp;&emsp;额(⊙o⊙)…虽然现在的日常开发已经普遍在使用Java1.7甚至1.8了。不过尝试着探讨下在Jav1.4以前实现安全的DCL还是一个还有意思的话题。我自己也没有找到太确定的答案，这方面的资料也非常的少。下面给出的实现代码不一定能保证正确，贴出来仅供参考，欢迎有兴趣的同学在评论区留言分享一下经验。</p>
<center><br><img src="http://occl9k36n.bkt.clouddn.com/expression004.jpg" alt="表情内容)"><br></center>


<h3 id="2-实现代码及思路"><a href="#2-实现代码及思路" class="headerlink" title="(2)实现代码及思路"></a>(2)实现代码及思路</h3><figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Singleton</span> </span>&#123;</div><div class="line">    <span class="keyword">private</span> <span class="keyword">static</span> Singleton instance = <span class="keyword">null</span>;</div><div class="line">    <span class="comment">// 获得对象实例的方法</span></div><div class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> Singleton <span class="title">getSingleton</span><span class="params">()</span> </span>&#123;</div><div class="line">        <span class="keyword">if</span>(instance == <span class="keyword">null</span>) &#123;                                      <span class="comment">//1.第一次检查</span></div><div class="line">            <span class="keyword">synchronized</span>(Singleton.class) &#123;                         <span class="comment">//2.第一个synchronized块</span></div><div class="line">            	Singleton temp = instance;                          <span class="comment">//3.给临时变量temp赋值</span></div><div class="line">            	<span class="keyword">if</span>(temp == <span class="keyword">null</span>) &#123;                                  <span class="comment">//4.第二次检查</span></div><div class="line">            		<span class="keyword">synchronized</span>(Singleton.class) &#123;                 <span class="comment">//5.第二个synchronized块</span></div><div class="line">            			temp = <span class="keyword">new</span> Singleton();                     <span class="comment">//6.解决问题的关键地方</span></div><div class="line">            		&#125;</div><div class="line">            		instance = temp;                                <span class="comment">//7.把temp的引用赋值给instance</span></div><div class="line">            	&#125;</div><div class="line">            &#125;</div><div class="line">        &#125;</div><div class="line">        <span class="keyword">return</span> instance;</div><div class="line">    &#125;</div><div class="line">    </div><div class="line">    <span class="function"><span class="keyword">private</span> <span class="title">Singleton</span><span class="params">()</span> </span>&#123;</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>&emsp;&emsp;上面给出的代码中，很关键的地方在于在synchronized块中引入了一个临时变量Singleton temp，通过对temp的判空及相应的初始化，保证在代码7处，执行intance = temp;时，<strong>instance不为null且完成了初始化</strong>。</p>
<hr>
<h2 id="8-内部类方式"><a href="#8-内部类方式" class="headerlink" title="8.内部类方式"></a>8.内部类方式</h2><h3 id="1-实现"><a href="#1-实现" class="headerlink" title="(1)实现"></a>(1)实现</h3><p>&emsp;&emsp;在第五部分的结尾我们提到了两个办法来实现线程安全的延迟初始化，内部类方式正是基于第二种方法–<strong>线程之间重排序透明性</strong><br>实现代码为：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Singleton</span> </span>&#123;</div><div class="line">	<span class="comment">// 获得对象实例的方法</span></div><div class="line">	<span class="function"><span class="keyword">public</span> <span class="keyword">static</span> Singleton <span class="title">getSingleton</span><span class="params">()</span> </span>&#123;</div><div class="line">		<span class="keyword">return</span> SingletonHolder.instance;</div><div class="line">	&#125;</div><div class="line">	</div><div class="line">	<span class="comment">/**</span></div><div class="line">     * 静态内部类与外部类的实例没有绑定关系，而且只有被调用时才会</div><div class="line">     * 加载，从而实现了延迟加载</div><div class="line">     */</div><div class="line">	<span class="keyword">private</span> <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">SingletonHolder</span> </span>&#123;</div><div class="line">	    <span class="comment">/**</span></div><div class="line">         * 静态初始化器，由JVM来保证线程安全</div><div class="line">         */</div><div class="line">		<span class="keyword">private</span> <span class="keyword">static</span> Singleton instance = <span class="keyword">new</span> Singleton();</div><div class="line">	&#125;</div><div class="line">	</div><div class="line">	<span class="function"><span class="keyword">private</span> <span class="title">Singleton</span><span class="params">()</span> </span>&#123;</div><div class="line">	&#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<h3 id="2-原理分析-1"><a href="#2-原理分析-1" class="headerlink" title="(2)原理分析"></a>(2)原理分析</h3><p>&emsp;&emsp;在这种方式中，使用了一个专门的内部类来初始化Singleton，JVM将推迟SingletonHolder的初始化操作，直到开始使用这个类时才初始化。并且在初始化的过程中JVM会去获取一个用于同步多个线程对同一个类进行初始化的锁，这样就不需要额外的同步。<strong>这种方式不仅能够保证线程安全，也能保证单例对象的唯一性，同时也延迟实例化，是一种非常推荐的方式</strong>。</p>
<center><br><img src="http://occl9k36n.bkt.clouddn.com/expression007.JPG" alt="表情内容)"><br></center>

<hr>
<h2 id="9-枚举方式"><a href="#9-枚举方式" class="headerlink" title="9.枚举方式"></a>9.枚举方式</h2><h3 id="1-实现方法"><a href="#1-实现方法" class="headerlink" title="(1)实现方法"></a>(1)实现方法</h3><p>从Java1.5起，可以通过使用枚举机制来实现单例模式：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">public</span> <span class="keyword">enum</span> Singleton &#123;</div><div class="line">	<span class="comment">// 定义枚举元素，他就是Singleton的一个实例</span></div><div class="line">	INSTANCE;</div><div class="line">	</div><div class="line">	<span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">doSomething</span><span class="params">()</span> </span>&#123;</div><div class="line">		<span class="comment">// do something</span></div><div class="line">	&#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p>调用方式<br><figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">Singleton singleton = Singleton.INSTANCE;</div><div class="line">singleton.doSomething();</div></pre></td></tr></table></figure></p>
<p>可以看到实现的代码非常的简洁，按照Joshua Bloch大神的原话来说：<br><strong>While this approach has yet to be widely adopted,a single-element enum type is the best way to implement a singleton.</strong></p>
<h3 id="2-序列化与反序列化的问题"><a href="#2-序列化与反序列化的问题" class="headerlink" title="(2)序列化与反序列化的问题"></a>(2)序列化与反序列化的问题</h3><p>&emsp;&emsp;在上述的几种单例模式实现中，在一个情况下它们会出现重新创建对象的情况，那就是<strong>反序列化</strong>。<br>&emsp;&emsp;通过序列化可以将一个单例的实例对象写到磁盘，然后再读回来，从而有效地获得一个实例。<strong>即使构造函数是私有的，反序列化时依然可以通过特殊的途径去创建类的一个新的实例</strong>，相当于调用该类的构造函数。反序列化操作提供一个很特别的钩子函数，类中具有一个私有的、被实例化的方法readResolve()，这个方法可以让开发人员控制对象的反序列化。例如，上述几个实例中如果要杜绝单例对象在被反序列化时重新生成对象，那么必须加入如下方法：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">private</span> Object <span class="title">readResolve</span><span class="params">()</span> <span class="keyword">throws</span> ObjectStreamException </span>&#123;</div><div class="line">    <span class="keyword">return</span> INSTANCE;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p>也就是在readResolve方法中将实例对象返回，而不是默认的重新生成一个新的对象。</p>
<h3 id="3-Java反射攻击"><a href="#3-Java反射攻击" class="headerlink" title="(3)Java反射攻击"></a>(3)Java反射攻击</h3><p>&emsp;&emsp;下面我们基于内部类实现的单例模式的方式，来演示一下通过JAVA的反射机制来“攻击”单例模式：</p>
<figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestMain</span> </span>&#123;</div><div class="line">	<span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> NoSuchMethodException, SecurityException, InstantiationException, IllegalAccessException, IllegalArgumentException, InvocationTargetException </span>&#123;</div><div class="line">		Class&lt;?&gt; classType = Singleton.class;  </div><div class="line">        Constructor&lt;?&gt; c = classType.getDeclaredConstructor(<span class="keyword">null</span>);  </div><div class="line">        c.setAccessible(<span class="keyword">true</span>);  </div><div class="line">        Singleton singleton1 = (Singleton) c.newInstance();  </div><div class="line">        Singleton singleton2 = Singleton.getSingleton();  </div><div class="line">        System.out.println(singleton1 == singleton2);  </div><div class="line">	&#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>&emsp;&emsp;运行结果：false，可以看到，通过反射获取构造函数，然后调用setAccessible(true)就可以调用私有的构造函数，所有singleton1和singleton2是两个不同的对象。如果要抵御这种攻击，可以修改构造器，让它在被要求创建第二个实例的时候抛出异常。<br>修改原有代码为：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Singleton</span> </span>&#123;</div><div class="line">	</div><div class="line">	<span class="function"><span class="keyword">public</span> <span class="keyword">static</span> Singleton <span class="title">getSingleton</span><span class="params">()</span> </span>&#123;</div><div class="line">		<span class="keyword">return</span> SingletonHolder.instance;</div><div class="line">	&#125;</div><div class="line">	</div><div class="line">	<span class="keyword">private</span> <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">SingletonHolder</span> </span>&#123;</div><div class="line">		<span class="keyword">private</span> <span class="keyword">static</span> Singleton instance = <span class="keyword">new</span> Singleton();</div><div class="line">	&#125;</div><div class="line">	</div><div class="line">	<span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">boolean</span> flag = <span class="keyword">false</span>; </div><div class="line">	</div><div class="line">	<span class="function"><span class="keyword">private</span> <span class="title">Singleton</span><span class="params">()</span> </span>&#123;</div><div class="line">    	<span class="keyword">synchronized</span>(Singleton.class) &#123;</div><div class="line">    		<span class="keyword">if</span>(flag == <span class="keyword">false</span>) &#123;</div><div class="line">    			flag = !flag;</div><div class="line">    		&#125; <span class="keyword">else</span> &#123;</div><div class="line">    			<span class="keyword">throw</span> <span class="keyword">new</span> RuntimeException(<span class="string">"单例模式被破坏！"</span>);  </div><div class="line">    		&#125;</div><div class="line">    	&#125;</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p>再次运行上面的测试代码：得到的结果为：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">Exception in thread <span class="string">"main"</span> java.lang.RuntimeException: 单例模式被破坏！</div><div class="line">	at com.danli.Singleton.&lt;init&gt;(Singleton.java:<span class="number">29</span>)</div><div class="line">	at com.danli.Singleton.getSingleton(Singleton.java:<span class="number">12</span>)</div><div class="line">	at com.danli.TestMain.main(TestMain.java:<span class="number">23</span>)</div></pre></td></tr></table></figure></p>
<p>可以看到，成功的阻止了单例模式被破坏。</p>
<p>但是我们如果直接基于枚举方式实现的单例模式进行同样的代码测试，会直接得到结果：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">Exception in thread <span class="string">"main"</span> java.lang.NoSuchMethodException: com.danli.Singleton.&lt;init&gt;()</div><div class="line">	at java.lang.Class.getConstructor0(Class.java:<span class="number">2730</span>)</div><div class="line">	at java.lang.Class.getDeclaredConstructor(Class.java:<span class="number">2004</span>)</div><div class="line">	at com.danli.TestMain.main(TestMain.java:<span class="number">20</span>)</div></pre></td></tr></table></figure></p>
<p><strong>可以看到，枚举方式实现的单例自己是可以避免反射攻击的</strong>。</p>
<h3 id="4-枚举方式的优点"><a href="#4-枚举方式的优点" class="headerlink" title="(4)枚举方式的优点"></a>(4)枚举方式的优点</h3><p>饿汉式、懒汉式、双重校验锁(DCL)还是静态内部类都存在的缺点：</p>
<blockquote>
<ul>
<li>都需要额外的工作(Serializable、transient、readResolve())来实现序列化，否则每次反序列化一个序列化的对象实例时都会创建一个新的实例。</li>
<li>可能会有人使用反射强行调用我们的私有构造器（如果要避免这种情况，可以修改构造器，让它在创建第二个实例的时候抛异常）。</li>
</ul>
</blockquote>
<p>&emsp;&emsp;枚举类很好的解决了这两个问题，使用枚举除了线程安全和防止反射强行调用构造器之外，还提供了自动序列化机制，防止反序列化的时候创建新的对象。因此，<strong>《EffectiveJava》Item3中推荐尽可能地使用枚举来实现单例。</strong><br>&emsp;&emsp;但是在Android中却不推荐这种用法，在Android官网<a href="https://developer.android.com/topic/performance/memory.html" target="_blank" rel="external">Manage Your App’s Memory</a>中有这样一段话：</p>
<blockquote>
<ul>
<li>Enums often require more than twice as much memory as static constants. You should strictly avoid using enums on Android.</li>
</ul>
</blockquote>
<p>&emsp;&emsp;意思就是枚举类这种写法虽然简单方便，但是内存占用上是静态变量的两倍以上，所以尽可能的避免这种写法。<br>&emsp;&emsp;不过网上有的建议是<strong>如果程序不是大量采用枚举，那么这种性能的体现是很小的，基本不会受到影响，不用特别在意</strong>。如果程序出现了性能问题，理论上这个地方就是一个性能优化点。</p>
<hr>
<h2 id="10-单例模式的扩展"><a href="#10-单例模式的扩展" class="headerlink" title="10.单例模式的扩展"></a>10.单例模式的扩展</h2><h3 id="1-定义"><a href="#1-定义" class="headerlink" title="(1)定义"></a>(1)定义</h3><p>&emsp;&emsp;上文的几种实现方式里，一个类都只产生一个对象。万一有天产品提的需求中，需要一个类只产能产生两三个对象呢？该怎么实现？</p>
<center><br><img src="http://occl9k36n.bkt.clouddn.com/expression005.jpg" alt="表情内容)"><br></center>


<p>&emsp;&emsp;这种需要产生固定数量对象的模式就叫做<strong>多例模式</strong>，实际上就是单例模式的自然推广，作为对象的创建模式，多例模式有以下的特点：</p>
<blockquote>
<ul>
<li>多例类可有多个实例；</li>
<li>多例类必须自己创建，管理自己的实例，并向外界提供自己的实例。</li>
</ul>
</blockquote>
<h3 id="2-应用实例"><a href="#2-应用实例" class="headerlink" title="(2)应用实例"></a>(2)应用实例</h3><p>&emsp;&emsp;喜欢打麻将的同学(捂脸)都知道，每一桌麻将牌局都需要两个骰子，因此骰子就应该是多例类，这里就以这个场景为例来说明多例模式的应用。<br>实现代码为：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Die</span> </span>&#123;</div><div class="line"></div><div class="line">	<span class="keyword">private</span> <span class="keyword">static</span> Die die1 = <span class="keyword">new</span> Die();</div><div class="line">	<span class="keyword">private</span> <span class="keyword">static</span> Die die2 = <span class="keyword">new</span> Die();</div><div class="line">	</div><div class="line">	<span class="function"><span class="keyword">private</span> <span class="title">Die</span><span class="params">()</span> </span>&#123;</div><div class="line">		</div><div class="line">	&#125;</div><div class="line">	</div><div class="line">	<span class="function"><span class="keyword">public</span> <span class="keyword">static</span> Die <span class="title">getInstance</span><span class="params">(<span class="keyword">int</span> whichOne)</span> </span>&#123;</div><div class="line">		<span class="keyword">if</span>(whichOne == <span class="number">1</span>) &#123;</div><div class="line">			<span class="keyword">return</span> die1;</div><div class="line">		&#125; <span class="keyword">else</span> &#123;</div><div class="line">			<span class="keyword">return</span> die2;</div><div class="line">		&#125;</div><div class="line">	&#125;</div><div class="line"></div><div class="line">	<span class="function"><span class="keyword">public</span> <span class="keyword">synchronized</span> <span class="keyword">int</span> <span class="title">dice</span><span class="params">()</span> </span>&#123;</div><div class="line">		Random rand = <span class="keyword">new</span> Random(System.currentTimeMillis());</div><div class="line">		<span class="keyword">int</span> value = rand.nextInt(<span class="number">6</span>);</div><div class="line">		value += <span class="number">1</span>;</div><div class="line">		<span class="keyword">return</span> value;</div><div class="line">	&#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p>&emsp;&emsp;在多例类Die中，使用了饿汉式方式创建了两个Die的实例，根据静态工厂方法的参数，工厂方法返还两个实例中的一个，Die对象调用die()方法代表掷骰子，这个方法会返还一个1–6之间的随机数，相当于骰子的点数。</p>
<h3 id="3-实践原则"><a href="#3-实践原则" class="headerlink" title="(3)实践原则"></a>(3)实践原则</h3><p>&emsp;&emsp;<strong>一个多例类可以使用静态变量存储所有的实例，特别是实例数目不多的时候，可以使用一个个的静态变量存储一个个的实例。当数目较多的时候，就需要使用Map等集合存储这些实例</strong>。<br>&emsp;&emsp;使用这种模式可以让我们在设计时决定在内存中有多少个实例，方便系统进行扩展，修正单例可能存在的性能问题，提高系统的相应速度。例如读取文件，我们可以在系统启动时完成初始化工作，在内存中启动固定数量的reader实例，然后在需要读取文件时就可以快速响应。</p>
<hr>
<h2 id="11-小结"><a href="#11-小结" class="headerlink" title="11.小结"></a>11.小结</h2><p>&emsp;&emsp;最后总结一下，不管哪种方案，时刻牢记单例模式的三大要点：</p>
<blockquote>
<ul>
<li>线程安全</li>
<li>延迟加载</li>
<li>序列化与反序列化安全</li>
</ul>
</blockquote>
<p>&emsp;&emsp;本文详细的分析了懒汉式，饿汉式，双重检查锁定，静态内部类，枚举五种方式的具体实现原理和优缺点，并简要介绍了单例模式的扩展–多例模式。希望大家看完之后能对单例模式有进一步的了解，并在日常工作中结合具体需求选择适合的单例模式实现。</p>
<h2 id="参考文献"><a href="#参考文献" class="headerlink" title="参考文献"></a>参考文献</h2><p><a href="http://www.ibm.com/developerworks/java/library/j-dcl/index.html" target="_blank" rel="external">1.Double-checked locking and the Singleton pattern</a><br><a href="http://www.infoq.com/cn/articles/double-checked-locking-with-delay-initialization" target="_blank" rel="external">2.双重检查锁定与延迟初始化</a><br><a href="http://blog.csdn.net/u013256816/article/details/50525335" target="_blank" rel="external">3.如何防止单例模式被JAVA反射攻击</a><br><a href="http://stormzhang.com/designpattern/2016/03/27/android-design-pattern-singleton/" target="_blank" rel="external">4.Android设计模式之单例模式</a><br><a href="http://stormzhang.com/designpattern/2016/04/04/singleton-extend/" target="_blank" rel="external">5.单例模式的一些注意点</a><br><a href="https://book.douban.com/subject/25843319/" target="_blank" rel="external">6.《设计模式之禅》第7章相关内容</a><br><a href="https://book.douban.com/subject/1214074/" target="_blank" rel="external">7.《Java与模式》第16–18章相关内容</a><br><a href="https://book.douban.com/subject/2243615/" target="_blank" rel="external">8.《Head First 设计模式》第5章相关内容</a></p>
]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;1-单例模式概述&quot;&gt;&lt;a href=&quot;#1-单例模式概述&quot; class=&quot;headerlink&quot; title=&quot;1.单例模式概述&quot;&gt;&lt;/a&gt;1.单例模式概述&lt;/h2&gt;&lt;h3 id=&quot;1-引言&quot;&gt;&lt;a href=&quot;#1-引言&quot; class=&quot;headerlink&quot; 
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title>Android中的广播使用小结</title>
    <link href="http://yoursite.com/2016/12/23/summary-use-broadcast/"/>
    <id>http://yoursite.com/2016/12/23/summary-use-broadcast/</id>
    <published>2016-12-23T13:09:11.000Z</published>
    <updated>2016-12-31T09:20:48.869Z</updated>
    
    <content type="html"><![CDATA[<hr>
<h2 id="1-广播的分类"><a href="#1-广播的分类" class="headerlink" title="1.广播的分类"></a>1.广播的分类</h2><h3 id="1-按照发送的方式分类"><a href="#1-按照发送的方式分类" class="headerlink" title="(1)按照发送的方式分类"></a>(1)按照发送的方式分类</h3><blockquote>
<ul>
<li>标准广播<br>是一种异步的方式来进行传播的，广播发出去之后，所有的广播接收者<strong>几乎是同一时间</strong>收到消息的。他们之间没有先后顺序可言，而且这种广播是没法被截断的。</li>
<li>有序广播<br>是一种同步执行的广播，在广播发出去之后，<strong>同一时刻只有一个广播接收器可以收到消息</strong>。当广播中的逻辑执行完成后，广播才会继续传播。</li>
</ul>
</blockquote>
<h3 id="2-按照注册的方式分类"><a href="#2-按照注册的方式分类" class="headerlink" title="(2)按照注册的方式分类"></a>(2)按照注册的方式分类</h3><blockquote>
<ul>
<li>动态注册广播<br>顾名思义，就是在代码中注册的。</li>
<li>静态注册广播<br>动态注册要求程序必须在运行时才能进行，有一定的局限性，如果我们需要在程序还没启动的时候就可以接收到注册的广播，就需要静态注册了。主要是在AndroidManifest中进行注册。</li>
</ul>
</blockquote>
<h3 id="3-按照定义的方式分类"><a href="#3-按照定义的方式分类" class="headerlink" title="(3)按照定义的方式分类"></a>(3)按照定义的方式分类</h3><blockquote>
<ul>
<li>系统广播<br>Android系统中内置了多个系统广播，每个系统广播都具有特定的intent-filter，其中主要包括具体的action，系统广播发出后，将被相应的BroadcastReceiver接收。系统广播在系统内部当特定事件发生时，由系统自动发出。</li>
<li>自定义广播<br>由应用程序开发者自己定义的广播</li>
</ul>
</blockquote>
<hr>
<h2 id="2-动态注册广播的实现"><a href="#2-动态注册广播的实现" class="headerlink" title="2.动态注册广播的实现"></a>2.动态注册广播的实现</h2><p>一段比较典型的实现代码为：</p>
<h3 id="1-实现一个广播接收器"><a href="#1-实现一个广播接收器" class="headerlink" title="(1)实现一个广播接收器"></a>(1)实现一个广播接收器</h3><figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MyBroadcastReceiver</span> <span class="keyword">extends</span> <span class="title">BroadcastReceiver</span> </span>&#123;</div><div class="line"></div><div class="line">    <span class="meta">@Override</span></div><div class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">onReceive</span><span class="params">(Context context, Intent intent)</span> </span>&#123;</div><div class="line">        Toast.makeText(context, <span class="string">"received in MyBroadcastReceiver"</span>, Toast.LENGTH_SHORT).show();</div><div class="line">        abortBroadcast();</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>主要就是继承一个BroadcastReceiver，实现onReceive方法，在其中实现自己的业务逻辑就可以了。</p>
<h3 id="2-注册广播"><a href="#2-注册广播" class="headerlink" title="(2)注册广播"></a>(2)注册广播</h3><figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div><div class="line">28</div><div class="line">29</div><div class="line">30</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MainActivity</span> <span class="keyword">extends</span> <span class="title">AppCompatActivity</span> </span>&#123;</div><div class="line"></div><div class="line">    <span class="keyword">private</span> IntentFilter intentFilter;</div><div class="line">    <span class="keyword">private</span> MyBroadcastReceiver myBroadcastReceiver;</div><div class="line"></div><div class="line">    <span class="meta">@Override</span></div><div class="line">    <span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">onCreate</span><span class="params">(Bundle savedInstanceState)</span> </span>&#123;</div><div class="line">        <span class="keyword">super</span>.onCreate(savedInstanceState);</div><div class="line">        setContentView(R.layout.activity_main);</div><div class="line">        intentFilter = <span class="keyword">new</span> IntentFilter();</div><div class="line">        intentFilter.addAction(<span class="string">"android.net.conn.CONNECTIVITY_CHANGE"</span>);</div><div class="line">        myBroadcastReceiver = <span class="keyword">new</span> MyBroadcastReceiver();</div><div class="line">        registerReceiver(myBroadcastReceiver, intentFilter);</div><div class="line">        </div><div class="line">        Button button = (Button) findViewById(R.id.button);</div><div class="line">        button.setOnClickListener(<span class="keyword">new</span> View.OnClickListener() &#123;</div><div class="line">            <span class="meta">@Override</span></div><div class="line">            <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">onClick</span><span class="params">(View v)</span> </span>&#123;</div><div class="line">                Intent intent = <span class="keyword">new</span> Intent(<span class="string">"android.net.conn.CONNECTIVITY_CHANGE"</span>);</div><div class="line">                sendBroadcast(intent); <span class="comment">// 发送广播</span></div><div class="line">            &#125;</div><div class="line">        &#125;);</div><div class="line">    &#125;</div><div class="line"></div><div class="line">    <span class="meta">@Override</span></div><div class="line">    <span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">onDestroy</span><span class="params">()</span> </span>&#123;</div><div class="line">        <span class="keyword">super</span>.onDestroy();</div><div class="line">        unregisterReceiver(myBroadcastReceiver);</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>这样MyBroadcastReceiver就可以收到相应的广播消息了。</p>
<hr>
<h2 id="3-静态注册广播的实现"><a href="#3-静态注册广播的实现" class="headerlink" title="3.静态注册广播的实现"></a>3.静态注册广播的实现</h2><p>还是用上面的按个MyBroadcastReceiver，只不过这次采用静态注册的方式<br>在manifest文件中增加如下的代码：<br><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line">&lt;receiver</div><div class="line">    android:name=".MyBroadcastReceiver"</div><div class="line">    android:enabled="true"</div><div class="line">    android:exported="true"&gt;</div><div class="line">    &lt;intent-filter&gt;</div><div class="line">        &lt;action android:name="android.net.conn.CONNECTIVITY_CHANGE" /&gt;</div><div class="line">    &lt;/intent-filter&gt;</div><div class="line">&lt;/receiver&gt;</div></pre></td></tr></table></figure></p>
<p>几个相关解释：</p>
<blockquote>
<ul>
<li>android:exported<br>此BroadcastReceiver能否接收其他App发出的广播(其默认值是由receiver中有无intent-filter决定的，如果有intent-filter，默认值为true，否则为false);</li>
<li>android:name<br>此broadcastReceiver类名;</li>
<li>android:permission<br>如果设置，<strong>具有相应权限的广播发送方发送的广播才能被此broadcastReceiver所接收</strong>;</li>
<li>android:process<br>broadcastReceiver运行所处的进程。默认为App的进程。可以指定独立的进程(Android四大组件都可以通过此属性指定自己的独立进程);</li>
</ul>
</blockquote>
<hr>
<h2 id="4-静态注册广播与动态注册广播的区别"><a href="#4-静态注册广播与动态注册广播的区别" class="headerlink" title="4.静态注册广播与动态注册广播的区别"></a>4.静态注册广播与动态注册广播的区别</h2><blockquote>
<ul>
<li>静态注册即使App退出，仍然能接收到广播</li>
<li>动态注册时，当Activity退出，就接收不到广播了</li>
<li>但是<strong>静态注册即使App退出，仍然能接收到广播这种说法自Android 3.1开始有可能不再成立</strong>。</li>
</ul>
</blockquote>
<p>说明：<br>Android 3.1开始系统在Intent与广播相关的flag增加了参数：<br>A. FLAG_INCLUDE_STOPPED_PACKAGES：包含已经停止的包（停止：即包所在的进程已经退出）<br>B. FLAG_EXCLUDE_STOPPED_PACKAGES：不包含已经停止的包<br>自Android3.1开始，系统本身增加了对所有App当前是否处于运行状态的跟踪。在发送广播时，不管是什么广播类型，<strong>系统默认直接增加了值为FLAG_EXCLUDE_STOPPED_PACKAGES的flag</strong>，导致即使是静态注册的广播接收器，对于其所在进程已经退出的App，同样无法接收到广播。<br>因此对于系统广播，由于是系统内部直接发出的，无法更改此intent的flag值，因此，<strong>从3.1开始对于静态注册的接收系统广播的BroadcastReceiver，如果App进程已经退出，将不能接收到广播</strong>。<br>但是<strong>对于自定义的广播，可以通过覆写此flag为FLAG_INCLUDE_STOPPED_PACKAGES，使得静态注册的BroadcastReceiver，即使所在App进程已经退出，也能接收到广播，并会启动应用进程</strong>，但此时的BroadcastReceiver是新建的。<br>实现代码为：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">Intent intent = <span class="keyword">new</span> Intent();</div><div class="line">intent.setAction(BROADCAST_ACTION);</div><div class="line">intent.addFlags(Intent.FLAG_INCLUDE_STOPPED_PACKAGES);</div><div class="line">sendBroadcast(intent);</div></pre></td></tr></table></figure></p>
<p>在3.1以前，不少App可能通过静态注册方式监听各种系统广播，以此进行一些业务上的处理（如即使App已经退出，仍然能接收到，可以启动service等..）,<strong>3.1后，静态注册接受广播方式的改变，将直接导致此类方案不再可行。于是，通过将Service与App本身设置成不同的进程已经成为实现此类需求的可行替代方案</strong>。</p>
<h3 id="API变化详情参见Android官方文档"><a href="#API变化详情参见Android官方文档" class="headerlink" title="API变化详情参见Android官方文档"></a><a href="https://developer.android.com/about/versions/android-3.1.html#launchcontrols" target="_blank" rel="external">API变化详情参见Android官方文档</a></h3><hr>
<h2 id="5-有序广播"><a href="#5-有序广播" class="headerlink" title="5.有序广播"></a>5.有序广播</h2><p>前面介绍过，有序广播是异步方式传播的。指的是发送出去的广播被BroadcastReceiver按照先后循序接收。有序广播的定义过程与普通广播无异，只是其的主要发送方式变为：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div></pre></td><td class="code"><pre><div class="line"><span class="comment">/**</span></div><div class="line"> * Broadcast the given intent to all interested BroadcastReceivers, delivering</div><div class="line"> * them one at a time to allow more preferred receivers to consume the</div><div class="line"> * broadcast before it is delivered to less preferred receivers.  This</div><div class="line"> * call is asynchronous; it returns immediately, and you will continue</div><div class="line"> * executing while the receivers are run.</div><div class="line"> * <span class="doctag">@param</span> intent The Intent to broadcast; all receivers matching this</div><div class="line"> *               Intent will receive the broadcast.</div><div class="line"> * <span class="doctag">@param</span> receiverPermission (optional) String naming a permissions that</div><div class="line"> *               a receiver must hold in order to receive your broadcast.</div><div class="line"> *               If null, no permission is required.</div><div class="line"> */</div><div class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">abstract</span> <span class="keyword">void</span> <span class="title">sendOrderedBroadcast</span><span class="params">(Intent intent,</span></span></div><div class="line">        @Nullable String receiverPermission);</div></pre></td></tr></table></figure></p>
<p>其他的几种重载的方法可以参见官方文档。<br>有序广播的主要特点：</p>
<blockquote>
<ul>
<li>同级别接收是随机的(结合下一条)</li>
<li>同级别动态注册（代码中注册）高于静态注册（AndroidManifest中注册）</li>
<li>排序规则为：<strong>将当前系统中所有有效的动态注册和静态注册的BroadcastReceiver按照priority属性值从大到小排序</strong></li>
<li>先接收的BroadcastReceiver可以对此有序广播进行截断，使后面的BroadcastReceiver不再接收到此广播，也可以对广播进行修改，使后面的BroadcastReceiver接收到广播后解析得到错误的参数值。当然，一般情况下，不建议对有序广播进行此类操作，尤其是针对系统中的有序广播。实现截断的代码为：<figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">abortBroadcast();</div></pre></td></tr></table></figure>
</li>
</ul>
</blockquote>
<hr>
<h2 id="6-标准广播"><a href="#6-标准广播" class="headerlink" title="6.标准广播"></a>6.标准广播</h2><p>标准广播的主要特点为：</p>
<blockquote>
<ul>
<li>同级别接收先后是随机的（无序的）</li>
<li>级别低的后接收到广播</li>
<li>接收器不能截断广播的继续传播，也不能处理广播</li>
<li>同级别动态注册（代码中注册）高于静态注册（AndroidManifest中注册）</li>
</ul>
</blockquote>
<hr>
<h2 id="7-广播的安全性问题"><a href="#7-广播的安全性问题" class="headerlink" title="7.广播的安全性问题"></a>7.广播的安全性问题</h2><p>由前文阐述可知，Android中的广播可以跨进程甚至跨App直接通信，且<strong>exported属性在有intent-filter的情况下默认值是true</strong>，由此将可能出现的安全隐患如下：</p>
<blockquote>
<ul>
<li>其他App可能会针对性的发出与当前App intent-filter相匹配的广播，由此导致当前App不断接收到广播并处理；</li>
<li>其他App可以注册与当前App一致的intent-filter用于接收广播，获取广播具体信息。</li>
</ul>
</blockquote>
<p>无论哪种情形，这些安全隐患都确实是存在的。由此，业界常见的一些<strong>增加安全性的方案</strong>包括：</p>
<blockquote>
<ul>
<li>对于同一App内部发送和接收广播，将exported属性人为设置成false，使得非本App内部发出的此广播不被接收；</li>
<li>在广播发送和接收时，都增加上相应的permission，用于权限验证；</li>
<li>发送广播时，指定特定广播接收器所在的包名，具体是通过intent.setPackage(packageName)指定，这样此广播将只会发送到此包中的App内与之相匹配的有效广播接收器中。</li>
<li>采用LocalBroadcastManager的方式</li>
</ul>
</blockquote>
<p>下文分析几种常见的处理方案。</p>
<hr>
<h2 id="8-本地广播LocalBroadcastManager"><a href="#8-本地广播LocalBroadcastManager" class="headerlink" title="8.本地广播LocalBroadcastManager"></a>8.本地广播LocalBroadcastManager</h2><h3 id="1-LocalBroadcastManager的概念"><a href="#1-LocalBroadcastManager的概念" class="headerlink" title="(1)LocalBroadcastManager的概念"></a>(1)LocalBroadcastManager的概念</h3><p><strong>为了解决安全性问题，Android在android.support.v4.content包中引入了LocalBroadcastManager。</strong>按照官方文档的描述,使用LocalBroadcastManager有如下的优势：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">Helper to register <span class="keyword">for</span> and send broadcasts of Intents to local objects within your process. <span class="function">This has a number of advantages over sending global broadcasts with <span class="title">sendBroadcast</span><span class="params">(Intent)</span>:</span></div><div class="line"><span class="params">(<span class="number">1</span>)</span>You know that the data you are broadcasting won't leave your app, so don't need to worry about leaking <span class="keyword">private</span> data.</div><div class="line"><span class="params">(<span class="number">2</span>)</span>It is not possible <span class="keyword">for</span> other applications to send these broadcasts to your app, so you don't need to worry about having security holes they can exploit.</div><div class="line"><span class="params">(<span class="number">3</span>)</span>It is more efficient than sending a global broadcast through the system.</div></pre></td></tr></table></figure></p>
<p>也就是说，<strong>使用该机制发出的广播只能够在应用程序内部进行传递，并且广播接收器也只能接收来自本地应用程序发出的广播</strong>，这样所有的安全性问题都不存在了。</p>
<h3 id="2-LocalBroadcastManager使用范例"><a href="#2-LocalBroadcastManager使用范例" class="headerlink" title="(2)LocalBroadcastManager使用范例"></a>(2)LocalBroadcastManager使用范例</h3><figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div><div class="line">28</div><div class="line">29</div><div class="line">30</div><div class="line">31</div><div class="line">32</div><div class="line">33</div><div class="line">34</div><div class="line">35</div><div class="line">36</div><div class="line">37</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MainActivity</span> <span class="keyword">extends</span> <span class="title">AppCompatActivity</span> </span>&#123;</div><div class="line">    <span class="keyword">private</span> IntentFilter intentFilter;</div><div class="line">    <span class="keyword">private</span> LocalReceiver localReceiver;</div><div class="line">    <span class="keyword">private</span> LocalBroadcastManager localBroadcastManager;</div><div class="line"></div><div class="line">    <span class="meta">@Override</span></div><div class="line">    <span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">onCreate</span><span class="params">(Bundle savedInstanceState)</span> </span>&#123;</div><div class="line">        <span class="keyword">super</span>.onCreate(savedInstanceState);</div><div class="line">        setContentView(R.layout.activity_main);</div><div class="line">        localBroadcastManager = LocalBroadcastManager.getInstance(<span class="keyword">this</span>); <span class="comment">// 获取实例</span></div><div class="line">        Button button = (Button) findViewById(R.id.button);</div><div class="line">        button.setOnClickListener(<span class="keyword">new</span> View.OnClickListener() &#123;</div><div class="line">            <span class="meta">@Override</span></div><div class="line">            <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">onClick</span><span class="params">(View v)</span> </span>&#123;</div><div class="line">                Intent intent = <span class="keyword">new</span> Intent(<span class="string">"com.example.broadcasttest.LOCAL_BROADCAST"</span>);</div><div class="line">                localBroadcastManager.sendBroadcast(intent); <span class="comment">// 发送本地广播</span></div><div class="line">            &#125;</div><div class="line">        &#125;);</div><div class="line">        intentFilter = <span class="keyword">new</span> IntentFilter();</div><div class="line">        intentFilter.addAction(<span class="string">"com.example.broadcasttest.LOCAL_BROADCAST"</span>);</div><div class="line">        localReceiver = <span class="keyword">new</span> LocalReceiver();</div><div class="line">        localBroadcastManager.registerReceiver(localReceiver, intentFilter); <span class="comment">// 注册本地广播监听器</span></div><div class="line">    &#125;</div><div class="line"></div><div class="line">    <span class="meta">@Override</span></div><div class="line">    <span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">onDestroy</span><span class="params">()</span> </span>&#123;</div><div class="line">        <span class="keyword">super</span>.onDestroy();</div><div class="line">        localBroadcastManager.unregisterReceiver(localReceiver);</div><div class="line">    &#125;</div><div class="line"></div><div class="line">    <span class="class"><span class="keyword">class</span> <span class="title">LocalReceiver</span> <span class="keyword">extends</span> <span class="title">BroadcastReceiver</span> </span>&#123;</div><div class="line">        <span class="meta">@Override</span></div><div class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">onReceive</span><span class="params">(Context context, Intent intent)</span> </span>&#123;</div><div class="line">            Toast.makeText(context, <span class="string">"received local broadcast"</span>, Toast.LENGTH_SHORT).show();</div><div class="line">        &#125;</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<h3 id="LocalBroadcastManager的官方文档链接"><a href="#LocalBroadcastManager的官方文档链接" class="headerlink" title="LocalBroadcastManager的官方文档链接"></a><a href="https://developer.android.com/reference/android/support/v4/content/LocalBroadcastManager.html" target="_blank" rel="external">LocalBroadcastManager的官方文档链接</a></h3><hr>
<h2 id="9-自定义广播权限"><a href="#9-自定义广播权限" class="headerlink" title="9.自定义广播权限"></a>9.自定义广播权限</h2><h3 id="1-一个自定义权限的示例"><a href="#1-一个自定义权限的示例" class="headerlink" title="(1)一个自定义权限的示例"></a>(1)一个自定义权限的示例</h3><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line">&lt;permission</div><div class="line">     android:name="com.self.define.permission.test"</div><div class="line">     android:label="BroadcastReceiverPermission"</div><div class="line">     android:protectionLevel="signature"&gt;</div><div class="line"> &lt;/permission&gt;</div></pre></td></tr></table></figure>
<p>在自定义权限时，<strong>通常会指定protectionLevel属性</strong>，常用的如下：</p>
<blockquote>
<ul>
<li>normal：默认的，应用安装前，用户可以看到相应的权限，但无需用户主动授权。</li>
<li>dangerous：normal安全级别控制以外的任何危险操作。需要dangerous级别权限时，Android会明确要求用户进行授权。常见的如：网络使用权限，相机使用权限及联系人信息使用权限等。</li>
<li>signature：它要求权限声明应用和权限使用应用使用相同的keystore进行签名。如果使用同一keystore，则该权限由系统授予，否则系统会拒绝。并且权限授予时，不会通知用户。它常用于应用内部。<strong>把protectionLevel声明为signature。如果别的应用使用的不是同一个签名文件，就没办法使用该权限，从而保护了自己的接收者</strong>。</li>
</ul>
</blockquote>
<h3 id="2-广播接收者"><a href="#2-广播接收者" class="headerlink" title="(2)广播接收者"></a>(2)广播接收者</h3><p>如果采用静态注册的方式：<br><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line"> &lt;receiver</div><div class="line">    android:name=".common.MyBroadcastReceiver"</div><div class="line">    android:exported="false"</div><div class="line">    android:permission="com.self.define.permission.test"&gt;</div><div class="line">    &lt;intent-filter&gt;</div><div class="line">        &lt;action android:name="action.name"/&gt;</div><div class="line">    &lt;/intent-filter&gt;</div><div class="line">&lt;/receiver&gt;</div></pre></td></tr></table></figure></p>
<p>如果采用动态注册的方式<br>相应的API有：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line">(<span class="number">1</span>)registerReceiver(BroadcastReceiver receiver, IntentFilter filter, String broadcastPermission, Handler scheduler)</div><div class="line">Register to receive intent broadcasts, to run in the context of scheduler.</div><div class="line">(<span class="number">2</span>)sendBroadcast(Intent intent, String receiverPermission)</div><div class="line">Broadcast the given intent to all interested BroadcastReceivers, allowing an optional required permission to be enforced.</div><div class="line">(<span class="number">3</span>)sendOrderedBroadcast(Intent intent, String receiverPermission)</div><div class="line">Broadcast the given intent to all interested BroadcastReceivers, delivering them one at a time to allow more preferred receivers to consume the broadcast before it is delivered to less preferred receivers.</div></pre></td></tr></table></figure></p>
<p>receiver是动态注册时，需要创建自己的使用权限，并且将protectionLevel设置为signature。这样，当别的应用和receiver所在的应用使用的签名不一样时，便不会启动该receiver。例如：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line">MyBroadcastReceiver receiver = <span class="keyword">new</span> MyBroadcastReceiver();</div><div class="line">IntentFilter intentFilter = <span class="keyword">new</span> IntentFilter();</div><div class="line">intentFilter.addAction(<span class="string">"com.example.broadcast.test"</span>);</div><div class="line">intentFilter.addCategory(Intent.CATEGORY_DEFAULT);</div><div class="line"><span class="comment">//注册receiver时，直接指定发送者应该具有的权限。不然外部应用依旧可以触及到receiver</span></div><div class="line">registerReceiver(receiver, intentFilter, <span class="string">"com.self.define.permission.test"</span>, <span class="keyword">null</span>);</div></pre></td></tr></table></figure></p>
<p>在注册的时候，最关键的一点是用<strong>registerReceiver(BroadcastReceiver, IntentFilter, String, Handler)</strong>进行注册，而不是平常用的是<strong>registerReceiver(BroadcastReceiver, IntentFilter)</strong>。相较于后者，前者在注册的时候要求了发送者必须具有的权限。如果发送者没有该权限，那么发送者发送的广播即使经过IntentFilter的过滤，也不会被receiver接收。此时如果再自定义一个权限，并且将权限的protectionLevel设置为signature，那么外部应用便无法使用该权限，也就无法触及到该receiver。<br>发送广播的代码为：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">Intent intent = <span class="keyword">new</span> Intent(<span class="string">"com.example.broadcast.test.permission"</span>);</div><div class="line">sendBroadcast(intent,<span class="string">"com.self.define.permission.test"</span>);</div></pre></td></tr></table></figure></p>
<p><strong>另外需要在manifest文件中定义权限并声明</strong><br><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line">&lt;permission</div><div class="line">    android:name="com.self.define.permission.test"</div><div class="line">    android:label="BroadcastReceiverPermission"</div><div class="line">    android:protectionLevel="signature"&gt;</div><div class="line">&lt;/permission&gt;</div><div class="line">&lt;uses-permission android:name="com.self.define.permission.test"/&gt;</div></pre></td></tr></table></figure></p>
<p><strong>切记要在<application>同级的位置配置使用到的权限</application></strong></p>
<hr>
<h2 id="10-其他几点补充"><a href="#10-其他几点补充" class="headerlink" title="10.其他几点补充"></a>10.其他几点补充</h2><h3 id="1-不同注册方式onReceive-context-intent-中的context具体类型"><a href="#1-不同注册方式onReceive-context-intent-中的context具体类型" class="headerlink" title="(1)不同注册方式onReceive(context, intent)中的context具体类型"></a>(1)不同注册方式onReceive(context, intent)中的context具体类型</h3><p>两个参数的官方定义为：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">Context: The Context in which the receiver is running.</div><div class="line">Intent: The Intent being received.</div></pre></td></tr></table></figure></p>
<p>BroadcastReceiver本身不是Context，其内部也不含有Context，但在onReceive(Context context, Intent intent)中有context参数。这个context随着receiver的注册方式的不同而不同：<br>静态注册：context为ReceiverRestrictedContext<br>动态注册：context为Activity的context<br>LocalBroadcastManager的动态注册：context为Application的context</p>
<h3 id="2-ANR问题"><a href="#2-ANR问题" class="headerlink" title="(2)ANR问题"></a>(2)ANR问题</h3><p>官方文档的描述：<br>When it runs on the main thread you should never perform long-running operations in it (<strong>there is a timeout of 10 seconds that the system allows before considering the receiver to be blocked and a candidate to be killed</strong>). You cannot launch a popup dialog in your implementation of onReceive().</p>
<hr>
<h2 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h2><h6 id="BroadcastReceiver文档链接"><a href="#BroadcastReceiver文档链接" class="headerlink" title="BroadcastReceiver文档链接"></a><a href="https://developer.android.com/reference/android/content/BroadcastReceiver.html" target="_blank" rel="external">BroadcastReceiver文档链接</a></h6><h6 id="四大组件之广播-Broadcast"><a href="#四大组件之广播-Broadcast" class="headerlink" title="四大组件之广播(Broadcast)"></a><a href="http://www.jianshu.com/p/d1decd1634f6" target="_blank" rel="external">四大组件之广播(Broadcast)</a></h6><h6 id="BroadcastReceiver与自定义权限"><a href="#BroadcastReceiver与自定义权限" class="headerlink" title="BroadcastReceiver与自定义权限"></a><a href="http://www.jianshu.com/p/a0e47c64027d" target="_blank" rel="external">BroadcastReceiver与自定义权限</a></h6><h6 id="Android总结篇系列：Android广播机制"><a href="#Android总结篇系列：Android广播机制" class="headerlink" title="Android总结篇系列：Android广播机制"></a><a href="http://www.jianshu.com/p/79134d8b3eba" target="_blank" rel="external">Android总结篇系列：Android广播机制</a></h6><h6 id="面试题-BroadcastReceiver"><a href="#面试题-BroadcastReceiver" class="headerlink" title="面试题-BroadcastReceiver"></a><a href="http://www.jianshu.com/p/57a1899c17fb" target="_blank" rel="external">面试题-BroadcastReceiver</a></h6>]]></content>
    
    <summary type="html">
    
      &lt;hr&gt;
&lt;h2 id=&quot;1-广播的分类&quot;&gt;&lt;a href=&quot;#1-广播的分类&quot; class=&quot;headerlink&quot; title=&quot;1.广播的分类&quot;&gt;&lt;/a&gt;1.广播的分类&lt;/h2&gt;&lt;h3 id=&quot;1-按照发送的方式分类&quot;&gt;&lt;a href=&quot;#1-按照发送的方式分类&quot; clas
    
    </summary>
    
    
  </entry>
  
</feed>
